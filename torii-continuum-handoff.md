# Continuum — Session Handover

**Current state (v0.2.85-alpha — RETENTION + AUTH-GATE + DEMO-MODE):** Three items shipped together; **not deployed from the subagent** (parent hands the operator the deploy block). (1) **Retention** — `ops/torii-disk-retention.sh` now prunes `/home/continuum/app.quarantine-*` (keep newest `RET_QUARANTINE_KEEP`=3 regardless of age; delete older than `RET_QUARANTINE_AGE_DAYS`=3), strictly name-anchored with a hard refuse on protected basenames; Ansible knobs `continuum_retention_quarantine_{parent,age_days,keep}`; fix for the 2026-07-20 quarantine disk-fill that broke the v0.2.84-alpha deploy. Shell test `pass=97 fail=0`. (2) **Auth-gate hardening** — multi-tab sign-out via a localStorage sign-out sentinel: `endSession()` broadcasts, a `storage` listener in `main.js` ends the reacting tab `localOnly` (no loop) and bounces to login; extended `shell-signout.test.js`; new `main.js` route-table source-structure test in `nav-guard.test.js`. (3) **Public demo mode** — signed-out `/demo/*` read-only mockup from obviously-fake fixtures (`src/demo/demo-fixtures.js`, every human string carries DEMO/Sample, fake Cashu balance 21042 sats). `nav-guard` marks `/demo` + `/demo/*` public; a signed-in operator on a demo route is redirected to the real screen (`demoRedirect`). Views take `{ demo, fixtures }` and swap data source via `demoSource()` — **no view fork**; demo makes **zero** agent/network calls (dashboard drops ProviderCard, routstr drops polls) and gates every CTA to login; persistent orange "DEMO MODE — fake data" banner; subtle "View demo" peek on the login card. Tests: `demo-fixtures`, `demo-mode-banner`, `demo-no-network` (asserts zero agent calls when `demo:true`), nav-guard demo rules. Release test counts: root vitest **1348 pass**, agent node:test **307 pass**, ops shell **97 pass**; `npm run build` clean. Branch `feat/v0.2.85-retention-authgate-demo`; version bumped root+agent+lockfiles → 0.2.85-alpha. **Deviation:** brief literally named the Ansible vars `retention_quarantine_*` but the repo convention is `continuum_retention_*`, so they ship as `continuum_retention_quarantine_*`.

**Prior state (v0.2.84-alpha — ROUTSTR-TOPUP-QR-1):** The Routstr page's top-up flow is now a **Lightning-invoice QR** payable from a phone. Clicking **Top Up** opens a modal with an editable amount (preset chips 500/1000/5000/21000, default 1000, hard-capped by `cashu.max_mint_sats`, default 100_000) and a **source toggle**: a **Cashu mint quote** (NUT-04) or an **NWC-issued invoice** (NIP-47 `make_invoice`, offered only once a wallet with the `make_invoice` capability is connected). The invoice renders as a scannable QR (`lightning:<BOLT11>`) via a **dependency-free vendored encoder** (`src/views/qr.js`, Nayuki MIT port) plus a copy button; the modal polls for settlement and updates the balance. **How to top up:** open Routstr → Top Up → pick amount + source → scan the QR (or tap Copy) and pay from any Lightning wallet → the modal detects payment and credits the balance. The legacy **paste-a-Cashu-token** path is preserved as a secondary link in the same modal, and the chat-dock `focusTopUp` handoff still opens this modal. Admin-gated routes reuse the `/api/wallet/*` middleware; a full BOLT11 is never logged (first-10 + last-6 only) and preimages are never logged; `checkMintQuote` writes its `minted:true` marker before minting proofs so a re-poll never double-mints. **Not deployed at authoring time** — the enabled deploy timer / live pin unchanged. Branch `feat/routstr-topup-lightning-qr`, annotated tag v0.2.84-alpha. Deviation: vendored QR ~9.4KB minified (3.5KB gzipped), above the ≤6KB target.

**Current state (v0.2.87-alpha — MEMORY-ACTIVATION-1):** A signed-in owner who lands on `#/memory` while their bot's memory is **locked** (`unlocked_for_owner:false`) now gets one unmistakable, guided way to turn it on. The Memory view fetches the **authoritative** per-owner state from `GET /api/memory` (`data.unlocked_for_owner`) before rendering anything; when locked it shows a **dominant first-run activation panel** (`data-testid="activation-panel"`) explaining that memory is owner-controlled, **encrypted in this browser before anything is persisted**, and requires the owner's signature. A single primary CTA **"Activate private memory"** (`data-testid="activate-memory"`) drives a pure, DOM-free state machine (`src/views/memory-activation.js`) through eight explicit, recoverable states: `ready → requesting-signature → activating → success`, plus `signer-rejected`, `signer-unavailable`, and `error`. The flow: `POST /api/memory/activate/challenge` → the owner signs a **kind-22242** challenge in their own NIP-07 signer (the SAME authorization shape as login — no manual event construction) → `GET /api/memory/ciphertexts` → the browser NIP-44-decrypts each blob → `POST /api/memory/activate {event, entries}`. The agent verifies the signature via `auth.verifyActionSignature()` (owner-bound to the session pubkey, single-use challenge → replay protection), unlocks via the SAME `memoryCache.unlock()` path (no parallel protocol), and appends a metadata-only `memory.activate` audit line (owner-prefix + entry count — never plaintext). Success is confirmed **only** by re-reading `unlocked_for_owner:true` (never optimism), after which the normal Memory Console is revealed **in place, no reload**. Already-unlocked owners bypass onboarding entirely. **Not deployed at authoring time.** Feature merged as PR #98 (`a342eef`, merge `9bab202`). **Release-hygiene note:** the intended `v0.2.84-alpha` tag name was pre-consumed on the remote by the unrelated Routstr top-up, and a stop-gap `v0.2.86-alpha` tag was cut on the merge; this feature is now cleanly re-versioned to **`v0.2.87-alpha`** (the next free version above the `v0.2.85-alpha` retention/demo release) so manifests, runtime `/api/health` + frontend `__APP_VERSION__`, docs, and the annotated `v0.2.87-alpha` tag all align. No published tag (`v0.2.84/85/86-alpha`) was rewritten or deleted.

**Prior (v0.2.83-alpha line — ROUTSTR-TOPUP-QR-1):** The Routstr page's top-up flow is now a **Lightning-invoice QR** payable from a phone. Clicking **Top Up** opens a modal with an editable amount (preset chips 500/1000/5000/21000, default 1000, hard-capped by `cashu.max_mint_sats`, default 100_000) and a **source toggle**: a **Cashu mint quote** (NUT-04) or an **NWC-issued invoice** (NIP-47 `make_invoice`, offered only once a wallet with the `make_invoice` capability is connected). The invoice renders as a scannable QR (`lightning:<BOLT11>`) via a **dependency-free vendored encoder** (`src/views/qr.js`, Nayuki MIT port) plus a copy button; the modal polls for settlement and updates the balance. **How to top up:** open Routstr → Top Up → pick amount + source → scan the QR (or tap Copy) and pay from any Lightning wallet → the modal detects payment and credits the balance. The legacy **paste-a-Cashu-token** path is preserved as a secondary link in the same modal, and the chat-dock `focusTopUp` handoff still opens this modal. Admin-gated routes reuse the `/api/wallet/*` middleware; a full BOLT11 is never logged (first-10 + last-6 only) and preimages are never logged; `checkMintQuote` writes its `minted:true` marker before minting proofs so a re-poll never double-mints. **Not deployed at authoring time** — the enabled deploy timer / live pin unchanged. Branch `feat/routstr-topup-lightning-qr`, annotated tag v0.2.84-alpha. Deviation: vendored QR ~9.4KB minified (3.5KB gzipped), above the ≤6KB target.

**Current version:** v0.2.82-alpha (MEMORY-1 — secure persistent memory: **encrypted-at-rest, consent-gated, owner-scoped, signed portability** — the first adaptive-stage foundation and the RAG-1 prerequisite. **Encrypted-at-rest, browser-sealed** (`agent/lib/memstore.mjs`): durable memory is sealed **NIP-44 v2 browser-side to the owner's own key**; the agent stores **ciphertext only** (never plaintext, never a key). Single writable root `memory/` (systemd `ProtectSystem=strict` + `ReadWritePaths` — fixed the **EROFS** cause where `dirForKind(PROCEDURAL_SKILL)` wrote outside the writable root; now `memory/procedural`, `legacyDirForKind` kept for read-back). Owner-namespaced scoped layout `memory/owners/<ownerHex>/bots/<botId>/projects/<slug>/<class>/<id>.enc`; classes semantic_fact (30094) / procedural_skill (30095) / destructive_intent (30096) / emergency_wipe (30097); atomic temp+rename at 0700/0600; quotas `MAX_ITEM_BYTES=65535`, perScopeItems 500, perScopeBytes 8 MiB, perOwnerBytes 64 MiB; hash-chained tamper-evident `memory/audit.jsonl`. **Consent, no silent persistence** (`agent/lib/consent.mjs`): AI/"remember this" memories are **proposals**, never auto-saved; the owner reviews the exact plaintext and approves by sealing it browser-side, bound to the exact `payload_sha256` + a single-use `approval_nonce`; approve sends ONLY `{payload_sha256, approval_nonce, ciphertext, event_id}` (`'payload' in body === false`); reject never persists. **Working values live** (`agent/lib/workingvalues.mjs`): Constitution + Code of Practice + Reference Canon surfaced as runtime working values with provenance and injected into prompt assembly behind a `fenceUntrusted`/`DATA_FENCE` boundary so retrieved/imported content can never act as instructions. **Signed portability foundation** (`agent/lib/portability.mjs`): owner-signed encrypted export (schema `torii.continuum.memory_bundle/1`, `BUNDLE_FORMAT_VERSION 1`, `MAX_BUNDLE_ITEMS 2000`); a detached bundle-manifest signature uses kind `30099` (`BUNDLE_SIG_KIND`, `manifest_digest` signed via `window.nostr.signEvent`) — **never published to a relay**; import is **quarantined (default-deny)**, owner approves/rejects each. Deletion honest: unlink + tombstone + audit; console states it cannot recall an already-downloaded bundle. **Kind 30092 stays character-root identity/policy/provenance ONLY — never memory contents.** **MEMORY-1 does exact-scope fetch only; similarity/vector retrieval remains RAG-1 (no vector DB — minimal deps).** API: 13 owner-from-session routes (working-values/usage/scoped/verify/delete/proposals list+propose+approve+reject/export/import/quarantine list+approve+reject), no IDOR, admin-gated where mutating, offline/401/403 handled. UI: owner console `#/memory` (`src/views/memory.js`, wired into `src/main.js` route + `src/shell.js` nav), XSS-safe `h()`/textContent only, sealing/signing browser-side, honest wording. Security: no owner private keys on host ever; consent bound to hash+single-use nonce (no replay/no silent persist); import default-deny quarantine; scope isolation owner+bot+project (traversal-safe hex); tamper-evident audit chain. Tests: agent `node --test` **286 pass** (+21 `memory1.test.js`); frontend `vitest` **401 pass** (+ `src/data/agent-memory.test.js` request-shape incl. approve-sends-no-plaintext + URL-encoded segments + offline block; `src/views/memory-structure.test.js` source-structure incl. browser-side sealing, hash+nonce, `BUNDLE_SIG_KIND=30099`, quarantine-on-import, honest relay wording); `npm run build` clean. Version -> 0.2.82-alpha (root+agent package.json + lockfiles). Annotated tag v0.2.82-alpha; **NOT deployed** — enabled deploy timer + live production pin unchanged. Docs: `docs/sovereign-ai-memory-1-spec.md` (new) + strategy/todo/progress/handoff + README + genesis-lora-rag-spec + Code of Practice §3 acceptance refs + shared workspace copies. This is the first adaptive-stage foundation and the RAG-1 prerequisite.) Prior: v0.2.81-alpha (SOVEREIGN-COP-1 — source-grounded **Sovereign AI Code of Practice** + constitution evolution to **`genesis-1.1.0`**, codifying the founding intent as a **three-layer principle architecture** *before* any adaptive LoRA/RAG slice. **Layer A** (`agent/lib/constitution.mjs`) — minimal machine-readable invariants, now a **mint-new-never-edit version registry**: `genesis-1.0.0` preserved byte-identical (digest still locked `178ad323601455f92a345b286eef6c9628f2e71ff7f3f8ad856c16a37e775524`); `genesis-1.1.0` (current) adds three sovereignty invariants — `selective-revelation` (privacy by selective disclosure), `verify-dont-trust`, `four-freedoms-forkable` — locked at `4761094b97937fa496b6e5280da8ff30a332d221429abf9195c35d99694a220e`. New `CONSTITUTION_BODIES` registry + `listConstitutionVersions()`/`getConstitutionByVersion()`/`getConstitutionLayers()`; `verifyConstitutionDigest(pinnedDigest, pinnedVersion)` verifies a manifest against **that version's** frozen body (unknown version → fail-closed; right-digest-wrong-version → fail-closed; legacy no-version → compare current). Economic/philosophical preferences are deliberately kept OUT of the hashed body (they live in Layer B/C). **Layer B** `docs/sovereign-ai-code-of-practice.md` (`cop-1.0.0`) — operational rules mapping each principle → engineering/agent behaviour + acceptance tests, a **normative hierarchy** (law/safety + hard refusal-of-clear-harm > owner authority > consent/privacy > humanitarian care > operational preferences > advisory references), and the **RAG-1 / LoRA gates**. **Layer C** `docs/sovereign-ai-reference-canon.md` (`canon-1.0.0`) — non-binding, exactly-attributed influences (9 entries + primary-source URLs + cautions), contested doctrine flagged as *attributed viewpoint*, no source elevated to unquestionable authority. `agent/core/genesis.mjs read()` now surfaces `constitution_is_current`/`constitution_current_version` so a bot born under an earlier-but-valid version reads as honest provenance, NOT tampering (covenants pinned, never rewritten). `GET /api/constitution` returns `layers` + `versions`. `src/views/genesis.js` renders a `layersCard` (Layer A/B/C rows + ordered normative hierarchy + doc names) + the genesis-1.1.0 invariants + an earlier-valid-covenant note — all `textContent`, doc paths as plain text, **no anchor/href/window.open** (no external-nav/XSS surface). Security: no owner private keys on host; text-only provenance; owner authority from session not body; historical digests fail-closed; fully backward compatible. Tests: agent `node --test` + frontend `vitest` extended (constitution current/historical/registry/verify/fail-closed/layers; genesis-structure layers + Code-of-Practice/Reference-Canon names + normative_hierarchy + no-href/anchor + invariants + earlier-valid-covenant); `npm run build` clean. Version -> 0.2.81-alpha (root+agent package.json + lockfiles). Annotated tag v0.2.81-alpha; **NOT deployed**. This is the prerequisite that unblocks the specified-but-unbuilt CHARACTER-*/RAG-*/LORA-* stages.) Prior: v0.2.78-alpha (GENESIS-1 — first secure vertical slice of the sovereign-bot stack. A deterministic, versioned **humanitarian starter constitution** (`agent/lib/constitution.mjs`, schema `torii.continuum.constitution/1`, version `genesis-1.0.0`, locked digest `178ad323601455f92a345b286eef6c9628f2e71ff7f3f8ad856c16a37e775524`) with four humanitarian articles + five genesis clauses + an **honest amendability** object (guarantees versioning/published-digest/default-deny/tamper-evidence; explicitly does NOT guarantee immutability/tamper-proofing/remote-enforcement). A **hash-chained append-only audit ledger** (`agent/lib/audit.mjs`) turns `memory/audit.jsonl` into tamper-EVIDENT JSONL (each line pins the prev hash; `verify()` finds the break point; appends serialized via a promise queue). A **genesis manifest** (`agent/core/genesis.mjs`) is the bot's birth certificate: owner-bound from the **verified session npub** (NEVER the request body — no IDOR), one-time + idempotent (a retry never forks a second identity), default-deny cross-owner (storage namespaced by 64-hex owner pubkey under `memory/genesis/<ownerHex>/manifest.json`), atomic temp+rename write at 0600 under a 0700 dir, no key material (public npub/hex only). Routes in `agent/index.mjs`: public `GET /api/constitution`; admin-gated + rate-limited `GET/POST /api/genesis` (owner from `req.session.npub`, sends only display_name/archetype/creative_intent, 201 create / 200 idempotent / 400 validation). Client `src/data/agent.js` (`constitution()`, `genesisRead()`, `genesisCreate()` — pubkey-free). UI: guarded `/genesis` route + sidebar nav (`src/views/genesis.js`) — creation form + full constitution preview when absent, provenance card with tamper-evidence badges (`constitution_ok`/`manifest_digest_ok`) when present; **LoRA + RAG honestly labelled `not-started` / subsequent stages, never faked**; XSS-safe `h()` (textContent only). Spec: `docs/sovereign-ai-genesis-lora-rag-spec.md` (+ shared copy `torii-continuum-sovereign-ai-genesis-spec.md`). Tests: agent +37 (`constitution`/`genesis`/`audit` .test.js) → **255/255**; frontend +9 (agent client genesis + `genesis-structure`) → **1278/1278**; `npm run build` clean (33 modules). Version -> 0.2.78-alpha (root+agent package.json + lockfiles). Frontend + agent; **NOT deployed**.) Prior: v0.2.75-alpha (AUTH-SIGNOUT-HARDEN-1 — sign-out is now a hard security boundary: authenticated history is replaced (not pushed) on sign-out, `popstate` + bfcache `pageshow(persisted)` revalidate auth via `restoreTarget`/`enforceRouteAuth`, and the SPA entry + API responses are `Cache-Control: no-store` so Back/bfcache can never restore a signed-out dashboard. Frontend + ops; **NOT deployed**.) Prior: v0.2.69-alpha AUTH-DIRECT-1 + VERSION-UPDATE-1 — direct NIP-07 login + version/self-update UX. Frontend + agent + ops; **NOT deployed**.) **Auth:** clicking Login on the unauthenticated homescreen invokes the browser NIP-07 signer directly (`window.nostr.signEvent`) — no intermediate modal. A verified signature dispatches `continuum:session-changed` and the router lands on the dashboard; cancel / missing-signer / denial / bounded 90s timeout (`SIGNER_TIMEOUT_MS`) / bad-signature all keep the operator on the login card with a concise inline message (`.login-inline-status`, `role=status`/`aria-live=polite`). A module-level `loginInFlight` guard blocks double-invoke/races across surfaces; the signer-missing case keeps Chrome/Firefox install links. `buildLoginEvent` (kind 22242 + challenge/relay tags) and `withTimeout` are pure exports for jsdom-free tests (`src/auth.js`, `src/auth.test.js`). **Version/update:** pre-login the card shows current + latest **non-interactively** via `src/data/release.js` `describeVersionState` (`Up to date` / `Latest vX` badge — never an unauthenticated update action); post-login `src/shell.js` `refreshSidebarVersion` reveals a two-step arm→confirm **Update** button only when a newer same-channel release exists, queues via `POST /api/update`, then `pollForUpgrade` polls `/api/version` and reloads on convergence. **Secure mechanism (privilege-separated):** the unprivileged agent writes a vetted `memory/update-request.json` (admin-gated, `confirm:true` required); root `ops/apply-update-request.sh` (fail-safe `ExecStartPre` on the deploy service, installed by `ops/deploy-bootstrap.sh`) **independently** re-validates tag grammar + allowlist and rewrites the deploy pin's `CONTINUUM_TARGET_TAG`; the existing deploy service converges — the agent never execs or writes root files, and only ever offers the server-known `latest`. `GET /api/version` is public + rate-limited; the latest check (`agent/core/release-check.mjs`) is cached/rate-limited, GitHub/network-failure tolerant (never blocks login), SSRF-pinned to api.github.com, prerelease-aware (`agent/core/semver.mjs`, semver.org §11.4) and same-channel (alpha→alpha) filtered. Agent routes + authz live in `agent/index.mjs`; spool lifecycle in `agent/core/updater.mjs`. Tests: agent semver/release-check/updater/update-routes suites; ops `ops/test/apply-update-request.test.sh` **49/49**; frontend `src/data/release.test.js`, `src/auth.test.js`, `src/version-update-ui.test.js`; full vitest + agent `node --test` + ops bash + `npm run build` green (counts in progress log). Visual QA desktop/mobile **not runnable in-repo** (no headless browser). Version -> 0.2.69-alpha (root+agent package.json + lockfiles); torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue #79 / PR / annotated tag v0.2.69-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.68: UI-COMPOSER-1 — floating chat/composer, Perplexity-style. Frontend-only. The bottom chat dock (`.chat-dock`) is no longer an edge-to-edge grid row: it becomes a `position:fixed`, centered, rounded (16px) floating panel with a restrained layered shadow + `backdrop-filter: blur()` over Continuum's amber/bronze tokens (`hsl(var(--sidebar)/.92)`, `--primary`, `--border`), `max-width:820px` on desktop, and `bottom: calc(16px + env(safe-area-inset-bottom))`. Content is never obscured: the composer's live height (`dockEl.offsetHeight` + a 16px gap) is published as the `--chat-reserve` CSS var and applied to `.main` padding-bottom, kept in sync via a jsdom-guarded `ResizeObserver` on the dock plus `resize`/auto-grow/expand/send hooks; a static `--chat-reserve:104px` fallback in theme.css prevents first-paint jump. The textarea auto-grows between a single-line min (40px) and a max (160px, in sync with `CHAT_INPUT_MAX`) then scrolls internally. Preserved exactly: Enter sends + `preventDefault`, Shift+Enter newline, `send()`/`compose()` prefill, agent/mock reply + thinking state, context chip / page-general scope / send / toggle controls, disabled/loading/error paths, and accessibility (dock gains `role="region"` + `aria-label="Continuum chat"`; existing `role="log" aria-live="polite"` + labelled input kept). Responsive: under 900px the dock is full-width-with-12px-margins (`max-width:none`) with its own safe-area inset; `prefers-reduced-motion` disables the transition. `landing-mode` still hides the dock and zeroes `.main` padding, so login/about views are unaffected. Geometry extracted into DOM-free `src/chat-layout.js` (`clampInputHeight`/`inputShouldScroll`/`reserveSpaceFor`) so tests run under the repo's jsdom-free convention. Files: **created** `src/chat-layout.js`, `src/chat-composer.test.js`; **edited** `src/chat.js`, `src/styles/chat.css`, `src/styles/layout.css`, `src/styles/theme.css`. Tests: new `src/chat-composer.test.js` **32/32** (auto-grow clamp/scroll, reserved-space geometry, keyboard, reserve wiring incl. guarded ResizeObserver, controls + a11y preserved, chat.css floating/rounded/blur/shadow/palette/centered/safe-area/max-height, mobile media query, layout.css single grid area + reserve, theme.css default); full vitest **1148/1148** (+32), agent `node --test` **180/180**, `npm run build` clean; ops suites unchanged (retention 60/60, deploy-unattended 99/99, cutover 106/106). Visual QA: no headless-browser tooling in the sandbox — desktop/mobile visual QA not runnable in-repo; correctness rests on the build + geometry/structure tests. Version -> 0.2.68-alpha (root+agent package.json + lockfiles); torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.68-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.67: OPS-RETENTION-1 — automatic disk-retention sweep. New `ops/torii-disk-retention.sh` reclaims the regenerable artefacts the deploy/cutover pipeline leaves behind — the unattended wrapper's source clones under `/opt/deploy/torii-continuum-<tag>/` and cutover staging trees under `/root/torii-final-cutover-<UTC>/` (each a full clone + `node_modules` + backup + logs; a **failed** cutover leaves its whole tree behind and nothing reaped it) — that on a small VPS accumulate until `npm ci` dies with `ENOSPC`. Fail-closed + idempotent policy: (1) runs only **after a verified-good deploy** — `ret_resolve_live_tag` dies unless the live agent serves exactly the tag pinned in the deploy conf (missing pin / unreachable agent / version mismatch = deploy service state unsafe); (2) source clones — keep the live release's clone, delete every other; (3) cutover staging — keep the single **newest verified** run (has `cutover-summary.txt`, the rollback set) **plus every run newer than it** (an un-superseded failed run's diagnostics + rollback material are retained until a LATER verified run supersedes them), delete every older run; **no verified run → delete nothing**; (4) deploy logs under `/var/log/torii-continuum` capped/rotated (truncate oversized in place preserving inode; keep newest N) while journal/audit are untouched. Safety: deletion only strictly inside the approved roots (`/opt/deploy`, the `/root` staging namespace, `/var/log/torii-continuum`); every candidate is canonicalised + re-checked via `ret_safe_target` (not a symlink, parent is exactly the approved root, not on/under any protected path, not root/`/`) → fails **skip+warn** per path; protected denylist covers config, encrypted state (memory/wallet/pending/ciphertexts), `/srv/continuum-projects`, certificates, `/home/continuum/app`, `/opt/torii-suite/work`, Ollama models, CUDA, journal/audit; NUL-safe `find -print0` (never glob/eval), quoted `rm`, `flock` serialised, conf parsed **line-wise** (never sourced); no secret read/written/logged. Wired into `ops/deploy-unattended.sh` as an **isolated post-deploy process** whose failure is logged+ignored (cleanup must never fail a completed deploy). `ret_report` prints reclaimed bytes + per-mount FS% with an **80% warning**; the cutover `preflight_free_space` gains a matching 80% warning (`PREFLIGHT_WARN_PCT=80`) while keeping the `PREFLIGHT_MIN_FREE_MB=2048` die-gate. Tests: new `ops/test/torii-disk-retention.test.sh` **60/60** (malicious dir-name/symlink/path-traversal, depth>1, protected candidate, the failed-deploy retention rule + no-verified fail-safe, idempotency, source-clone keep-live + symlink-not-followed, log rotation refuse-non-approved, fail-closed pin/health/service-state, conf-never-executed, static guards); `deploy-unattended.test.sh` **99/99** (sweep-wired + failure-non-fatal asserts); cutover **106/106**, vitest **1116/1116**, agent `node --test` **178/178**, `npm run build` clean. Version -> 0.2.67-alpha (root+agent package.json + lockfiles); cutover pins its own tag v0.2.67-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.67-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.65: OPS-CUTOVER-6 — final VPS cutover close-out + robust onboarding CTA detection. **v0.2.63-alpha is LIVE on `chiefmonkey.art`**: the one-time final cutover completed and live acceptance passed — root launcher **HTTP 200**; `nginx`, torii-base **sidecar**, and **continuum-agent** services **active**; Kanban acceptance with the v0.2.63 read-only todo overlay: **Torii Quest** 6 cards (3 Todo / 0 Doing / 3 Done), **Continuum** 7 cards (6 Todo / 0 Doing / 1 Done); onboarding preview passed; the unattended **`torii-continuum-deploy.timer` is enabled and active** (enabled only after the successful converge, OPS-CUTOVER-3); OPS-CUTOVER-5 **free-space preflight** + rollback protections verified live. The one-time cutover, live-acceptance, and deployment-automation tasks are DONE. The single post-deploy false-failure — the onboarding **CTA probe** rejecting a valid live page because it required the exact substring `Sign in with browser extension` while the live button label is wrapped in an icon `<svg>`/`<span>` and wording can drift — is fixed in `ops/torii-final-cutover.sh`: detection is now **intent-based and markup-tolerant** (`normalize_cta_text` strips tags->spaces/collapses/lowercases; `text_has_cta` matches `PREVIEW_CTA_REGEX` `sign[ -]?in (with|using)( [a-z0-9]+)* extension`; probes `url_has_cta`/`wait_url_has_cta` and the pre-mutation asset check all use it). Reworded/split-markup CTAs pass; blank/error pages fail closed; canonical `PREVIEW_CTA` kept only as a log label. Tests (+15) `source` the actual matcher + regex and prove it. Validation: cutover 103/103, deploy-unattended 97/97, vitest 1116/1116, agent `node --test` 178/178, `npm run build` clean. Version -> 0.2.65-alpha (root+agent package.json + lockfiles); cutover pins its own tag v0.2.65-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.65-alpha; **not re-deployed**, now-enabled timer **not** altered. Prior v0.2.64: gate index on stored session, never auto-login on boot (frontend-only, PR #71, root package.json 0.2.64-alpha, untagged). Prior v0.2.63: v0.2.63-alpha (KANBAN-TODO-OVERLAY + OPS-CUTOVER-5 — fix for a live v0.2.62-alpha acceptance blocker where both `/projects/{slug}/board` routes rendered Todo/Doing/Done with **0 cards** despite nonzero project todos (Continuum 6 open, Torii Quest 3 open). Root cause: project **todos** (kind 30081, edited on the Overview tab) and board **cards** (kind 30084) are distinct entities never bridged — the board read only `cardsFor` (empty until a card is manually added) plus the agent-import overlay (empty when logged out). Fix: a **read-only overlay** in `src/views/board.js` maps each project todo to a board card (open->Todo-conventional column, done->Done-conventional via the shared `columnForStatus`), counted in the header total, per-column counts and empty-check, linking back to the Overview tab; **never written to the store** (mirrors the agent-import overlay), so no migration and works logged-out; deliberately excluded from `boardStatsFor` (dashboard progress stays card-based per KANBAN-PROG-1). New pure primitive `filterTodosForColumn` unit-tested. Two acceptance observations confirmed **EXPECTED, not defects**: `#/dashboard`->`#/` is the unit-tested `guardRedirect`, and the root "sign in with nostr extension" shell renders when there is no Continuum NIP-07 session (a browser extension present != an app session). Ops (OPS-CUTOVER-5): disk-safety hardening — a **preflight free-space gate** (`preflight_free_space`, >=2048 MiB on `/root`,`/home`,`/opt`,`/var/www`, deduped by mountpoint) runs FIRST in `main` and fails closed before any clone/mutation; the rollback backup tar now **excludes regenerable cache/build artifacts** (`node_modules`,`.git`,`.cache`,`.npm`,`dist`,`.vite`) while still capturing `env`/`registry.json`/`root_app.conf` byte-for-byte. Also folds in v0.2.62-alpha OPS-CUTOVER-4 (`prune_old_staging_dirs` after a successful cutover, fixing the live `ENOSPC` at `npm ci`). Tests: vitest 1114/1114 (+11 `board-todos.test.js`), cutover 88/88, deploy-unattended 97/97, agent `node --test` 178/178, `npm run build` clean; root+agent package.json 0.2.63-alpha; cutover pins its own tag v0.2.63-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged; not deployed, timer left disabled. Prior v0.2.61: v0.2.61-alpha (OPS-CUTOVER-3 — ops-only hotfix to the unattended deploy path from a live v0.2.59-alpha run that failed the Continuum converge at the first role task with `continuum_user is undefined`: (1) the six non-secret structural role vars now ship as role defaults and the wrapper passes per-host values via a validated `-e` extra-vars JSON instead of a gitignored `group_vars/all.yml`, `continuum_version` stays required, live config preserved byte-for-byte, vault-free; (2) the cutover installs the deploy timer with `--no-enable-timer` and enables it only after a successful first converge — no retry storm. deploy-unattended 97/97, cutover 65/65; not deployed. Note: v0.2.60-alpha was already taken by LOGIN-CONTRAST, so this ops hotfix ships as v0.2.61-alpha. Prior v0.2.60: LOGIN-CONTRAST — frontend-only; raised the fancy glass login card's muted text (body lede, status line, version footer, link separator) from the dim `--muted-foreground` grey to warm off-whites so all login-card text clears WCAG AA over the translucent glass; no layout/glass/background/button changes; root package.json bumped `0.2.59 → 0.2.60`; no agent runtime code changed; onboarding preview stays **v0.1.21-preview**. Prior v0.2.59: OPS-CUTOVER-2 — ops-only hotfix to `ops/torii-final-cutover.sh` from a live v0.2.58-alpha run: (1) the git checkout + torii-base bootstrap now run under a public `umask 022` and the launcher webroot is force-set to 0755 dirs / 0644 files after install, fixing the nginx **HTTP 403** caused by 0600/0700 modes propagated via `cp -a` under the root-only `umask 077`; scoped to `/opt/torii/launcher` only — `/opt/torii/env`, `registry.json`, `root_app.conf` untouched; (2) rollback moved to a single recursion-guarded **`EXIT`-trap** chokepoint so explicit `die`/`exit` after mutation roll back — the old `die(){ exit 1; }` bypassed the ERR trap and exited with no rollback. Root + agent package.json bumped 0.2.58→0.2.59-alpha; no app/agent runtime code changed; onboarding preview stays **v0.1.21-preview**. Prior v0.2.58: v0.2.58-alpha (FANCY-LOGIN — frontend-only; unauthenticated login redesigned into a full-viewport blurred Vermilion Dawn torii backdrop under a centered glass modal (new `src/assets/torii-login-bg.webp`); auth gate unchanged (login shows only when no live session); NIP-07 sign-in + demo/About links preserved; reduced-motion respected; reuses `--font-display` (no external fonts); root package.json bumped `0.2.57 → 0.2.58`; no agent runtime code changed; onboarding preview v0.1.21-preview — unchanged. Merged with OPS-CUTOVER-1 — in-repo root-owned cutover operator script `ops/torii-final-cutover.sh` fetched from one immutable annotated tag; **no app/agent runtime code changed**.)

**Current version:** v0.2.83-alpha (MEMORY-1 privacy correction — **ciphertext-only from proposal creation onward**, superseding v0.2.82-alpha. Root cause fixed: v0.2.82-alpha's `consent.propose()` persisted a **plaintext** `payload` (+ `evidence`) in `memory/owners/<ownerHex>/bots/<botId>/pending/<id>.json`, contradicting the encrypted-at-rest guarantee. Now (`agent/lib/consent.mjs`, schema `torii.continuum.memory_proposal/2`): the browser **seals the proposed payload NIP-44 v2 to the owner's own key BEFORE it reaches the agent** (`sealProposalPayload()` in `src/views/memory.js` → `window.nostr.nip44.encrypt` + canonical `payload_sha256`); `propose()` **refuses any plaintext** (`code:'plaintext_refused'` if `'payload' in params`), requires a sealed `ciphertext` (`isSealedString`, ≤131072 B) + a 64-hex `payload_sha256`, and stores **only** `{ciphertext, ciphertext_sha256, payload_sha256, scope, d_tag (slug), source (enum), approval_nonce, status}` — no `payload`/`evidence`/title/preview field exists. Owner review **decrypts the stored ciphertext client-side** (`window.nostr.nip44.decrypt`), recomputes `canonicalSha256Hex` and only enables approve when `recomputed === p.payload_sha256`. `approve({expectPayloadSha256, approvalNonce})` takes **no ciphertext param** — it binds to the exact reviewed hash (`hash_mismatch`) + single-use nonce (`bad_nonce`), then **atomically promotes the already-sealed ciphertext** into the durable scoped store via `memstore.put({ciphertext, expectSha256: ciphertext_sha256})` and drops the pending blob; the server never sees plaintext or a key. `reject()` securely unlinks the pending ciphertext (metadata-only audit). `migratePlaintextProposals()` runs once at startup (`agent/index.mjs`), detects any retired `memory_proposal/1` file or stray `payload`/`evidence` field, unlinks it **without reading or logging its content**, and appends a metadata-only `memory.migrate.purge_plaintext_proposal` audit line; legacy proposals are never surfaced through any API. **Safe-metadata surface documented** (spec §3.1): the only owner-supplied free-form value is the payload, which is always sealed; `d_tag`/`project` are slugs (`^[a-z0-9][a-z0-9_-]{0,63}$`), class/kind/source are enums, hashes are opaque 64-hex — metadata cannot smuggle memory text. Import/quarantine remain ciphertext-only (unchanged). Tests: agent `node --test` **291 pass** (memory1.test.js adds propose-refuses-plaintext, stores-only-ciphertext on-disk assertion, approve-binds-hash+nonce+promotes, reject-unlinks, corrupt-ciphertext-fails, survives-restart, migration-purges-legacy, and a **recursive plaintext-marker scan** over the pending/quarantine/audit/index/enc trees asserting a known plaintext marker never appears on disk); frontend `vitest` **403 pass** (`agent-memory.test.js` approve sends hash+nonce+event_id only — no payload/ciphertext/key; `memory-structure.test.js` locks browser sealing/decrypt/hashVerified/approval-re-sends-nothing); `npm run build` clean. Version → 0.2.83-alpha (root+agent package.json + lockfiles). Annotated tag v0.2.83-alpha; **NOT deployed** — enabled deploy timer + live production pin unchanged. Docs corrected everywhere (spec §1/§2.2/§3 + new §3.1, Code of Practice §3, strategy/todo/progress/handoff/README) to state ciphertext-only from proposal creation onward.) Prior: v0.2.82-alpha (MEMORY-1 — secure persistent memory: **encrypted-at-rest, consent-gated, owner-scoped, signed portability** — **SUPERSEDED by v0.2.83-alpha: its proposal path persisted plaintext at rest; see the current entry.** — the first adaptive-stage foundation and the RAG-1 prerequisite. **Encrypted-at-rest, browser-sealed** (`agent/lib/memstore.mjs`): durable memory is sealed **NIP-44 v2 browser-side to the owner's own key**; the agent stores **ciphertext only** (never plaintext, never a key). Single writable root `memory/` (systemd `ProtectSystem=strict` + `ReadWritePaths` — fixed the **EROFS** cause where `dirForKind(PROCEDURAL_SKILL)` wrote outside the writable root; now `memory/procedural`, `legacyDirForKind` kept for read-back). Owner-namespaced scoped layout `memory/owners/<ownerHex>/bots/<botId>/projects/<slug>/<class>/<id>.enc`; classes semantic_fact (30094) / procedural_skill (30095) / destructive_intent (30096) / emergency_wipe (30097); atomic temp+rename at 0700/0600; quotas `MAX_ITEM_BYTES=65535`, perScopeItems 500, perScopeBytes 8 MiB, perOwnerBytes 64 MiB; hash-chained tamper-evident `memory/audit.jsonl`. **Consent, no silent persistence** (`agent/lib/consent.mjs`): AI/"remember this" memories are **proposals**, never auto-saved; the owner reviews the exact plaintext and approves by sealing it browser-side, bound to the exact `payload_sha256` + a single-use `approval_nonce`; approve sends ONLY `{payload_sha256, approval_nonce, ciphertext, event_id}` (`'payload' in body === false`); reject never persists. **Working values live** (`agent/lib/workingvalues.mjs`): Constitution + Code of Practice + Reference Canon surfaced as runtime working values with provenance and injected into prompt assembly behind a `fenceUntrusted`/`DATA_FENCE` boundary so retrieved/imported content can never act as instructions. **Signed portability foundation** (`agent/lib/portability.mjs`): owner-signed encrypted export (schema `torii.continuum.memory_bundle/1`, `BUNDLE_FORMAT_VERSION 1`, `MAX_BUNDLE_ITEMS 2000`); a detached bundle-manifest signature uses kind `30099` (`BUNDLE_SIG_KIND`, `manifest_digest` signed via `window.nostr.signEvent`) — **never published to a relay**; import is **quarantined (default-deny)**, owner approves/rejects each. Deletion honest: unlink + tombstone + audit; console states it cannot recall an already-downloaded bundle. **Kind 30092 stays character-root identity/policy/provenance ONLY — never memory contents.** **MEMORY-1 does exact-scope fetch only; similarity/vector retrieval remains RAG-1 (no vector DB — minimal deps).** API: 13 owner-from-session routes (working-values/usage/scoped/verify/delete/proposals list+propose+approve+reject/export/import/quarantine list+approve+reject), no IDOR, admin-gated where mutating, offline/401/403 handled. UI: owner console `#/memory` (`src/views/memory.js`, wired into `src/main.js` route + `src/shell.js` nav), XSS-safe `h()`/textContent only, sealing/signing browser-side, honest wording. Security: no owner private keys on host ever; consent bound to hash+single-use nonce (no replay/no silent persist); import default-deny quarantine; scope isolation owner+bot+project (traversal-safe hex); tamper-evident audit chain. Tests: agent `node --test` **286 pass** (+21 `memory1.test.js`); frontend `vitest` **401 pass** (+ `src/data/agent-memory.test.js` request-shape incl. approve-sends-no-plaintext + URL-encoded segments + offline block; `src/views/memory-structure.test.js` source-structure incl. browser-side sealing, hash+nonce, `BUNDLE_SIG_KIND=30099`, quarantine-on-import, honest relay wording); `npm run build` clean. Version -> 0.2.82-alpha (root+agent package.json + lockfiles). Annotated tag v0.2.82-alpha; **NOT deployed** — enabled deploy timer + live production pin unchanged. Docs: `docs/sovereign-ai-memory-1-spec.md` (new) + strategy/todo/progress/handoff + README + genesis-lora-rag-spec + Code of Practice §3 acceptance refs + shared workspace copies. This is the first adaptive-stage foundation and the RAG-1 prerequisite.) Prior: v0.2.81-alpha (SOVEREIGN-COP-1 — source-grounded **Sovereign AI Code of Practice** + constitution evolution to **`genesis-1.1.0`**, codifying the founding intent as a **three-layer principle architecture** *before* any adaptive LoRA/RAG slice. **Layer A** (`agent/lib/constitution.mjs`) — minimal machine-readable invariants, now a **mint-new-never-edit version registry**: `genesis-1.0.0` preserved byte-identical (digest still locked `178ad323601455f92a345b286eef6c9628f2e71ff7f3f8ad856c16a37e775524`); `genesis-1.1.0` (current) adds three sovereignty invariants — `selective-revelation` (privacy by selective disclosure), `verify-dont-trust`, `four-freedoms-forkable` — locked at `4761094b97937fa496b6e5280da8ff30a332d221429abf9195c35d99694a220e`. New `CONSTITUTION_BODIES` registry + `listConstitutionVersions()`/`getConstitutionByVersion()`/`getConstitutionLayers()`; `verifyConstitutionDigest(pinnedDigest, pinnedVersion)` verifies a manifest against **that version's** frozen body (unknown version → fail-closed; right-digest-wrong-version → fail-closed; legacy no-version → compare current). Economic/philosophical preferences are deliberately kept OUT of the hashed body (they live in Layer B/C). **Layer B** `docs/sovereign-ai-code-of-practice.md` (`cop-1.0.0`) — operational rules mapping each principle → engineering/agent behaviour + acceptance tests, a **normative hierarchy** (law/safety + hard refusal-of-clear-harm > owner authority > consent/privacy > humanitarian care > operational preferences > advisory references), and the **RAG-1 / LoRA gates**. **Layer C** `docs/sovereign-ai-reference-canon.md` (`canon-1.0.0`) — non-binding, exactly-attributed influences (9 entries + primary-source URLs + cautions), contested doctrine flagged as *attributed viewpoint*, no source elevated to unquestionable authority. `agent/core/genesis.mjs read()` now surfaces `constitution_is_current`/`constitution_current_version` so a bot born under an earlier-but-valid version reads as honest provenance, NOT tampering (covenants pinned, never rewritten). `GET /api/constitution` returns `layers` + `versions`. `src/views/genesis.js` renders a `layersCard` (Layer A/B/C rows + ordered normative hierarchy + doc names) + the genesis-1.1.0 invariants + an earlier-valid-covenant note — all `textContent`, doc paths as plain text, **no anchor/href/window.open** (no external-nav/XSS surface). Security: no owner private keys on host; text-only provenance; owner authority from session not body; historical digests fail-closed; fully backward compatible. Tests: agent `node --test` + frontend `vitest` extended (constitution current/historical/registry/verify/fail-closed/layers; genesis-structure layers + Code-of-Practice/Reference-Canon names + normative_hierarchy + no-href/anchor + invariants + earlier-valid-covenant); `npm run build` clean. Version -> 0.2.81-alpha (root+agent package.json + lockfiles). Annotated tag v0.2.81-alpha; **NOT deployed**. This is the prerequisite that unblocks the specified-but-unbuilt CHARACTER-*/RAG-*/LORA-* stages.) Prior: v0.2.78-alpha (GENESIS-1 — first secure vertical slice of the sovereign-bot stack. A deterministic, versioned **humanitarian starter constitution** (`agent/lib/constitution.mjs`, schema `torii.continuum.constitution/1`, version `genesis-1.0.0`, locked digest `178ad323601455f92a345b286eef6c9628f2e71ff7f3f8ad856c16a37e775524`) with four humanitarian articles + five genesis clauses + an **honest amendability** object (guarantees versioning/published-digest/default-deny/tamper-evidence; explicitly does NOT guarantee immutability/tamper-proofing/remote-enforcement). A **hash-chained append-only audit ledger** (`agent/lib/audit.mjs`) turns `memory/audit.jsonl` into tamper-EVIDENT JSONL (each line pins the prev hash; `verify()` finds the break point; appends serialized via a promise queue). A **genesis manifest** (`agent/core/genesis.mjs`) is the bot's birth certificate: owner-bound from the **verified session npub** (NEVER the request body — no IDOR), one-time + idempotent (a retry never forks a second identity), default-deny cross-owner (storage namespaced by 64-hex owner pubkey under `memory/genesis/<ownerHex>/manifest.json`), atomic temp+rename write at 0600 under a 0700 dir, no key material (public npub/hex only). Routes in `agent/index.mjs`: public `GET /api/constitution`; admin-gated + rate-limited `GET/POST /api/genesis` (owner from `req.session.npub`, sends only display_name/archetype/creative_intent, 201 create / 200 idempotent / 400 validation). Client `src/data/agent.js` (`constitution()`, `genesisRead()`, `genesisCreate()` — pubkey-free). UI: guarded `/genesis` route + sidebar nav (`src/views/genesis.js`) — creation form + full constitution preview when absent, provenance card with tamper-evidence badges (`constitution_ok`/`manifest_digest_ok`) when present; **LoRA + RAG honestly labelled `not-started` / subsequent stages, never faked**; XSS-safe `h()` (textContent only). Spec: `docs/sovereign-ai-genesis-lora-rag-spec.md` (+ shared copy `torii-continuum-sovereign-ai-genesis-spec.md`). Tests: agent +37 (`constitution`/`genesis`/`audit` .test.js) → **255/255**; frontend +9 (agent client genesis + `genesis-structure`) → **1278/1278**; `npm run build` clean (33 modules). Version -> 0.2.78-alpha (root+agent package.json + lockfiles). Frontend + agent; **NOT deployed**.) Prior: v0.2.75-alpha (AUTH-SIGNOUT-HARDEN-1 — sign-out is now a hard security boundary: authenticated history is replaced (not pushed) on sign-out, `popstate` + bfcache `pageshow(persisted)` revalidate auth via `restoreTarget`/`enforceRouteAuth`, and the SPA entry + API responses are `Cache-Control: no-store` so Back/bfcache can never restore a signed-out dashboard. Frontend + ops; **NOT deployed**.) Prior: v0.2.69-alpha AUTH-DIRECT-1 + VERSION-UPDATE-1 — direct NIP-07 login + version/self-update UX. Frontend + agent + ops; **NOT deployed**.) **Auth:** clicking Login on the unauthenticated homescreen invokes the browser NIP-07 signer directly (`window.nostr.signEvent`) — no intermediate modal. A verified signature dispatches `continuum:session-changed` and the router lands on the dashboard; cancel / missing-signer / denial / bounded 90s timeout (`SIGNER_TIMEOUT_MS`) / bad-signature all keep the operator on the login card with a concise inline message (`.login-inline-status`, `role=status`/`aria-live=polite`). A module-level `loginInFlight` guard blocks double-invoke/races across surfaces; the signer-missing case keeps Chrome/Firefox install links. `buildLoginEvent` (kind 22242 + challenge/relay tags) and `withTimeout` are pure exports for jsdom-free tests (`src/auth.js`, `src/auth.test.js`). **Version/update:** pre-login the card shows current + latest **non-interactively** via `src/data/release.js` `describeVersionState` (`Up to date` / `Latest vX` badge — never an unauthenticated update action); post-login `src/shell.js` `refreshSidebarVersion` reveals a two-step arm→confirm **Update** button only when a newer same-channel release exists, queues via `POST /api/update`, then `pollForUpgrade` polls `/api/version` and reloads on convergence. **Secure mechanism (privilege-separated):** the unprivileged agent writes a vetted `memory/update-request.json` (admin-gated, `confirm:true` required); root `ops/apply-update-request.sh` (fail-safe `ExecStartPre` on the deploy service, installed by `ops/deploy-bootstrap.sh`) **independently** re-validates tag grammar + allowlist and rewrites the deploy pin's `CONTINUUM_TARGET_TAG`; the existing deploy service converges — the agent never execs or writes root files, and only ever offers the server-known `latest`. `GET /api/version` is public + rate-limited; the latest check (`agent/core/release-check.mjs`) is cached/rate-limited, GitHub/network-failure tolerant (never blocks login), SSRF-pinned to api.github.com, prerelease-aware (`agent/core/semver.mjs`, semver.org §11.4) and same-channel (alpha→alpha) filtered. Agent routes + authz live in `agent/index.mjs`; spool lifecycle in `agent/core/updater.mjs`. Tests: agent semver/release-check/updater/update-routes suites; ops `ops/test/apply-update-request.test.sh` **49/49**; frontend `src/data/release.test.js`, `src/auth.test.js`, `src/version-update-ui.test.js`; full vitest + agent `node --test` + ops bash + `npm run build` green (counts in progress log). Visual QA desktop/mobile **not runnable in-repo** (no headless browser). Version -> 0.2.69-alpha (root+agent package.json + lockfiles); torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue #79 / PR / annotated tag v0.2.69-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.68: UI-COMPOSER-1 — floating chat/composer, Perplexity-style. Frontend-only. The bottom chat dock (`.chat-dock`) is no longer an edge-to-edge grid row: it becomes a `position:fixed`, centered, rounded (16px) floating panel with a restrained layered shadow + `backdrop-filter: blur()` over Continuum's amber/bronze tokens (`hsl(var(--sidebar)/.92)`, `--primary`, `--border`), `max-width:820px` on desktop, and `bottom: calc(16px + env(safe-area-inset-bottom))`. Content is never obscured: the composer's live height (`dockEl.offsetHeight` + a 16px gap) is published as the `--chat-reserve` CSS var and applied to `.main` padding-bottom, kept in sync via a jsdom-guarded `ResizeObserver` on the dock plus `resize`/auto-grow/expand/send hooks; a static `--chat-reserve:104px` fallback in theme.css prevents first-paint jump. The textarea auto-grows between a single-line min (40px) and a max (160px, in sync with `CHAT_INPUT_MAX`) then scrolls internally. Preserved exactly: Enter sends + `preventDefault`, Shift+Enter newline, `send()`/`compose()` prefill, agent/mock reply + thinking state, context chip / page-general scope / send / toggle controls, disabled/loading/error paths, and accessibility (dock gains `role="region"` + `aria-label="Continuum chat"`; existing `role="log" aria-live="polite"` + labelled input kept). Responsive: under 900px the dock is full-width-with-12px-margins (`max-width:none`) with its own safe-area inset; `prefers-reduced-motion` disables the transition. `landing-mode` still hides the dock and zeroes `.main` padding, so login/about views are unaffected. Geometry extracted into DOM-free `src/chat-layout.js` (`clampInputHeight`/`inputShouldScroll`/`reserveSpaceFor`) so tests run under the repo's jsdom-free convention. Files: **created** `src/chat-layout.js`, `src/chat-composer.test.js`; **edited** `src/chat.js`, `src/styles/chat.css`, `src/styles/layout.css`, `src/styles/theme.css`. Tests: new `src/chat-composer.test.js` **32/32** (auto-grow clamp/scroll, reserved-space geometry, keyboard, reserve wiring incl. guarded ResizeObserver, controls + a11y preserved, chat.css floating/rounded/blur/shadow/palette/centered/safe-area/max-height, mobile media query, layout.css single grid area + reserve, theme.css default); full vitest **1148/1148** (+32), agent `node --test` **180/180**, `npm run build` clean; ops suites unchanged (retention 60/60, deploy-unattended 99/99, cutover 106/106). Visual QA: no headless-browser tooling in the sandbox — desktop/mobile visual QA not runnable in-repo; correctness rests on the build + geometry/structure tests. Version -> 0.2.68-alpha (root+agent package.json + lockfiles); torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.68-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.67: OPS-RETENTION-1 — automatic disk-retention sweep. New `ops/torii-disk-retention.sh` reclaims the regenerable artefacts the deploy/cutover pipeline leaves behind — the unattended wrapper's source clones under `/opt/deploy/torii-continuum-<tag>/` and cutover staging trees under `/root/torii-final-cutover-<UTC>/` (each a full clone + `node_modules` + backup + logs; a **failed** cutover leaves its whole tree behind and nothing reaped it) — that on a small VPS accumulate until `npm ci` dies with `ENOSPC`. Fail-closed + idempotent policy: (1) runs only **after a verified-good deploy** — `ret_resolve_live_tag` dies unless the live agent serves exactly the tag pinned in the deploy conf (missing pin / unreachable agent / version mismatch = deploy service state unsafe); (2) source clones — keep the live release's clone, delete every other; (3) cutover staging — keep the single **newest verified** run (has `cutover-summary.txt`, the rollback set) **plus every run newer than it** (an un-superseded failed run's diagnostics + rollback material are retained until a LATER verified run supersedes them), delete every older run; **no verified run → delete nothing**; (4) deploy logs under `/var/log/torii-continuum` capped/rotated (truncate oversized in place preserving inode; keep newest N) while journal/audit are untouched. Safety: deletion only strictly inside the approved roots (`/opt/deploy`, the `/root` staging namespace, `/var/log/torii-continuum`); every candidate is canonicalised + re-checked via `ret_safe_target` (not a symlink, parent is exactly the approved root, not on/under any protected path, not root/`/`) → fails **skip+warn** per path; protected denylist covers config, encrypted state (memory/wallet/pending/ciphertexts), `/srv/continuum-projects`, certificates, `/home/continuum/app`, `/opt/torii-suite/work`, Ollama models, CUDA, journal/audit; NUL-safe `find -print0` (never glob/eval), quoted `rm`, `flock` serialised, conf parsed **line-wise** (never sourced); no secret read/written/logged. Wired into `ops/deploy-unattended.sh` as an **isolated post-deploy process** whose failure is logged+ignored (cleanup must never fail a completed deploy). `ret_report` prints reclaimed bytes + per-mount FS% with an **80% warning**; the cutover `preflight_free_space` gains a matching 80% warning (`PREFLIGHT_WARN_PCT=80`) while keeping the `PREFLIGHT_MIN_FREE_MB=2048` die-gate. Tests: new `ops/test/torii-disk-retention.test.sh` **60/60** (malicious dir-name/symlink/path-traversal, depth>1, protected candidate, the failed-deploy retention rule + no-verified fail-safe, idempotency, source-clone keep-live + symlink-not-followed, log rotation refuse-non-approved, fail-closed pin/health/service-state, conf-never-executed, static guards); `deploy-unattended.test.sh` **99/99** (sweep-wired + failure-non-fatal asserts); cutover **106/106**, vitest **1116/1116**, agent `node --test` **178/178**, `npm run build` clean. Version -> 0.2.67-alpha (root+agent package.json + lockfiles); cutover pins its own tag v0.2.67-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.67-alpha; **not deployed** — the enabled `torii-continuum-deploy.timer` and the live production pin are **not** altered.) Prior v0.2.65: OPS-CUTOVER-6 — final VPS cutover close-out + robust onboarding CTA detection. **v0.2.63-alpha is LIVE on `chiefmonkey.art`**: the one-time final cutover completed and live acceptance passed — root launcher **HTTP 200**; `nginx`, torii-base **sidecar**, and **continuum-agent** services **active**; Kanban acceptance with the v0.2.63 read-only todo overlay: **Torii Quest** 6 cards (3 Todo / 0 Doing / 3 Done), **Continuum** 7 cards (6 Todo / 0 Doing / 1 Done); onboarding preview passed; the unattended **`torii-continuum-deploy.timer` is enabled and active** (enabled only after the successful converge, OPS-CUTOVER-3); OPS-CUTOVER-5 **free-space preflight** + rollback protections verified live. The one-time cutover, live-acceptance, and deployment-automation tasks are DONE. The single post-deploy false-failure — the onboarding **CTA probe** rejecting a valid live page because it required the exact substring `Sign in with browser extension` while the live button label is wrapped in an icon `<svg>`/`<span>` and wording can drift — is fixed in `ops/torii-final-cutover.sh`: detection is now **intent-based and markup-tolerant** (`normalize_cta_text` strips tags->spaces/collapses/lowercases; `text_has_cta` matches `PREVIEW_CTA_REGEX` `sign[ -]?in (with|using)( [a-z0-9]+)* extension`; probes `url_has_cta`/`wait_url_has_cta` and the pre-mutation asset check all use it). Reworded/split-markup CTAs pass; blank/error pages fail closed; canonical `PREVIEW_CTA` kept only as a log label. Tests (+15) `source` the actual matcher + regex and prove it. Validation: cutover 103/103, deploy-unattended 97/97, vitest 1116/1116, agent `node --test` 178/178, `npm run build` clean. Version -> 0.2.65-alpha (root+agent package.json + lockfiles); cutover pins its own tag v0.2.65-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged. Issue/PR/annotated tag v0.2.65-alpha; **not re-deployed**, now-enabled timer **not** altered. Prior v0.2.64: gate index on stored session, never auto-login on boot (frontend-only, PR #71, root package.json 0.2.64-alpha, untagged). Prior v0.2.63: v0.2.63-alpha (KANBAN-TODO-OVERLAY + OPS-CUTOVER-5 — fix for a live v0.2.62-alpha acceptance blocker where both `/projects/{slug}/board` routes rendered Todo/Doing/Done with **0 cards** despite nonzero project todos (Continuum 6 open, Torii Quest 3 open). Root cause: project **todos** (kind 30081, edited on the Overview tab) and board **cards** (kind 30084) are distinct entities never bridged — the board read only `cardsFor` (empty until a card is manually added) plus the agent-import overlay (empty when logged out). Fix: a **read-only overlay** in `src/views/board.js` maps each project todo to a board card (open->Todo-conventional column, done->Done-conventional via the shared `columnForStatus`), counted in the header total, per-column counts and empty-check, linking back to the Overview tab; **never written to the store** (mirrors the agent-import overlay), so no migration and works logged-out; deliberately excluded from `boardStatsFor` (dashboard progress stays card-based per KANBAN-PROG-1). New pure primitive `filterTodosForColumn` unit-tested. Two acceptance observations confirmed **EXPECTED, not defects**: `#/dashboard`->`#/` is the unit-tested `guardRedirect`, and the root "sign in with nostr extension" shell renders when there is no Continuum NIP-07 session (a browser extension present != an app session). Ops (OPS-CUTOVER-5): disk-safety hardening — a **preflight free-space gate** (`preflight_free_space`, >=2048 MiB on `/root`,`/home`,`/opt`,`/var/www`, deduped by mountpoint) runs FIRST in `main` and fails closed before any clone/mutation; the rollback backup tar now **excludes regenerable cache/build artifacts** (`node_modules`,`.git`,`.cache`,`.npm`,`dist`,`.vite`) while still capturing `env`/`registry.json`/`root_app.conf` byte-for-byte. Also folds in v0.2.62-alpha OPS-CUTOVER-4 (`prune_old_staging_dirs` after a successful cutover, fixing the live `ENOSPC` at `npm ci`). Tests: vitest 1114/1114 (+11 `board-todos.test.js`), cutover 88/88, deploy-unattended 97/97, agent `node --test` 178/178, `npm run build` clean; root+agent package.json 0.2.63-alpha; cutover pins its own tag v0.2.63-alpha; torii-base v0.1.4 / preview v0.1.21-preview unchanged; not deployed, timer left disabled. Prior v0.2.61: v0.2.61-alpha (OPS-CUTOVER-3 — ops-only hotfix to the unattended deploy path from a live v0.2.59-alpha run that failed the Continuum converge at the first role task with `continuum_user is undefined`: (1) the six non-secret structural role vars now ship as role defaults and the wrapper passes per-host values via a validated `-e` extra-vars JSON instead of a gitignored `group_vars/all.yml`, `continuum_version` stays required, live config preserved byte-for-byte, vault-free; (2) the cutover installs the deploy timer with `--no-enable-timer` and enables it only after a successful first converge — no retry storm. deploy-unattended 97/97, cutover 65/65; not deployed. Note: v0.2.60-alpha was already taken by LOGIN-CONTRAST, so this ops hotfix ships as v0.2.61-alpha. Prior v0.2.60: LOGIN-CONTRAST — frontend-only; raised the fancy glass login card's muted text (body lede, status line, version footer, link separator) from the dim `--muted-foreground` grey to warm off-whites so all login-card text clears WCAG AA over the translucent glass; no layout/glass/background/button changes; root package.json bumped `0.2.59 → 0.2.60`; no agent runtime code changed; onboarding preview stays **v0.1.21-preview**. Prior v0.2.59: OPS-CUTOVER-2 — ops-only hotfix to `ops/torii-final-cutover.sh` from a live v0.2.58-alpha run: (1) the git checkout + torii-base bootstrap now run under a public `umask 022` and the launcher webroot is force-set to 0755 dirs / 0644 files after install, fixing the nginx **HTTP 403** caused by 0600/0700 modes propagated via `cp -a` under the root-only `umask 077`; scoped to `/opt/torii/launcher` only — `/opt/torii/env`, `registry.json`, `root_app.conf` untouched; (2) rollback moved to a single recursion-guarded **`EXIT`-trap** chokepoint so explicit `die`/`exit` after mutation roll back — the old `die(){ exit 1; }` bypassed the ERR trap and exited with no rollback. Root + agent package.json bumped 0.2.58→0.2.59-alpha; no app/agent runtime code changed; onboarding preview stays **v0.1.21-preview**. Prior v0.2.58: v0.2.58-alpha (FANCY-LOGIN — frontend-only; unauthenticated login redesigned into a full-viewport blurred Vermilion Dawn torii backdrop under a centered glass modal (new `src/assets/torii-login-bg.webp`); auth gate unchanged (login shows only when no live session); NIP-07 sign-in + demo/About links preserved; reduced-motion respected; reuses `--font-display` (no external fonts); root package.json bumped `0.2.57 → 0.2.58`; no agent runtime code changed; onboarding preview v0.1.21-preview — unchanged. Merged with OPS-CUTOVER-1 — in-repo root-owned cutover operator script `ops/torii-final-cutover.sh` fetched from one immutable annotated tag; **no app/agent runtime code changed**.)
**Currently deployed server version:** v0.2.29-alpha (deployed to the SHC VPS on a Node 22.23.1 host). **v0.2.30-alpha through v0.2.41-alpha are code+PR only. v0.2.42-alpha was deployed manually to the live box (cutover succeeded, funded state/admin preserved); v0.2.43-alpha through v0.2.57-alpha are code+PR only — not yet deployed.**

Paste this whole block at the start of a new Perplexity Computer session to resume work seamlessly.

**Active focus:** **v0.2.59-alpha ships OPS-CUTOVER-2: a live-run hotfix for the in-repo cutover operator script** (`ops/torii-final-cutover.sh`; ops-only, no app/agent runtime code changed; onboarding preview stays **v0.1.21-preview**; root + agent package.json bumped 0.2.58→0.2.59-alpha). **Why.** A live v0.2.58-alpha run failed in `validate_torii_base` with `FATAL: public launcher probe failed with HTTP 403`: the script's root-only `umask 077` was inherited by the git checkout (0600 files / 0700 dirs), and torii-base's mode-preserving `cp -a` propagated those private modes into `/opt/torii/launcher`, so nginx returned 403. Worse, `die(){ exit 1; }` bypassed the `ERR` trap, so the fatal probe exited with **no rollback**. **Two fixes.** (1) **Public modes:** the checkout in `clone_annotated_tag` and the sanctioned bootstrap both run under `umask 022`; `enforce_public_static_modes` then forces the launcher webroot to 0755 dirs / 0644 files (the exact manual recovery the operator ran). Widening is scoped to `/opt/torii/launcher` only — `/opt/torii/env` (0640, holds `TORII_ADMIN_TOKEN`), `registry.json` and `root_app.conf` sit above it and are never chmodded; the preview stage (mktemp 0700) is normalised through the same helper. (2) **Robust rollback:** a single recursion-guarded `EXIT`-trap (`on_exit`) is the one rollback chokepoint, reached by every exit path incl. explicit `die`/`exit`; a failure before any mutation is a safe no-op. **Recovery / scope:** the failed run stopped in `validate_torii_base`, which precedes the Continuum and preview phases, so **no Continuum pin or onboarding-preview state was mutated** — only torii-base was redeployed. **Validation:** `bash -n` clean; ops cutover suite **59/59**; vitest, agent `node --test`, `npm run build` green. **Not deployed.**

**Active focus:** **v0.2.58-alpha ships OPS-CUTOVER-1: the audited combined cutover as an in-repo, root-owned operator script** (`ops/torii-final-cutover.sh`; **no app/agent runtime code changed**, onboarding preview stays **v0.1.21-preview**; root + agent package.json bumped 0.2.57→0.2.58-alpha). **Why.** A prior delivery pasted the standalone cutover script body straight into an interactive VPS shell and the paste **truncated mid-function** — an interactive shell runs what it received, so a partial paste risks a partial, dangerous run. This release incorporates the same audited cutover into the repo so it is fetched from **one immutable annotated release tag** and invoked with a short command from a verified clone: `sudo bash ops/torii-final-cutover.sh`. **Two structural defenses replace the broken delivery:** (1) the entire executable body lives inside a single `{ … }` **brace group** whose closing brace is the last line, so `bash` parses to the matching brace before running anything and a truncated copy fails `bash -n` and runs NOTHING (proven at four truncation depths); (2) the dangerous `exec sudo -- bash "$0"` re-exec is **gone** — when pasted/sourced `$0` is the interactive shell (`-bash`), so the script instead **requires root** (dies with the documented invocation) and **refuses to be sourced** (`BASH_SOURCE`≠`$0`). **Behaviour preserved from the audited source** (`torii-final-cutover.sh`): exact annotated-tag verification (`git cat-file -t`==`tag`) + version-marker checks for torii-base **v0.1.4** (VERSION / sidecar `package.json` / `const VERSION = '0.1.4';` / launcher 'Continuum amber'), torii-continuum **v0.2.58-alpha** (its own tag), onboarding preview **v0.1.21-preview** (VERSION + exact CTA "Sign in with browser extension"); root-only timestamped backups under `/root/torii-final-cutover-<UTC>/`; torii-base redeploy via its sanctioned bootstrap (`TORII_DOMAIN`+`SKIP_CERTBOT=1`); Continuum OPS-DEPLOY-2 bootstrap+pin+`systemctl start` with a `/api/health` **version gate**; **fail-closed** onboarding-preview layout detection (root-symlink vs continuum-dir; ambiguity/absence aborts) with an atomic stage→`mv -T`→`.prev` swap keeping exactly one rollback; registry `root_app`/app-name preservation checks; ERR-trap rollback; **no secrets printed, no broad sudoers, public HTTPS clones only**. Also fixed the source draft's mixed-case `SIDEcar_HEALTH_URL` typo. New `ops/torii-final-cutover.README.md` documents invocation, rollback locations, and a **preflight** that confirms the prior truncated paste made no mutations. **Tests:** new `ops/test/torii-final-cutover.test.sh` **36/36** (brace-group + truncation-fails-`bash -n`, root/source guards, no `exec sudo`, pinned annotated tags + version markers, fail-closed preview detection, atomic swap + single rollback, health gates, backups, no-secrets/no-broad-sudoers, HTTPS-only). Full verification (Node 20.20.1): all ops shell suites green (`continuum-adopt` 219, `deploy-unattended` 55, `deploy-restart` 25, `installer-preflight` 18, `installer-shared-parent` 10, `installer-signal` 9, `nginx-install` 13, `torii-final-cutover` 36), root `vitest run` **1032/1032**, `npm run build` clean (0.2.52-alpha, `dist/` free of `preview-assets/`). shellcheck UNAVAILABLE in sandbox (`bash -n` clean on every ops script). Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed** (this script itself does **not** deploy on your behalf). Prior slice: **v0.2.51-alpha ships ONBOARD-UI-1: clearer signer-extension login copy + stronger numbered-step wayfinding in the onboarding preview** (onboarding preview → **v0.1.21-preview**, `preview-assets/onboarding-v0.1.21/`; **no app/agent runtime code changed** — root + agent package.json bumped 0.2.50→0.2.51-alpha for release discipline). Closes GitHub issue **#57**. Design-review mockup only (vendored under `preview-assets/`, never bundled by `vite build`). **(1) Login button copy:** the Step-1 primary CTA now reads exactly **"Sign in with browser extension"** (was "Sign with Plebeian Signer"); the lede names the signer context plainly ("a browser extension such as Plebeian Signer, nos2x or Alby") and the ghost button reads "Use a remote signer instead"; the no-signer-found message became "No browser extension signer found — install a NIP-07 extension (e.g. Plebeian Signer, nos2x or Alby)". No tracking, no CDN, no key material added — `onboarding-client.js` routing/Routstr logic is byte-identical to v0.1.20 except that one message string (`CONTINUUM_HOME`, `data-claimed`, `renderSuccessAdvance` and the duplicate-pay guards intact). **(2) Numbered-step wayfinding (`shared.css`):** completed steps are now **filled** (bronze `--bronze` numeral chip, dark numeral, weight 600); the current step gets a **brighter** amber-bright 3px ring + glow + weight 600 (was a 2px `--amber` ring), preserving the 36px chip size. Non-colour cues (size + fill + ring) keep it colour-blind safe; added an explicit `.step-dot:focus-visible` amber-bright outline for keyboard nav; **no new animation** (reduced-motion safe). WCAG audit: completed numeral on bronze **5.78:1** (AA small text), current numeral on amber **10.75:1**, current/focus ring vs bar **14.15:1** (≥3 UI). **(3) Preserved:** claimed-Routstr simplified terminal state + successful routing to app/login/dashboard unchanged (verified against v0.1.20 diff). **(4) Doc fix:** the stale onboarding `README.md` version header (`0.1.19-preview`) corrected to `0.1.21-preview`; top-level `preview-assets/README.md` "Current preview" pointer advanced to v0.1.21 and the deploy example rewritten for the new dir. **Tests:** the v0.1.21 test suite asserts the new button copy (and `not.toContain('Sign with Plebeian Signer')`), the browser-extension/Plebeian-Signer step-1 context, the new signer-not-found message + NIP-07 mention, the step-dot wayfinding states (bronze fill + dark numeral, amber-bright 3px ring + 36px width, focus-visible outline, no animation), and the README version header. Verification (Node 20.x): root `vitest run` **1032/1032** (19 files, +160 from the new version dir), `npm run build` clean (0.2.51-alpha; `dist/` free of `preview-assets/`). Static QA artifact (no headed browser in sandbox): desktop + mobile step strips in all states + programmatic WCAG audit. Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.50-alpha ships OPS-DEPLOY-2: safe unattended deployment via a server-side pull** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#54**. Problem: local-device automation cannot make outbound SSH and interactive VPS SSH prompts for a password, so a push deploy is impossible; the existing role's redeploy still needs a human to run `ansible-playbook` on the box. Design — a small, root-owned wrapper the VPS runs on a systemd timer that **delegates to the already-hardened `continuum` role** (never reimplements state backup, the atomic staging→swap cutover, the version-asserting health gate, rescue rollback, or cleanup). New files (all `ops/`): **`deploy-unattended.sh`** (→ `/usr/local/sbin/torii-continuum-deploy`): strict `^v<semver>(-prerelease)?$` tag grammar rejecting branches/SHAs/shell+YAML metacharacters (proven against injection strings), optional fail-closed allowlist, optional `git tag -v` signed-tag gate (`CONTINUUM_REQUIRE_SIGNED_TAGS=1`), **idempotent no-op** when live `/api/health` already serves the tag, fresh per-tag clone under `/opt/deploy`, renders localhost vault-free inventory (`continuum_version`+`torii_domain` only), runs `ansible-playbook --tags continuum`, **independently re-verifies** the live version on top of the role's gate, prunes old releases (keeps newest N, never the live one); `flock`-guarded, `set -euo pipefail`, refuses non-root, sourceable for in-process tests. **`systemd/torii-continuum-deploy.{service,timer}`** (oneshot + ~5min timer; no inbound SSH/ports). **`sudoers/torii-continuum-deploy.example`** (OPTIONAL remote/CI trigger: locked non-login `toriideploy` principal, `NOPASSWD` on **exactly the wrapper, no args** — never general passwordless sudo). **`deploy-bootstrap.sh`** (idempotent fail-closed one-time installer: wrapper 0755 root:root, units, root-only `0600` pin-file skeleton it never clobbers, optional principal+sudoers validated with `visudo -cf`, optional dedicated ed25519 key + host-key-pinning guidance). **After bootstrap, releasing a version is a single root-only edit of `/etc/torii/continuum-deploy.conf` (`CONTINUUM_TARGET_TAG=`); a bad release fails closed and self-reverts via the role's rollback — no SSH in the deploy loop.** Security: no secret read/written/logged (vault-free path preserves `config.yaml`/`session_secret`/funded key byte-for-byte); no auth weakened; no general sudo; no ports opened. Tests: new `ops/test/deploy-unattended.test.sh` **55/55** (tag grammar incl. injection, version gate, allowlist fail-closed, version extraction, localhost/vault-free inventory, prune-keeps-live, wrapper fail-closed guards, oneshot/timer wiring, scoped-sudo/no-`NOPASSWD: ALL`, bootstrap 0755/0600/visudo/locked-principal/host-key-pin/no-clobber); existing `deploy-restart` **25/25**, root `vitest run` **872/872**, `npm run build` clean (0.2.50-alpha). Both `package.json` bumped 0.2.49→0.2.50-alpha. Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.49-alpha ships CONT-LIVE-UI-1: surface live provider/wallet/kanban data in the UI** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#52**. Live v0.2.48-alpha authenticated acceptance found three UI regressions where correct backend data never reached the operator. **(1) Dashboard Providers panel blank** despite "Polling every 20s": `ProviderCard()` (`src/views/dashboard.js`) fired the first `tickProvider(body)` synchronously *before* the card was appended, so `body.isConnected` was false, the tick bailed (and cleared its own poll), and the body stayed empty until the first 20s interval — fixed with a synchronous "Checking providers…" placeholder + a deferred first tick via `queueMicrotask(() => tickProvider(body))` (card is in the DOM by then); interval unchanged. **(2) Routstr green `connected` pill but em-dash Cashu balance:** field-name mismatch — `GET /api/wallet/balance` returns `{ total_sats, ... }` but the poll read `r.data.balance_sats` → `undefined` → `formatSats(undefined)` → `—`, while still setting `connected:true` and persisting `cashuBalanceSats:undefined` (so the em dash stuck across re-renders); the top-up modal shared the bug (`received_sats`/`balance_sats`; the endpoint returns `added_sats` only). Fixed with a pure exported `readBalanceSats()` (prefers `total_sats`, `balance_sats` fallback, returns `null` — not `0` — when absent), an in-place balance-number update (`balanceNumEl.textContent`, no page tear), and a receive modal that reads `added_sats` then re-reads the authoritative balance. A configured-but-empty wallet now shows an honest `0 sats` (balance sums to 0 across mints) instead of `—`; a non-zero balance requires one or more `cashu.mints` on the agent AND redeemed proofs via top-up (`/api/wallet/receive`). **(3) Kanban header total + per-column counts excluded imported read-only cards:** `totalCards` and each `col-count` summed only native `cardsFor`, never `importedCardsFor`, so a board/column of only imported work read `0`; fixed to add the imported cards, with the distribution primitive extracted as a pure exported `filterImportedForColumn()`. **Security:** fail-closed posture unchanged — every wallet/health/source route stays `requireAdmin`, no proofs/tokens/secrets/full endpoints are rendered, the wallet probe stays a read-only NUT-07/identity check; these are pure client-side field/ordering fixes with no new network or storage surface. **Tests:** new `src/views/routstr.test.js` (8), `src/views/board-counts.test.js` (7), `src/views/dashboard-structure.test.js` (4). Verification (Node 20.x): root `vitest run` **872/872** (18 files, +19), agent `node --test` **168/168** (unchanged), `npm run build` clean (0.2.49-alpha), `npm audit --omit=dev` root prod **0** / agent prod **0**; root full audit **5** dev-only (vite/vitest/esbuild). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.48-alpha ships OPS-DEPLOY-1: restart the promoted agent + verify the deployed version** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#49**. **Root cause it fixes (diagnosed from the live 0.2.43-vs-0.2.47 incident):** the `continuum-agent` systemd unit file is version-independent, so a code-only redeploy atomically promotes a new release tree WITHOUT changing the unit; the role's `restart continuum-agent` handler is never notified and the old `state: started` task is a no-op against the already-running PID. The swap changed the code on disk but the live process kept serving the PREVIOUS release (stale `/api/health` version, new `/api/wallet/health` + `/api/projects/:slug/sources` routes 404). The old health gate accepted any HTTP 200, so the broken deploy passed. **Fix (`ops/ansible/roles/continuum/tasks/main.yml`):** the unit-install task now `register:`s `continuum_unit_install`; after the JIT smoke test the role (1) runs `meta: flush_handlers` so any unit-file-change restart fires BEFORE readiness (handlers run in definition order — `reload systemd` daemon-reload then `restart continuum-agent`), (2) explicitly `state: restarted` **only when `continuum_promote.changed` AND NOT `continuum_unit_install.changed`** (the exact code-only case the handler misses; the guard prevents a double restart when the unit itself changed — that path is the handler's), then (3) an idempotent enabled+`state: started` ensure. Readiness is strengthened: `uri` now uses `return_content: true` and the `until` requires HTTP 200 AND `agent_health.json.version == (continuum_version | regex_replace('^v',''))`, so a stale process left running by a missed restart reports the OLD version and FAILS readiness, tripping the existing block/rescue rollback instead of passing a broken deploy. Exactly one restart happens per real deploy; the health gate always inspects the post-restart process. Everything else is preserved verbatim: block/rescue rollback (quarantine restore + webroot rollback + standalone re-enable), fail-closed backup, config/state verbatim copy (`session_secret`/funded key never rotated), adoption modes, V8 JIT smoke test, standalone-unit disabling, and `no_log` secret hygiene. The unit template stays version-independent (that is WHY the explicit restart is needed) — a regression test locks that assumption. **Tests:** new `ops/test/deploy-restart.test.sh` (**25** hermetic assertions): restart-before-readiness wiring (register, flush_handlers, guarded `state: restarted`, idempotent ensure), the version-asserting health gate, strict ordering (flush < restart < ensure < health) + handler daemon-reload-before-restart ordering, the version-independent-unit lock, rescue/rollback/webroot/standalone/JIT/config-copy survival, `no_log` retention + no secret in any printed `msg`, and pure version-equality cases proving a **stale 0.2.43-alpha live version FAILS** readiness while the **correct 0.2.48-alpha PASSES** (leading `v` stripped, empty version fails). **Full verification (Node 20.20.1 sandbox; agent code unchanged):** ops shell `deploy-restart` **25/25**, `continuum-adopt` **219/219**, `installer-preflight` **18/18**, `installer-shared-parent` **10/10**, `installer-signal` **9/9**, `nginx-install` **13/13**; root `vitest run` **853/853**; agent `node --test` **168/168**; `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz, version stamps 0.2.48-alpha); `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows the same **5 dev-only** findings (`@vitest/mocker`/`esbuild`/`vite`/`vite-node`/`vitest` toolchain, not shipped in `dist/`; breaking major-vite fix deferred). Lockfile top-level `version` fields left as-is (prior releases never bumped them; `npm ci` validates the dependency tree, not that field — build re-ran clean). No hostnames/devices/secrets in code, tests, or docs. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.47-alpha ships KANBAN-SYNC + wallet health** (agent + frontend; onboarding preview stays **v0.1.20-preview**; both package.json + lockfiles bumped `0.2.46 → 0.2.47`). Closes GitHub issues **#44/#45/#46/#47**. **#46 (read-only project-source adapters, agent):** new `agent/core/project-sources.mjs` + `agent/lib/markdown-todo.mjs` import from local Markdown todo files and public GitHub issues, both **strictly allowlisted, fail-closed**: local paths only under a configured `local_root` with traversal + symlink-escape rejected and file size bounded; GitHub only for `allow_github` `owner/repo` pairs, no credential needed, base URL pinned (`_ghPin`, `redirect:'error'`) as an SSRF guard, with bounded response bytes / `max_issues` (200) / `max_pages` / timeout / rate-limit detection. Records normalize to a stable-fingerprint shape (`{fingerprint, project, source{type,ref,label,id}, title, description, status, priority, labels[], url, readOnly:true, createdAt, updatedAt}`), `dedupeRecords` collapses repeats, and the last valid snapshot is cached per project + **retained on partial/offline/error**. Nothing logs content/tokens/full identifiers. New `project_sources:` config block validated in `loadConfig` (enabled-but-unallowlisted / markdown-without-`local_root` / malformed `allow_github` / unknown type all refuse boot); new admin-gated `GET /api/projects/:slug/sources` + rate-limited `POST /api/projects/:slug/sources/refresh`. **#45 (imported cards on the Kanban, frontend):** `src/views/board.js` keeps imported records in an **ephemeral per-slug `importState` Map, never written to the board store**, so manual cards are never overwritten and repeated refreshes never duplicate (keyed by fingerprint). Each renders as a `board-card imported` container — not draggable, no move/edit controls, source badge + priority/label pills, read-only `imported-badge`, `Read-only imported card:` aria-label, safe external link (`target:'_blank'`, `rel:'noopener noreferrer'`); `renderSyncBar` adds source + status filters, a manual Refresh button, a last-sync line, and stale/partial/error `sync-banner`s. New client fns `walletHealth`/`projectSources`/`refreshProjectSources` in `src/data/agent.js`. Mounted `/continuum/` base path, accessibility, mobile, dark styling, and login/dashboard/about behavior all preserved. **#47 (real Cashu wallet health, agent + frontend):** admin-gated `GET /api/wallet/health` backed by a **non-mutating** `wallet.health()` using cashu-ts (^3.7.1) v3 read primitives (`loadMint()` + `getMintInfo()` identity getters + `.isSupported(7)` NUT-07 + `groupProofsByState`) — no mint/melt/swap, serialized on-disk wallet untouched; honest disabled/healthy/degraded/unreachable states + sanitized reasons; never exposes tokens/proofs/secrets; `src/views/dashboard.js` adds a wallet-health row to the Provider card (polled alongside `healthModels()` via `Promise.all`). **#44 (docs):** todo/progress/handoff/README refreshed through v0.2.46 (stale "next-action" claims removed) then this release recorded with evidence; examples generic/configurable, no real hostnames/secrets, no cross-project queue contamination. Tests: new agent `project-sources.test.js` **29** / `markdown-todo.test.js` **12** / `project-sources-config.test.js` **9** (subprocess) / `wallet-health.test.js` **14**; extended `src/data/agent.test.js` **23** + `src/views/board-structure.test.js` **14**. **Root `vitest run` 853/853, agent `node --test` 168/168**, `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz), `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows **5 dev-only** findings (esbuild/vite/vitest toolchain, not shipped in `dist/`; fix is a breaking major-vite bump, deferred). Migration is additive/backward-compatible — `project_sources` defaults disabled; wallet probe reads existing state without mutation. Code + PR only — **not merged, tagged, or deployed.** **PR #48 review remediation (same branch, version held at v0.2.47-alpha):** a blocking review was addressed in place — **M1** partial-refresh data loss fixed (`refresh()` retains failed sources' last-valid records per-source via a `bySource` Map, replaces only healthy ones, preserves the full snapshot + `syncedAt` on total failure, honest `partial`/`stale`/`retained`); **M2** dedupe title-collapse scoped to Markdown-only so distinct/cross-repo same-title GitHub issues survive (native `fingerprint` wins); **M3** `mintHealth` extracted+exported and covered with a mocked cashu-ts v3 wallet (healthy/degraded/unreachable/timeout/no-NUT-07/spent-pending/no-mutation/no-leak); plus **L4** `GET /api/wallet/health` rate-limited (`wallet_health_per_min`, default 6) + 15 s server cache, **L5** `insertionIndex` ignores imported cards, **L6** absolute `local_root` enforced, **L7** numeric bounds hard-ceilinged (`CEILINGS`), **L8** dead `cache_ttl_sec` removed, **L9** `ok: anyOk` simplified, **L10** empty sync bar suppressed, **N11/N12** doc wording corrected (read-only identity/NUT-07 probe, source/mocked-unit guards not live-browser tests). Re-verified: agent 168/168, root 853/853, build clean, audits unchanged. No ops files touched. Prior slice: **v0.2.46-alpha shipped LOGIN-FIX: sidebar/demo Login "Could not reach agent: Bad Request"** (frontend-only; **no agent code changed**, agent stays **v0.2.43-alpha**, onboarding preview stays **v0.1.20-preview**). In the demo shell (`#/projects`), clicking the bottom-left sidebar **Login** with Plebeian Signer installed failed with *"Could not reach agent: Bad Request"*. **Root cause:** the SPA↔agent client `req()` in `src/data/agent.js` always sent `Content-Type: application/json`, even for the **bodyless** `POST /api/auth/challenge`; Fastify v5 rejects an empty body carrying that content-type with **400 `FST_ERR_CTP_EMPTY_JSON_BODY`** (`error:"Bad Request"`) before the handler runs — so the challenge never issued. The onboarding client's `postJson` was already hardened against exactly this (v0.2.32-alpha, pinned in `agent/test/fastify-v5-api.test.js`); the SPA client had **drifted** and never got the fix. **Fix:** `req()` now declares the JSON content-type **only when a body is actually sent** (mirrors onboarding `postJson`; challenge POST now 200s). Added `errorReason(json,status)` (pure, exported) to surface the specific Fastify `message` over the generic label (200-char cap, never stack traces). Both login entry points (`src/views/login.js` view + `src/shell.js` sidebar button) already share the single `startLogin()` in `src/auth.js` — no duplicate flow to merge; the drift was in the shared HTTP client. Privacy/keys unchanged (nsec stays in the extension; agent still verifies id+sig+admin-pubkey+challenge). Tests: new regression cases in `src/data/agent.test.js` (mock NIP-07 signer + fetch; assert challenge POST has no content-type/no body, verify POST sends exact `{event}` + JSON content-type + adopts token, full `errorReason` map). `vitest run` **843/843**, agent `node --test` **104/104**, ops shell suites green, `npm run build` clean. Root version → **v0.2.46-alpha**; agent NOT bumped (its empty-body 400 is correct standard Fastify behaviour — no protocol change). **Kanban discoverability (reported):** board is at `#/projects/:slug/board`, reached via the **Overview · Board** switcher after opening a project; all project routes are demo-browsable (only `/dashboard` is auth-gated), so the Board link **is** visible in demo mode — just not surfaced on the `#/projects` index cards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.44-alpha ships APP-ROUTING: application-first root (login + dashboard), sales page isolated to `#/about`** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Fulfils the operator ask *"separate out that sales page from the root of continuum and replace it with a login page and the dashboard."* The root (`/continuum/`) is now the application, never the marketing page. A pure, DOM-free guard layer (`src/nav-guard.js`: `rootTarget`/`guardRedirect`/`isProtectedPattern`, `PROTECTED_PATTERNS=['/dashboard']`) decides routing: **logged in → root redirects to `#/dashboard`; logged out → a branded Amber login page (`src/views/login.js`) renders in place at root** (shared `toriiSvg` mark, full-bleed `landing-mode`, drives the existing NIP-07/Plebeian-Signer `startLogin()` — no password auth, no key material in the DOM; small non-primary links to `#/about` + the demo shell). The former marketing view is reframed/exported as `renderAbout` in `src/views/landing.js` and lives only at **`#/about`** — never the root, never an onboarding-completion target. `#/dashboard` is guarded: a logged-out visitor (incl. refresh / deep link, since guards run on every router resolve including the initial one) is bounced to login at root; because root renders login **in place** (never itself bouncing when logged out), `protected → root → login` is terminal — **no redirect loop**. A session drop while on a protected view (sign-out / 401 expiry) reactively returns to login via the `continuum:session-changed` listener. `adoptOnboardingSession()` still runs before first render, so a freshly onboarded operator with a live `torii.session` lands authenticated on the dashboard (no login bounce); demo views (`#/projects`/`#/marketplace`/`#/routstr`) + all Amber nav preserved. No Google/gstatic/Fontshare/CDN/analytics added — local/system assets + inline SVG only. New `test/app-routing-auth.test.js` (**20** tests): unauth-root→login, auth-root→dashboard, dashboard protection, no-loop, `#/about`/demo isolation, onboarding adoption→authenticated root, expired/invalid→login, plus source-structure locks on `main.js`/views. Browser QA: no headed browser / no jsdom in the sandbox, so the contract is proven by the exhaustive pure guard tests + the source locks + the rebuilding no-external-request scan (guards run on the initial resolve = refresh/deep-link). Root `package.json` → **v0.2.44-alpha**. **Ops addendum in the same PR (v0.2.44-alpha):** a live v0.2.43 rerun hit one more exact bug — `group_vars/all.yml.example` shipped `torii_root_app: launcher`, but the installed CLI/API accepts `torii set-root <registered-name|none>` and returned **404 `{error: app_not_installed, name: launcher}`** (the Torii Suite launcher at `/` is the sentinel `none`, not a registered app), rolling the deploy back; rollback worked and the operator reran with `-e torii_root_app=none`. Fix: the example default is now **`torii_root_app: "none"`**, and the continuum role no longer calls `set-root` raw — it routes through **`ops/lib/continuum-adopt.sh set-root-safe`**, which `normalize_root_app`-maps legacy/empty/`launcher` (+ `torii`/`base`/`root`/`home`/`homepage`/`default`) → `none`, sources the admin token from `/opt/torii/env` silently (never printed), **skips** the call when `torii get-root` already matches, and returns 0 for the default so a normal install can never trip the rescue rollback. An explicit **registered** app name still passes through unchanged; pointing the root at `continuum` emits a discouraged-choice warning but is honoured when set deliberately — **Continuum is never made the root by its own installer.** `ops/README.md` gains a "Root-app selector correction (v0.2.44-alpha)" subsection; `continuum-adopt.test.sh` grew **188 → 219** (mock `torii` CLI reproducing the exact live 404; `launcher`→`none` normalization incl. synonyms/whitespace/case; default skip when already none + reset when not; explicit registered app honoured; continuum-warn; no-token-leak; example/role anti-drift greps). No-Google/no-CDN guarantees untouched. Tests: `vitest run` **795/795** (775 prior + 20), agent `node --test` **104/104**, `continuum-adopt.test.sh` **219** + ops regressions 18/10/9/13, root build clean (`dist/` free of external loading vectors, only the inert Plebeian-Signer store anchor remains), `npm audit --omit=dev` 0 vulns (root + agent). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.43-alpha ships OPS-HARDENING: encode three live-discovered cutover corrections** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). The v0.2.42-alpha cutover succeeded manually on the live box; three environment realities had to be fixed by hand and are now encoded permanently. **(1) Torii CLI `register` uses flags, not positionals** — the installed CLI is `torii register <name> [--display …] [--desc …] [--version …]`; the old positional form failed with `unknown flag: Continuum`. The role now calls `torii register {{ continuum_register_name }} --display "Continuum" --desc "App builder + agent" --version <bare-semver>` (bare version via `continuum_version | regex_replace('^v','')`), with new `continuum_register_*` defaults. **(2) `MemoryDenyWriteExecute` must be `no`** — Node 22's V8 JITs with W^X memory; `=yes` makes the kernel refuse the mprotect and V8 dies on startup (fatal `SetPermissions` / errno 12 / `SIGTRAP`). `continuum-agent.service.j2` now sets `MemoryDenyWriteExecute=no` with an inline rationale and keeps every other compatible directive (`NoNewPrivileges`, `Protect*`, `RestrictNamespaces`, `LockPersonality`, empty `CapabilityBoundingSet`). The role runs a **Node V8 JIT smoke test** under the rendered MDWE value via a transient `systemd-run` scope before starting the real service. **(3) nginx cannot traverse `/home/continuum`** (0750 home + `ProtectHome` → HTTP 500) — the build is now published to a **public webroot `/var/www/torii/continuum`** (root-owned, dirs 0755 / files 0644) via new `deploy_webroot` (atomic staged swap, prior webroot kept as timestamped backup); only the static bundle is copied out, source + encrypted state stay private under `/home/continuum/app`. A **single** `location /continuum/` prefix alias serves both the SPA entry and the hashed `assets/*` bundle (no regex-alias, no `/home`). The cutover `rescue` now also rolls the webroot back (`rollback_webroot`) + reloads nginx on failure. Re-running against the now-live v0.2.42 Ansible layout is idempotent (re-detects `existing-ansible`, preserves config, never rotates `session_secret`/touches the funded key). `ops/test/continuum-adopt.test.sh` grew **117 → 188** assertions (webroot deploy/rollback + perms + dangerous-target refusal; mock Torii CLI parser reproducing `unknown flag: Continuum` vs flag-form success; systemd MDWE=no contract + retained hardening; Node V8 WASM smoke; nginx section is now a **no-nested-assets regression lock**; existing-ansible upgrade idempotency). New `ops/README.md` v0.2.43 subsection + upgrade command. Root + agent version → v0.2.43-alpha (+ both lockfiles). Tests: adopt **188/188**, ops regressions green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. **AMENDMENT (pre-merge, same PR #40) — frontend zero-external-request + asset-404 fix:** (4) removed all production Google (`fonts.googleapis.com`/`fonts.gstatic.com`) **and** Fontshare (`api.fontshare.com`) preconnects/stylesheets from `index.html`; typography is now a **deliberate local system font stack** in `src/styles/theme.css`, so production makes **zero external requests** (no fonts downloaded/vendored). (5) the live black page was the nested `location /continuum/assets/` alias+`try_files` returning 404 for hashed `.js`/`.css`; both nginx templates now use a **single parent alias** only. (6) `ops/README.md` upgrade steps corrected to the real live paths (`/opt/deploy/torii-continuum-v0.2.42-alpha` checkout, app at `/home/continuum/app`; prefer a fresh version-specific public clone of the final tag) — the old `/opt/torii/src/torii-continuum` never existed. New frontend tests: `test/no-external-cdn.test.js` (builds `dist/`, asserts index + assets carry no banned CDN/font host and no external loading vector; same-origin `/continuum/api` allowed) and `test/nginx-continuum-routing.test.js` (HTTP server modelling nginx `alias`+`try_files`, using the **real hashed filenames** from `dist/index.html`, proves `/continuum/` index + hashed `.js`/`.css` = 200 and deep-route SPA fallback). `vitest run` now **775/775**. Browser QA: no headed browser in the sandbox — verified via the build + HTTP-200 routing + same-origin-only fetch scan; the app is a hash-router SPA (`#/`,`#/projects`,`#/marketplace`,`#/routstr`,`#/dashboard`) so the server contract is fully exercised, and the live box already rendered the Amber dashboard with zero console/network errors after the manual asset-route fix. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.42-alpha ships OPS-HARDENING: transactional adoption/cutover + partial-adoption recovery** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the live v0.2.41-alpha adoption failure: the role migrated live state INTO `/home/continuum/app/agent` and only THEN ran `ansible.builtin.git` into `/home/continuum/app` — cloning into a dir already populated with runtime state. It aborted, leaving the **original standalone layout intact** AND a **partial, non-git `/home/continuum/app`** holding a copy of the state; the untouched `torii-continuum-agent.service` was restored and health is good. **Fix (`ops/lib/continuum-adopt.sh` + `roles/continuum/tasks/main.yml`):** `layout_detect` gains a 3rd `app_dir` arg and a new **`partial-adoption`** mode (agent has state but `app/.git` absent while standalone is still present — a real git-backed Ansible install still wins; a non-git app dir carrying state is recovered, never built on top of). New `authoritative_state_dir` picks the untouched standalone as the live source over the partial copy. New `stage_reset`/`promote_release`/`rollback_release`: the `git checkout` + `npm ci` + `vite build` run in a **clean staging dir** (`/home/continuum/app.staging`) while the **old unit keeps serving**; authoritative state is copied in **only after a successful build**; the old unit is stopped only immediately before an **atomic same-filesystem swap** into `/home/continuum/app`, quarantining (never deleting) any pre-existing app dir to `app.quarantine-<UTC>`. The `block`/`rescue` cutover stops the new unit on any promotion/`/api/health` failure, rolls an existing-Ansible tree back from quarantine (or re-enables the standalone for adopt/partial), and prints backup + quarantine paths with an exact **secret-free** recovery command. Re-running after the exact observed failure is idempotent. `ops/test/continuum-adopt.test.sh` grew to **117** hermetic assertions (3-arg detect incl. partial-adoption + absent-app-dir; `authoritative_state_dir`; `stage_reset` refuses `/ /home /root /opt /opt/torii`; `promote_release` clean/quarantine/refuse-existing-quarantine/idempotent; `rollback_release` restore/refuse-without-failed-dir/no-quarantine; end-to-end partial-adoption recovery proving byte-for-byte config equality + funded-key retention + quarantine preservation + untouched standalone; v0.2.42 anti-drift greps that clone/build target staging, state is staged before promotion, promotion+rescue are gated on a swap fact, and the old unit stops only at cutover). New "Transactional cutover + partial-adoption recovery (v0.2.42-alpha)" subsection in `ops/README.md` with the exact recovery invocation for the current partial-adoption VPS (same vault-free localhost invocation, `continuum_version: v0.2.42-alpha`). Root + agent version → v0.2.42-alpha (+ both lockfiles). Tests this slice: adopt **117/117**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.41-alpha ships OPS-HARDENING: vault-free standalone adoption / redeploy** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes the blocker left by v0.2.40-alpha: on `adopt-standalone`/`existing-ansible` the live `config.yaml` is preserved byte-for-byte, but the preserve path still rendered a `.config.candidate.yaml` from `config.yaml.j2` (which references `{{ admin_npub }}`/`{{ session_secret }}`) for the drift diagnostic — so a truly vault-free run (no `group_vars/vault.yml`, no vault password) died with an undefined-variable error before finishing adoption. **Fix (in `roles/continuum/tasks/main.yml`):** a new `continuum_vault_vars_present` fact is computed defensively via the `default('')` filter (an undefined var yields `false`, never raises); the candidate render + `config-drift` + candidate removal + "differ" warning are all guarded behind `continuum_vault_vars_present | bool` and SKIP entirely when the vault vars are absent (a non-secret debug notes the diagnostic was skipped). Adoption still preserves the live config byte-for-byte, so the funded Routstr key stays decryptable. A fresh install or an explicit `continuum_allow_config_rotation=true` rotation still needs the vault vars, and their absence in those modes **fails closed** with a clear, non-secret `ansible.builtin.fail` *before* any state is written. `ops/test/continuum-adopt.test.sh` grew to **66** hermetic assertions (vault-free adopt end-to-end incl. byte-for-byte config preservation + funded-key migration + no-secret-in-output; existing-ansible redeploy; fresh missing-vars fail-closed; explicit-rotation missing-vars fail-closed; presence truth table; anti-drift greps for the new guards). New "Vault-free adoption / redeploy (v0.2.41-alpha)" subsection in `ops/README.md` gives the exact secret-free localhost invocation (inventory `torii→localhost ansible_connection: local`, `group_vars/all.yml` with `torii_domain`/`continuum_version`, `ansible-playbook -i inventory.yml site.yml --tags continuum` with NO `--ask-vault-pass`). Operator handles NO secrets and needs NO vault password for adoption. Tests this slice: adopt suite **66/66**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.40-alpha ships OPS-HARDENING: safe standalone→Ansible adoption, no config clobber, fail-closed backups** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the dangerous deployment mismatch that blocked the live v0.2.39-alpha rollout: the continuum Ansible role cloned to `/home/continuum/app` and **re-rendered `config.yaml`/`session_secret` unconditionally on every run**, while the live standalone box uses `/opt/torii/continuum-agent` + unit `torii-continuum-agent.service` on port 8787 — so running the old role over that box would have overwritten the session_secret (orphaning the funded Routstr key, encrypted at rest under a key derived from it) and/or double-bound 8787. **Fix:** the risky detect/backup/migrate/config-decision logic is factored into a sourceable, unit-tested lib `ops/lib/continuum-adopt.sh`, and `roles/continuum/tasks/main.yml` is now a thin guarded orchestrator that (1) **detects** layout (`fresh`/`adopt-standalone`/`existing-ansible`; an existing Ansible layout always wins); (2) takes a **fail-closed** timestamped **root-only 0700 backup** of `config.yaml`+`memory/`+`ciphertexts/`+`pending/` from both layouts under `/root/continuum-backup-<UTC>/` before any mutation (no backup ⇒ abort); (3) on adopt **stops+disables** the standalone unit (frees 8787) and **migrates config+state verbatim** — `session_secret` copied byte-for-byte, never regenerated, migration idempotent; (4) **never clobbers** an existing config on routine deploy — renders from vault ONLY on a fresh install or explicit `-e continuum_allow_config_rotation=true` (off by default), and on a preserve runs a one-way drift check that prints only `same`/`differ` (never a value) and surfaces a safe "rotation pending" notice; (5) wraps the service+nginx cutover in a `block`/`rescue` that on failure prints the backup path + exact recovery command. `git force:true` resets only tracked files, and all agent state is gitignored, so a re-checkout/build can't delete encrypted state. New `roles/continuum/defaults/main.yml` holds the safe tunables (`continuum_standalone_dir`, `continuum_standalone_service`, `continuum_backup_root`, `continuum_allow_config_rotation: false`); secret-touching tasks carry `no_log: true`. Pinned by `ops/test/continuum-adopt.test.sh` (**44** hermetic assertions: detection precedence, fail-closed backup, verbatim+idempotent migration, config-action matrix, no-secret drift, permissions, anti-drift greps). New "Adopting a standalone install with Ansible" runbook in `ops/README.md`. **Operator handles NO secrets** at any point; deploy is the normal `ansible-playbook … --tags continuum`. Tests this slice: adopt suite **44/44**; unchanged app/agent suites not re-run beyond the required build/audit. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.39-alpha ships ONBOARDING-GATING + APP-MOUNT: real claimed-state Step-3 gating, dup-pay race closed, completion lands on the actual app dashboard** (onboarding preview → **v0.1.20-preview**, `preview-assets/onboarding-v0.1.20/`). Two live defects on the v0.1.19/v0.2.38 stack plus the app-mount follow-through. **User-funded Routstr key, all encrypted agent memory, and prior payment state preserved — no invoice created/paid/re-submitted, no `memory/**` touched.** **(A) Claimed-state gating (root cause):** a verified funded key still rendered both setup cards, key-entry/reveal, *Verify & connect* and *Request an invoice* — a CSS cascade defect, not logic: author `display` rules (`.btn-primary{inline-flex}`, `.wallet-choices/.routstr-paths{flex}`, `.choice-card{grid}`) outrank the UA `[hidden]{display:none}`, so `el.hidden=true` visually no-op'd. Fix: canonical `[hidden]{display:none !important}` in `shared.css`; the status-only resume path funnels through `renderSuccessAdvance` via a single `showClaimed` helper (replaces `showConnected`); the panel carries `data-claimed="1"` as single source of truth, leaving one green summary + exactly one standalone Continue. **Dup-pay race closed:** `data-claimed` checked as the first line of quote/key-connect/pay-confirm handlers (pay button also permanently disabled once claimed) — a second invoice/payment is structurally impossible in claimed state. **(B) App mount:** `CONTINUUM_HOME` `/continuum/` → `/continuum/#/dashboard` (+ curtain anchor) so completion opens the multi-view Amber SPA dashboard, not the marketing root; open-redirect guard preserved. **Session handoff:** `adoptOnboardingSession()` (in `src/data/agent.js`, called once in `main.js` before `initStore()`) fails closed if the SPA already has a live session, else adopts the `localStorage['torii.session']` envelope token into `continuum.session.v1` only if `tokenLooksLive` (shape + unexpired), so a freshly onboarded user is not bounced to sales/login; `deriveSameOriginBase` makes `agentUrl()` fall back to same-origin `/continuum` under the mount (no CDN/third-party origin). **No separate torii-suite PR required** — the `/continuum/` alias + subpath refresh + `/continuum/api/*` proxy + SPA build + agent install + nginx fragment + launcher registration are all in this repo's `ops/ansible`; the root launcher is never overwritten. **Safe idempotent deploy (operator step, NOT run here, from `ops/ansible/`):** `ansible-playbook -i inventory.yml site.yml --ask-vault-pass --tags continuum`. **(C) Wording:** residual VPS/daemon/box copy in `routstr.js`/`landing.js`/`auth.js` scrubbed to "your Torii, your gateway". Tests: root vitest **762/762** (new `src/data/agent.test.js` 11 cases; preview v0.1.20 **153**), agent `node --test` **104/104**, `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Honest limitation: no jsdom/live agent here, so no live funded-agent click-through — gating proven by the CSS `!important` cascade guarantee + source/behaviour guards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.38-alpha ships ONBOARDING-FINISH: verified-state cleanup, correct balance units, inline key reveal, deterministic final curtain** (onboarding preview → **v0.1.19-preview**, `preview-assets/onboarding-v0.1.19/`). A focused live UX/completion pass from three screenshots; no character/animation/CDN/provider-pinning behaviour changed and no payment/NWC semantics changed beyond balance *display* and safe key *retrieval*; the funded key + prior state are preserved. **(A) Balance units (root cause):** Routstr denominates balances in MILLISATS but `extractBalanceSats` surfaced the raw `balance` verbatim, so a 10,000-sat key showed "10000000 sats"; the provider adapter now divides msat fields to whole sats (explicit `*_sats` trusted), a marker-guarded `displayBalanceSats` migration divides legacy stored envelopes (no `balance_units`) by 1000 on read while new envelopes carry `balance_units:'sat'`, and all balances render via `formatSats` (e.g. `10,000 sats`). **(B) Step 3 verified state:** once claimed/encrypted/verified, `collapseStep3Setup` hides both path cards, both forms, the invoice card, the progress bar and all three setup CTAs, leaving one green summary + a SINGLE Continue button outside it, no countdown/auto-advance (supersedes the v0.1.18 countdown), advance idempotent. **(C) Step 5 recovery kit:** removed the unbranded "Reveal full Routstr key (one-time)" button + its `window.confirm` flow (never revealed the key); the ROUTSTR KEY row now has branded inline eye + copy controls that fetch the key over the existing admin-authenticated no-store `export-key` endpoint, hold it in memory only (never localStorage/sessionStorage/URL/logs), and re-mask on toggle/step-leave/tab-hide/session-loss/timeout (`shouldMaskReveal`/`REVEAL_TIMEOUT_MS`); the Download click is itself the explicit confirmation and embeds the full key in the saved file while still excluding the NWC secret. **(D) Final curtain:** the old curtain only `console.log`'d a commented redirect and spun forever; `initCurtain` now navigates deterministically to the resolved same-origin `/continuum/` home (a `window.__toriiContinuumHome` override accepted only same-origin — open-redirect guard), reveals a real "Open Continuum now" fallback link on a bounded timer, honours `prefers-reduced-motion`, and drops the spinner shortly after the nav attempt so it can never hang. Tests: agent `node --test` **104/104**, root vitest **269/269** (preview v0.1.19 **145**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.37-alpha ships ONBOARDING-PAY-RECOVERY: paid-but-unclaimed Routstr recovery + payment progress state machine + working recovery kit** (onboarding preview → **v0.1.18-preview**, `preview-assets/onboarding-v0.1.18/`). Fixes a live incident where an operator connected NWC and paid 10,000 sats to fund a Routstr session, but the UI said "payment confirmed" without surfacing the `sk-` key, never auto-advanced, and the Recovery Kit "download" was a no-op that just stepped the deck. **No new invoice created or paid during diagnosis or fix.** Delivered a safe SSH recovery runbook first (inspect `memory/secrets/*.enc` by metadata only — never decrypt; reuse the operator's existing browser session token against the loopback admin API; claim the already-paid key with the **idempotent** `POST /api/onboarding/routstr/recover` sending `{}` — the agent re-submits the stored bolt11 and never re-pays; redacted status/verify; one-time no-store `export-key` only if a manual copy is required). Root fact: **payment-confirmed does NOT imply the key is stored** — the claim is a separate post-settlement step; recovery states are paid+claimed / paid+unclaimed (the live case) / unpaid-or-expired. **Agent (v0.2.37-alpha):** new admin-gated endpoints `GET /api/onboarding/recovery/state` (redacted resume snapshot; `claimable` true only when a pending invoice is stored with no key), `GET /api/onboarding/recovery-kit` (`Cache-Control: no-store`, secret-free by default), `POST /api/onboarding/routstr/export-key` (one-time full-key reveal; `confirm===true` required else 400, 409 when no key, `no-store`, rate-limited, persists `export_count`/`last_exported_at` audit, logs warn never the key). **Preview (v0.1.18):** pure `ONBOARD_PHASES`/`PHASE_META` state machine driving an accessible (role=status, aria-live) progress + scanning bar + elapsed timer across connect→quote→pay→claim→verify; **SUCCESS only when the agent reports `key_stored===true`** (`classifyPayResult`/`classifyRecoverResult` map bare paid/recoverable → retryable `PAID_UNCLAIMED`, never success); duplicate-payment prevention (Confirm button permanently disabled after first click); success renders the redacted key then auto-advances after a short countdown with an immediate Continue-now; refresh-resume reads `recovery/state` on load and finishes a `claimable` claim via empty-body `recover` (no re-payment); real Recovery Kit download via Blob (`buildRecoveryKit`) whose default excludes the NWC secret + full key and carries redacted previews + restoration instructions, with the full key included only via an explicit confirmed one-time `export-key` reveal; Step 5 markup no longer prints static secret-adjacent values. Preserved: character/animation behaviour, self-hosted Three.js (no CDN), provider pinning, redaction, rate limits, no autonomous spend, no nsec on server. Tests: agent `node --test` **102/102**, root vitest **453/453** (preview v0.1.18 **124**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.36-alpha ships ONBOARDING-ASSET: new Chiefmonkey GLB + forbidden-safe animation state machine** (onboarding preview → **v0.1.17-preview**, `preview-assets/onboarding-v0.1.17/`). A substantial asset + animation upgrade of the design-review onboarding mockup (NOT the production app). A fresh Chiefmonkey source GLB replaces the old `chiefmonkey6.glb` and is crushed for production by a new reproducible local optimizer `tools/optimize-glb.mjs` (gltf-transform 4.4.1 + draco3dgltf 1.5.7 + sharp 0.35.3, build-time only, no third-party runtime CDN): `dedup → weld → resample → textureCompress(webp,q82,≤1024²) → prune → draco(edgebreaker)`, dropping every forbidden locomotion/knock-down clip with the runtime's own `isForbiddenClip` predicate (single source of truth). Source 9,298,852 B (SHA `87b0048c…c37dd`) is kept OUT of git (build artifact only, see `tools/SOURCE.md`); the committed optimized `assets/chiefmonkey-onboarding.glb` is 2,347,780 B — **74.75% smaller** (target ≥60%), SHA `0253d5e1…e2fcb` — with a `.manifest.json` recording sizes/%reduction/deterministic SHA + retained/dropped clip inventory (13 retained, 5 forbidden dropped: `Clapping_Run`/`Knock_Down`/`Running`/`Stylish_Walk_inplace`/`Walking`; mesh/skin(24 joints)/material/texture preserved nonzero, clip names survive Draco). `character.js` now runs a dedicated forbidden-safe state machine — each of the 5 deck steps plays one dedicated clip from `STEP_CLIPS`/`selectStepClip` (1 Talk_with_Hands_Open, 2 Agree_Gesture, 3 mage_soell_cast_3, 4 Gentlemans_Bow, 5 Idle_10; dormant step-6 Victory_Cheer curtain), applied deterministically on entering/restoring via the v0.1.15 `resolveReadyStep` path (order-independent) — and a raycast hit-test (`Raycaster.intersectObject`, NDC from canvas rect) plays a random one-shot from a curated `CLICK_POOL` (`pickClickReaction`, no immediate repeat) then crossfades back to the current step clip on the mixer `finished` event, ignoring UI-control clicks / respecting `prefers-reduced-motion` / ignoring click-spam. Step-1/2/3 prompt/success/failure `onboarding:anim` overrides are transient and always return to the current step clip (no race can permanently overwrite step state). Deterministic offline tests parse the optimized GLB JSON chunk (counts nonzero, referenced clip names survive), assert no forbidden pattern is selectable, verify the committed asset matches manifest SHA + size budget (≤3 MB, ≥60%), and cover every step mapping / restore ordering / status→current-step return / click hit-no-hit + UI-guard + no-immediate-repeat + spam guard + crossfade return / reduced motion / final load-error. Verified target-file + full root vitest green and `npm run build` clean. Code + draft PR only — preview snapshot only, not deployed. Prior slices (onboarding preview arc): **v0.2.35-alpha / v0.1.16-preview** (PR #32) reworked Step 2 (wallet-agnostic NWC connect via NIP-47 `get_info` capability matrix, funding gated on `pay_invoice`) and Step 3 (Routstr existing-`sk-…`-key OR fund-a-new-session: quote → explicit confirm → NWC pay → claim minted key, with a RECOVERABLE poll-timeout carrying the bolt11 via `/lightning/recover`); secrets stored encrypted at rest (AES-256-GCM), admin-gated + rate-limited, pay refuses unless `confirm===true` AND `pay_invoice` advertised. **v0.2.34-alpha / v0.1.15-preview** (PR #31) added the deterministic character↔deck step-sync state machine (`createCharacterSync`/`recordStep`/`resolveReadyStep`/`markCharacterFailed`) that fixed the intermittent invisible-Chiefmonkey-after-soft-reload race by remembering steps before model readiness instead of dropping them then hard-applying Step 1. **v0.2.33-alpha / v0.1.14-preview** (PR #30) fixed refresh dropping Step 2 → Step 1 (`restoreSession`/`isSessionValid` fail-closed, `window.__toriiRestoredStep`) and Chiefmonkey invisible on reload (route both GLB stall AND error through `nextLoadAttempt`). Prior slice: **v0.2.32-alpha ships ONBOARDING-STEP1-FIX: "agent challenge failed (400)" on Sign with Plebeian Signer** (onboarding preview → **v0.1.13-preview**, `preview-assets/onboarding-v0.1.13/`). Client-only fix: `onboarding-client.js`'s `postJson` was sending `Content-Type: application/json` on the **bodyless** `POST /api/auth/challenge`, and Fastify v5 rejects an empty body carrying that header with `400 FST_ERR_CTP_EMPTY_JSON_BODY` — failing step 1 before the NIP-07 prompt (which only fires after a challenge is fetched), so Chiefmonkey animated but no signer opened. Fix: `postJson` only sets the JSON content-type + serialises a body when a body is actually present; the challenge call now goes out bodyless (→ 200), verify unchanged. No server endpoint added, no validation weakened, no bunker-connect endpoint; NIP-07 primary + browser-client NIP-46 secondary, `localStorage['torii.session']`, exact signer wording, and the three clips (`HandGesture_00`/`Idle_03`/`Confused_02`) all preserved. New offline vitest case (challenge POST has no body/no JSON content-type; verify still does) + new agent `fastify-v5-api` case pinning the empty-body-400 vs bodyless-200 contract. Prior slice: **v0.2.31-alpha shipped ONBOARDING-STEP1: live NIP-07/NIP-46 auth for onboarding step 1** (onboarding preview → **v0.1.12-preview**, `preview-assets/onboarding-v0.1.12/`). New self-contained `onboarding-client.js` (ES module, no build step, no third-party CDN) wires step 1 to the same-origin agent API. NIP-07 primary via `window.nostr` (button "Sign with Plebeian Signer"): `POST /api/auth/challenge` → sign the exact kind-22242 auth event `agent/core/auth.mjs` expects (`content == challenge`, `['challenge', …]` + `['relay', origin]` tags) → `POST /api/auth/verify` → persist to exactly `localStorage['torii.session']` as `{ token, expires_at, pubkey, method, created_at }` (token + public identity only, no secrets). Fails closed on malformed responses, expired challenges, pubkey/challenge mismatch, or absent token. NIP-46 secondary with the browser as the client (bunker46 architecture, "Use a different signer"): parses a pasted `bunker://` string and asks the remote signer to sign the same event over its relay — **no server bunker-connect endpoint**, no key/secret sent to the agent, no silent fallback. Chiefmonkey phases via the existing event channel: `HandGesture_00` prompting/signing, `Idle_03` success, `Confused_02` failure, each with an ordered fallback to an existing GLB clip (the GLB ships neither `HandGesture_00` nor `Confused_02`, so the fallback runs and never freezes). Desktop-only gate + all prior perf fixes preserved. 28 offline vitest cases; verified root vitest 28/28, root build, agent `node --test`, ops regressions, agent `npm audit --omit=dev`. Code + draft PR only. Prior slice: **v0.2.30-alpha shipped AGENT-SEC-OPT-TORII-PERMS: least-privilege fix for the shared `/opt/torii` parent.** The v0.2.29 deploy surfaced a production regression — `ops/install-agent.sh` created the shared parent with an unconditional `install -d -m 0750 -o continuum -g continuum /opt/torii`, and because `install -d` re-applies mode+owner on every run it clamped the parent that torii's *other* apps live under (torii-base launcher, quest tooling) to `0750 continuum:continuum`, stripping its world-execute bit. nginx (`www-data`) could then no longer traverse `/opt/torii` to reach `/opt/torii/launcher/index.html`, so `/` fell through to a default nginx 404 (Quest under `/var/www` stayed readable). Fix: create `/opt/torii` **only if absent**, `root:root 0755`, and **never re-own/re-mode an existing shared parent**; the agent's own subdir `/opt/torii/continuum-agent` stays locked `0750 continuum:continuum` (nginx never serves from it — the agent is loopback-proxied on 8787). New hermetic + anti-drift regression `ops/test/installer-shared-parent.test.sh` (10 assertions) pins that the installer no longer chowns the shared parent to the service user, guards creation with an existence check, and keeps `$INSTALL_DIR` locked. The previously-planned **onboarding** work moves to **v0.2.31-alpha**. Operator out-of-band unblock if needed before redeploy: `sudo chown root:root /opt/torii && sudo chmod 0755 /opt/torii` (restores o+x, leaves the agent subdir untouched). Prior slice: **v0.2.28-alpha shipped AGENT-SEC-CASHU-LTS: the money-path dependency migration flagged in v0.2.27.** `@cashu/cashu-ts` moved `2.5.3 → 3.7.1` (the maintained `v3-lts` "security-fixes-only" line; dist-tag `v3-lts` resolved to `3.7.1` at implementation time, latest was `4.7.0` — v4 deliberately not taken), pinned `^3.7.1`, lockfile regenerated with sha512 integrity and the deprecation notice cleared. Code adapted in `agent/core/wallet.mjs` only: class rename `CashuMint→Mint` / `CashuWallet→Wallet`; the boot warm-up `getMintInfo()` (async in v2, now a sync cached getter that throws pre-load in v3) replaced by `await wallet.loadMint()`; an idempotent `ensureLoaded()` guard added before `receive()`/`send()` to restore v2 lazy-load-on-demand (verified zero extra network once keysets are cached). Token/proof codec, `Token`/`Proof`/`SendResponse` shapes, and the `receive()→Proof[]` contract are unchanged between the versions (both default to token-v4 `cashuB`). Safety proof: a token encoded by the **real 2.5.3 library** decodes under 3.7.1 with all mint/unit/memo/id/amount/secret/C preserved AND **re-encodes byte-identically**; on-disk `memory/wallet/*.json` survives a JSON round-trip unchanged — no re-mint, conversion, deletion, or network mutation. New offline `agent/test/cashu-migration.test.js` (8 cases) pins every used Cashu boundary incl. malformed/truncated tokens failing closed without leaking secrets; agent `node --test` now **38/38**. `npm audit --omit=dev` and full audit both **0 vulnerabilities**; real `node index.mjs` boot on Node 20.20.1 clean with no new warnings and Fastify 5.10.0 intact; root build + ops regressions (13/13, 9/9) green; prod tree shrank 71→68 packages. **Runtime gate — RESOLVED in v0.2.29-alpha (AGENT-SEC-CASHU-LTS-RUNTIME):** cashu-ts 3.7.1 declares `engines.node >=22.4.0` across the whole v3-lts line. This is now treated as a **HARD deployment prerequisite for the money path, not an advisory** — an `EBADENGINE` warning during `npm ci` is not an acceptable production state for a wallet. v0.2.29 raised `agent/package.json` (+ agent lockfile) `engines.node` to `>=22.4.0` (root frontend `package.json` deliberately left engine-free — static vite/vitest, no agent runtime), rewrote the `ops/install-agent.sh` preflight to gate on a robust major.minor.patch check (`ops/lib/node-version.sh::node_version_ok`, rejecting 22.0.x–22.3.x, erroring with the floor and stopping before touching anything), added `ops/test/installer-preflight.test.sh` (18 host-independent boundary assertions), fixed the stale `wallet.mjs` comment, and made `ops/README` state Node 22 LTS is a hard prerequisite. Verified under a real **Node 22.11.0** runtime (sandbox default is Node 20.20.1): clean `npm ci` with no EBADENGINE, 0 vulns, agent 38/38, ops 18/9/13, root build + vitest green, and a `node index.mjs` boot → `/api/health` 200 (v0.2.29-alpha) with no warnings. **Deploy prerequisite:** upgrade the VPS to Node 22 LTS before re-running `ops/install-agent.sh`; it will refuse to run on Node 20. **NOT yet deployed** — v0.2.26 remains the live server; v0.2.27 remains the newest shipped code. Prior slice: **v0.2.27 shipped AGENT-SEC: production dependency remediation.** `npm audit --omit=dev` in `agent/` reported 5 HIGH advisories in the Fastify tree during the live v0.2.26 deploy — all rooted in `fast-uri` (path-traversal GHSA-q3j6-qgpj-74h6 + host-confusion GHSA-v39h-62p7-jpjc) plus a Fastify content-type body-validation-bypass (GHSA-jx2c-rxcm-jvmq, no v4 backport). Cleared by upgrading `fastify ^4.28.1 → ^5.10.0`, `@fastify/cors ^9 → ^11`, `@fastify/rate-limit ^9 → ^11` and regenerating the lockfile; `fast-uri` now resolves to patched 3.1.3/4.1.0. No app code change beyond dropping the explicit `disableRequestLogging: false` from the Fastify constructor — passing the top-level option at all (even the default `false`) trips Fastify v5's `FSTDEP023` deprecation (guard is `!== undefined`, verified in installed `fastify@5.10.0` source + under `--trace-deprecation`; warning says removed in `fastify@6`); the drop is a no-op that silences the warning, request logging still emits. `@cashu/cashu-ts` stays at 2.5.3 (**no published npm/GHSA advisory**, so it does not block this hotfix — but the deprecation points to the `v3-lts` "security-fixes-only" line, so a v3-lts/v4 migration is a **security-relevant money-path follow-up to prioritise**, deferred as a separate slice to avoid behaviour drift in money-handling code — not cosmetic). Post-fix `npm audit --omit=dev` = **0 vulnerabilities**. Added `agent/test/wallet.test.js` (offline wallet guard/failure paths + cashu-ts codec regression) and `agent/test/fastify-v5-api.test.js` (cors preflight + rate-limit route-config contract); agent `node --test` now 30/30, rate-limit smoke green, root build green, ops regression 13+9 green. **NOT yet deployed** — v0.2.26 remains the live server version until an operator re-runs `install-agent.sh`. Base-path awareness + Ollama fallback landed in v0.2.6. Docs hygiene sweep across v0.2.7 → v0.2.12. **v0.2.13 shipped CONT-HEALTH-1**: dashboard now has a live Provider card polling `/api/health/models` every 20s. **v0.2.14 shipped SUITE-VPS-READY-1 rate-limit slice** — the public auth surface is behind `@fastify/rate-limit@9` per-IP with 429 + `Retry-After`; the in-memory challenges Map is capped and LRU-by-expiry evicted with structured `[auth]` logs, all prefix-only. **v0.2.26 shipped SUITE-VPS-READY-2: agent deploy tooling + first-touch admin claim.** New `ops/` assets let an operator run the agent as a hardened standalone systemd service: `ops/systemd/torii-continuum-agent.service` (locked `continuum` user, `ProtectSystem=strict`, only `memory/` + `config.yaml` writable), `ops/nginx/torii-api.conf` (same-origin `/api/` proxy + edge `limit_req`), `ops/install-agent.sh` (idempotent strict-bash installer: locked user, `rsync` without clobbering state, `npm ci --omit=dev`, one-time config gen with a fresh `openssl` session_secret, config validation, nginx `-t`-before-reload wiring, bounded `/api/health` proof), and a refreshed `ops/README.md` runbook. `agent/core/auth.mjs` gained first-touch admin bootstrap: with an empty `admin_npub` the first valid NIP-07 verifier atomically claims admin (npub persisted to `config.yaml` 0600 via injected `persistAdminNpub` in `config.mjs`), race-safe and fail-closed; `/api/health` now reports `admin_claimed`. **The agent is NOT deployed by this work** — running `install-agent.sh` on a server is a separate operator step. **CONT-HEALTH-2 (real Cashu wallet health probe) shipped in v0.2.47-alpha** (see the Active focus above): a non-mutating `wallet.health()` + admin-gated `GET /api/wallet/health` + a Provider-card row, verifying mint identity/NUT-07 support and reporting balance without touching proofs or wallet state.

**Active focus:** **v0.2.55-alpha / agent v0.2.50-alpha ships FIXUP-1: pre-PR reviewer remediation on the `feature/continuum-extra-jobs-batch` branch (3 fixes, frontend + agent).** **FIX A (BLOCKING, TEAMS-1 follow-up):** the operator-npub input rejected real `npub1…` Bech32 keys because TEAMS-1 validated 64-hex only. New **dependency-free** Bech32 codec `src/lib/npub.js` (`parseNpub`/`toNpub`/`shortNpub`; standard BIP-173 polymod + checksum, generator `[0x3b6a57b2,…]`, final XOR 1 — the root frontend stays vite+vitest-only, NO new runtime dep): `parseNpub(input)` accepts a checksum-verified `npub1…` OR a raw 64-hex key and returns `{ok,hex}` canonical lowercase hex (or `{ok:false,reason}`). `src/data/store.js addMember` now validates+normalizes via `parseNpub` and stores the **canonical hex**, so dedupe holds across equivalent `npub1…`/hex forms; `src/views/team.js` imports the shared `shortNpub` (removed the local dup), accepts either form (placeholder + maxlength 70), and displays members via `shortNpub`. Tests: new `src/lib/npub.test.js` (12; canonical NIP-19 vector `npub180cvv…`↔`3bf0c63f…`, hex accept, case/trim canonicalization, uppercase npub, empty/short/gibberish/63-hex/broken-checksum rejects, round-trip) + `src/data/members.test.js` +3 (npub1 accepted→canonical hex, cross-form dedupe throws, bad-checksum reject). **FIX B (NWC-ERR-1 hardening):** the app-level `setErrorHandler` in `agent/index.mjs` logged the full `err` (stack/secret-bearing). Now logs SCRUBBED only — `{url, code, name, msg: scrub(err.message)}`, never raw `err`/stack. New tiny module `agent/lib/scrub.mjs` (`scrub(s)`) redacts ≥16-hex runs (→`[redacted]`, covers 64-hex secrets/pubkeys) + `nostr+?walletconnect://…` URIs (→`[nwc-uri-redacted]`) then truncates to 200 chars — extracted to its own module so it's unit-testable without booting Fastify (index.mjs `listen()`s at import). Kept local (not imported from onboarding.mjs's `sanitizeReason`) so the boundary stays clean; <500 pass-through + sanitized 5xx `{ok:false,error:'internal error'}` behaviour unchanged. Test: new `agent/test/scrub.test.js` (6; 64-hex redacted, `nostr+walletconnect://`+`nostrwalletconnect://` redacted incl. embedded secret gone, 200-char truncation, empty/non-string fallback, ordinary message intact). **FIX C (KANBAN-PROG-1 limitation):** `src/views/dashboard.js` per-project progress gains a muted italic caption ("Counts local board cards; imported read-only cards not included.") via `h()` textContent — imported read-only source-sync cards (board.js `importState`) aren't persisted so `boardStatsFor` can't see them; folding them in is deferred as **KANBAN-PROG-2** (new not-started TODO). **Tests:** root `vitest run` **913/913** (was 898, +15: npub 12 + members +3); agent `node --test` **178/178** (+6 scrub); `npm run build` clean (version stamp 0.2.55-alpha). Constraints honored: no new runtime deps (Bech32 hand-rolled), textContent-only rendering for npub/label, scrub never logs a 64-hex secret or NWC URI, no device names/hostnames. Code + local commit only — **not pushed, not a PR**. Prior slice: **v0.2.54-alpha ships CARD-AGENT-1: a Kanban card → agent "vibe code" action (PREFILL-ONLY, consent boundary preserved)** (frontend-only; **no agent code changed, no agent version bump**, onboarding preview stays **v0.1.20-preview**). Part of the `feature/continuum-extra-jobs-batch` branch (batched with TEAMS-1 v0.2.53 + CHAT-CONTEXT-1 v0.2.52 + KANBAN-PROG-1 v0.2.51 + LAYOUT-1 v0.2.50 + NWC-ERR-1 v0.2.49). Operator ask (job #1 tail): *"The team can get tasks and vibe code their responses."* **What it does:** each Kanban card gains one new action button (glyph `✦`, aria-label "Ask Continuum to work on this task") in the existing `.card-moves` row (alongside ✎ ‹ ↑ ↓ ›). Clicking it drafts a bounded task prompt from the card and **prefills it into the chat dock — it does NOT send it.** **CRITICAL consent boundary:** the agent spends sats per request, so the operator must explicitly hit Send; a consent gate on `/api/chat` itself is still a flagged follow-up. **New chat export (`src/chat.js`):** `compose(text)` sets `mode='page'`, re-derives the active thread via `syncActiveThread()` (so the turn lands in the project's board thread `project:<slug>`), sets `inputEl.value=text`, `autosize()`, `setExpanded(true)`, `renderContext()`, and focuses the input — it never calls `send()` and is a no-op if the dock isn't mounted. **Card action (`src/views/board.js`):** `renderCard`'s new iconBtn calls `askContinuum(slug, card, columns)`, which derives `projectName` from `getProject(slug).content.name` (+ `(<slug>)`) and `columnName` from the card's column event, defensively re-asserts `setChatContext({ label:'<name> · Board', where:'project-board:'+slug })`, then calls `compose(prompt)`. The button is always enabled (prefill works in mock mode too). **Prompt builder (`src/views/card-prompt.js`, new, pure/DOM-free):** `buildCardPrompt(card, projectName, columnName)` returns `Work on this task.\nTitle: …` + optional `Details: …` (omitted when the description is empty) + `Project: …` + `Status: …`, trimming each field and capping the total at ~600 chars — housed in its own module so vitest imports it without a DOM. **Card text is still rendered only via `h()`/`iconBtn` textContent** (never innerHTML). **Explicitly deferred to CARD-AGENT-2** (recorded as a new not-started TODO): the agent-side "task → pending draft" skill that turns a vibe-coded response into a reviewable draft under `agent/pending/` for a human to approve + sign/publish — **not in this slice; no `agent/` code changed.** **Tests:** new `src/views/card-prompt.test.js` (**6** cases: full title+description+project+column shape; Details omitted on empty description; Details omitted when description missing; each field trimmed; ~600-char cap on long fields; accepts a bare content object). Root `vitest run` **898/898** (was 892, +6). `npm run build` clean (version stamp 0.2.54-alpha). Code + local commit only — **not pushed, not a PR** (the batch PR is managed separately). Prior slice: **v0.2.53-alpha ships TEAMS-1: operator roster + Kanban card attribution (LOCAL-FIRST FOUNDATION)** (frontend-only; **no agent code changed, no agent version bump**, onboarding preview stays **v0.1.20-preview**). Part of the `feature/continuum-extra-jobs-batch` branch (batched with CHAT-CONTEXT-1 v0.2.52 + KANBAN-PROG-1 v0.2.51 + LAYOUT-1 v0.2.50 + NWC-ERR-1 v0.2.49). Operator ask (job #1): *"Lets open up teams… the admin can invite other npubs to an editors level… maybe we should call them operators… the team can all add their own notes and tasks… and these can be entered on the kanban and moved around."* **Scope decision:** this is a LOCAL-FIRST FOUNDATION slice — the roster lives entirely in the local store and carries **NO authorization weight**; the agent's `requireAdmin` is untouched. **Data model:** new addressable kind `KIND.TEAM_MEMBER = 30093` (`d = member:<npub>`); `emptyState()` gains a **global** `members: []` (workspace-wide, NOT per-project → `deleteProject` does not cascade to it) with defensive `initStore()` coercion. Three exported store helpers mirror `addTodo`/`addCard`: `listMembers()` (sorted by `addedAt`), `addMember({npub,label})` (validates a 64-hex npub, lowercases + trims it, rejects duplicates, clamps label to 40 via the private `cleanText`, role `'operator'`, `addedBy 'admin'`, `addedAt` set), and `removeMember(npub)` (case-insensitive, no-op on unknown). **Team view (`src/views/team.js`, new):** a "Team" page (page-header eyebrow "Workspace" + title "Team" + sub "Operators you've added to this Continuum.") with an **"Add operator npub"** form (npub + optional label; inline error surfacing) and a roster list — each row shows a truncated mono npub, label, role pill, added-time, and a Remove button — rendered exclusively via `h()`/`textContent`, re-rendered in place through a `subscribe()` handler torn down on `hashchange`. Wired as route `/team` in `main.js` + a "Team" nav item (new `iconTeam` SVG) after Dashboard in `shell.js` NAV_ITEMS + `getActiveNav()` case. `setChatContext({ label:'Team', where:'team' })` on mount. **Card attribution (`src/views/board.js`):** `openCardEditor`'s free-text assignee `<input>` is replaced by `buildAssigneeSelect()` — a `<select>` of "Unassigned" + each operator's display string (label else short npub), using the existing `assignee` field unchanged (no card data-shape change); an empty roster degrades to just "Unassigned", and a card whose current assignee predates the roster keeps its value as a selectable "(not on roster)" option so editing never drops it. **Explicitly deferred to TEAMS-2** (recorded as a new not-started TODO): agent-side operator authorization (`requireOperator`, operator allow-list in config), relay multi-user sync, and NIP-17 invite/accept + an audit trail — this slice grants no real multi-user editing rights. **Tests:** new `src/data/members.test.js` (**17** cases: npub validate/lowercase/trim, empty/short/non-hex reject, duplicate reject case-insensitively, label clamp to 40, default empty label, `listMembers` sort-by-addedAt + returns-a-copy, `removeMember` by-npub/case-insensitive/unknown-noop, persistence across reload, legacy-state coercion of a missing `members` array, and `deleteProject` does-not-cascade). Root `vitest run` **892/892** (was 875, +17). `npm run build` clean (version stamp 0.2.53-alpha). Code + local commit only — **not pushed, not a PR** (the batch PR is managed separately). Prior slice: **v0.2.52-alpha ships CHAT-CONTEXT-1: project-scoped + page-aware chat threads with a page/general mode toggle** (frontend-only; **no agent code changed, no agent version bump**, onboarding preview stays **v0.1.20-preview**). Part of the `feature/continuum-extra-jobs-batch` branch (batched with KANBAN-PROG-1 v0.2.51 + LAYOUT-1 v0.2.50 + NWC-ERR-1 v0.2.49). Operator asks: job #5 *"the chat should be page aware… a side conversation like, whilst you're doing that job on continuum, can you find me the best things to do in Costa Rica"* + job #6 *"the chat in the Projects area however are specific to that project… these apps are all interoperable."* **Threading model:** the single global message array is replaced by a per-thread map keyed by context+mode via a new pure DOM-free module `src/chat-threads.js` (`threadKeyFor(ctx, mode)`): general mode → one shared `'general'` thread (side conversations); page mode inside a project → `'project:<slug>'` so ALL pages of one project (home, board, notes, files) share that project's conversation; page mode elsewhere → `'page:<route>'` (per-page, e.g. `page:/dashboard`). Slug is derived from an explicit router `projectSlug` else parsed from the view `where` string (`project:<slug>`/`project-board:<slug>`; the `projects` index has no colon so it stays a page thread). Switching page/mode swaps the visible history; each thread's history is kept in memory and greeted only when empty. **Mode toggle:** a small accessible `<button class="chat-mode">` next to the context chip flips "This page" (default) ↔ "General" (`aria-pressed`, dynamic `aria-label`, `.active` styling); the chip reads `context · general` in general mode. **Enriched context:** the object sent to the agent now carries `label`, `where`, `mode`, `route`, `pageType` (mapped from the router pattern via `pageTypeFor`), `projectSlug`, and best-effort `columnId`/`cardId` (null — not in the hash); built from `currentRoute()` + the view `where`. The agent/server is unchanged and ignores unknown fields; mock replies still work. **Persistence:** threads are persisted to `localStorage['continuum.chat.threads']`, each thread bounded to `THREAD_CAP=100` messages (oldest trimmed) via pure `trimThread`; load is defensive (`sanitizeThreads` drops malformed entries, try/catch → in-memory only on parse/quota failure). Thread-safety: `send()` pins the turn to `activeKey` before the await so an AI reply arriving after navigation still lands in the originating thread. User/AI text is still rendered **only via `textContent`** (never innerHTML). **True in-app agent editing is NOT in scope** — a follow-up TODO "agent-driven in-page edits" is recorded as future/not-started. **Tests:** new `src/chat-threads.test.js` (**19** cases: thread-key derivation for project board/home/dashboard/marketplace/projects-index/general/missing-route, slug parsing, page-type mapping, trim cap, sanitize drop/cap); root `vitest run` **875/875** (was 856, +19). Code + local commit only — **not pushed, not a PR** (the batch PR is managed separately). Prior slice: **v0.2.51-alpha ships KANBAN-PROG-1: dashboard progress now derives from real kanban board data** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Part of the `feature/continuum-extra-jobs-batch` branch (batched with LAYOUT-1 v0.2.50 + NWC-ERR-1 v0.2.49). Operator ask (job #2): *"Connect the progress to the data on the Kanban."* **Root cause:** the dashboard's overall + per-project progress were both derived from milestone status (`milestonesFor`), seed-ish data disconnected from the Kanban boards the operator actually manages. **Fix:** new exported pure helper `boardStatsFor(slug)` in `src/data/store.js` returns `{ total, backlog, todo, doing, done, percent }` — it `ensureBoard`s, counts `cardsFor` each column, and folds each column name into a status bucket via `bucketForColumnName()`, the INVERSE of `board.js`'s `columnForStatus()` sharing its regex vocabulary (done|complete|shipped → done; doing|progress|active|wip|review → doing; backlog|icebox|someday → backlog; todo|to do|to-do|inbox → todo; default → todo) so board placement and dashboard progress agree; `percent = done/total` rounded (0 when total 0), safe for a boardless/cardless project. `src/views/dashboard.js` now aggregates board stats across all projects for the "Overall progress" card (`done/total cards · doing`) and renders per-project total + a Done/Doing/Todo/Backlog breakdown line + a done/total progress bar in the "By project" section. The per-project section is re-rendered in place through a `subscribe()` handler (unsubscribed alongside the provider poll in `stopProviderPoll()` on leaving `#/dashboard`, and guarded against firing into a detached node), so adding/moving a card reflects immediately without navigation. Card titles are never rendered here (counts only) — no raw HTML, all `util.h()` textContent. **Tests:** `src/data/board.test.js` +3 (default Todo/Doing/Done buckets + 40% percent; empty board → all zeros/percent 0; custom names Review→doing / Icebox→backlog); root `vitest run` **856/856**. Code + local commit only — **not pushed, not a PR** (the batch PR is managed separately). Prior slice: **v0.2.50-alpha ships LAYOUT-1: constrain the chat dock to the main column + full-height sidebar** (frontend CSS-only; **no JS/shell/agent code changed**, onboarding preview stays **v0.1.20-preview**). Part of the `feature/continuum-extra-jobs-batch` branch (batched with NWC-ERR-1 v0.2.49-alpha). Operator ask (job #4): *"Make the chat bar at the bottom stay within the page content… and the left sidebar should extend to the bottom of the page 100%."* **Root cause:** `#app`'s grid areas were `"sidebar main" / "chat chat"`, so the chat dock spanned both columns and sat under the sidebar, and the sidebar stopped short of the bottom. **Fix (`src/styles/layout.css` + `src/styles/chat.css`):** changed the areas to `"sidebar main" / "sidebar chat"` so the sidebar spans both rows (full height to the very bottom) and the chat dock lives only under `main` (constrained to the main content width). Rows stay `1fr auto`; `main` keeps `overflow-y:auto` and takes the `1fr` remainder so there's no page overflow / double scrollbar even with chat expanded to `46vh`. Aligned the chat dock's side padding with the main column (`12px 16px → 12px 32px`; mobile drops to `16px` to match the narrower mobile main padding). The chat dock remains a direct child of `#app` (`mountChat(root)`), so `grid-area:chat` still applies — no shell restructuring; `.sidebar { overflow-y:auto }` + `.sidebar-footer { margin-top:auto }` still scroll internally and pin the footer to the bottom. Both themes unaffected. **Tests:** root `vitest run` **853/853** (CSS-only, no new tests). Code + local commit only — **not pushed, not a PR** (the batch PR is managed separately). Prior slice: **v0.2.49-alpha ships NWC-ERR-1: harden NWC onboarding Step 2 error paths** (agent-only; **no frontend/ops code changed**, onboarding preview stays **v0.1.20-preview**). Operator reported Step 2 (connecting an NWC wallet) surfaced a raw **"Internal Server Error"**. **Root cause:** `walletConnect()`/`walletTest()` in `agent/core/onboarding.mjs` called `client.getInfo()` inside `try { … } finally { close() }` with **no catch**. The live NIP-47 path can *throw* (relay unreachable, nip04 encrypt/decrypt, JSON parse) instead of returning `{ok:false}`; the uncaught exception propagated out of the thin Fastify route adapter, and with no app-level error handler Fastify answered a bare **500 "Internal Server Error"**. **Fix:** (1) wrapped `getInfo()` in both methods (and `payInvoice()` in `routstrPay()`) with `try/catch`, keeping `finally { close() }`; a throw now folds into the existing sanitized `502 { ok:false, error:'wallet did not respond to get_info' }` shape so the frontend treats it exactly like a non-throwing failure; a new `sanitizeReason()` helper logs a short, bounded reason with any long hex run (secret/pubkey) redacted — the URI/secret never enters a log line or response. The Routstr provider adapter already catches all network errors internally (returns `{ok:false}`), so no additional wrapping was needed there. (2) added an app-level `app.setErrorHandler` in `agent/index.mjs` as defense in depth — client errors (`statusCode < 500`, e.g. Fastify validation/empty-body 400s) pass through unchanged; any 5xx/unexpected throw is logged in full server-side and answered with a sanitized JSON `500 { ok:false, error:'internal error' }` (no stack, message, or secret). Structured onboarding `{code,body}`, auth 401/403, and rate-limit 429s never reach the handler (they use `reply.code().send()` / the rate-limit `errorResponseBuilder`), so those behaviours are preserved. **Tests:** extended `agent/test/onboarding.test.js` — throwing `getInfo` in `walletConnect()`/`walletTest()` each return 502 (not a bare 500) with `close()` still called; throwing `payInvoice` in `routstrPay()` returns 502 with `close()` still called; a secret-discipline test proves the URI/secret never appears in the response body or any log line even when the thrown error message embeds them. Full agent suite **172/172 pass** (Node 20.20.1 sandbox after `npm ci`). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.48-alpha ships OPS-DEPLOY-1: restart the promoted agent + verify the deployed version** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#49**. **Root cause it fixes (diagnosed from the live 0.2.43-vs-0.2.47 incident):** the `continuum-agent` systemd unit file is version-independent, so a code-only redeploy atomically promotes a new release tree WITHOUT changing the unit; the role's `restart continuum-agent` handler is never notified and the old `state: started` task is a no-op against the already-running PID. The swap changed the code on disk but the live process kept serving the PREVIOUS release (stale `/api/health` version, new `/api/wallet/health` + `/api/projects/:slug/sources` routes 404). The old health gate accepted any HTTP 200, so the broken deploy passed. **Fix (`ops/ansible/roles/continuum/tasks/main.yml`):** the unit-install task now `register:`s `continuum_unit_install`; after the JIT smoke test the role (1) runs `meta: flush_handlers` so any unit-file-change restart fires BEFORE readiness (handlers run in definition order — `reload systemd` daemon-reload then `restart continuum-agent`), (2) explicitly `state: restarted` **only when `continuum_promote.changed` AND NOT `continuum_unit_install.changed`** (the exact code-only case the handler misses; the guard prevents a double restart when the unit itself changed — that path is the handler's), then (3) an idempotent enabled+`state: started` ensure. Readiness is strengthened: `uri` now uses `return_content: true` and the `until` requires HTTP 200 AND `agent_health.json.version == (continuum_version | regex_replace('^v',''))`, so a stale process left running by a missed restart reports the OLD version and FAILS readiness, tripping the existing block/rescue rollback instead of passing a broken deploy. Exactly one restart happens per real deploy; the health gate always inspects the post-restart process. Everything else is preserved verbatim: block/rescue rollback (quarantine restore + webroot rollback + standalone re-enable), fail-closed backup, config/state verbatim copy (`session_secret`/funded key never rotated), adoption modes, V8 JIT smoke test, standalone-unit disabling, and `no_log` secret hygiene. The unit template stays version-independent (that is WHY the explicit restart is needed) — a regression test locks that assumption. **Tests:** new `ops/test/deploy-restart.test.sh` (**25** hermetic assertions): restart-before-readiness wiring (register, flush_handlers, guarded `state: restarted`, idempotent ensure), the version-asserting health gate, strict ordering (flush < restart < ensure < health) + handler daemon-reload-before-restart ordering, the version-independent-unit lock, rescue/rollback/webroot/standalone/JIT/config-copy survival, `no_log` retention + no secret in any printed `msg`, and pure version-equality cases proving a **stale 0.2.43-alpha live version FAILS** readiness while the **correct 0.2.48-alpha PASSES** (leading `v` stripped, empty version fails). **Full verification (Node 20.20.1 sandbox; agent code unchanged):** ops shell `deploy-restart` **25/25**, `continuum-adopt` **219/219**, `installer-preflight` **18/18**, `installer-shared-parent` **10/10**, `installer-signal` **9/9**, `nginx-install` **13/13**; root `vitest run` **853/853**; agent `node --test` **168/168**; `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz, version stamps 0.2.48-alpha); `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows the same **5 dev-only** findings (`@vitest/mocker`/`esbuild`/`vite`/`vite-node`/`vitest` toolchain, not shipped in `dist/`; breaking major-vite fix deferred). Lockfile top-level `version` fields left as-is (prior releases never bumped them; `npm ci` validates the dependency tree, not that field — build re-ran clean). No hostnames/devices/secrets in code, tests, or docs. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.47-alpha ships KANBAN-SYNC + wallet health** (agent + frontend; onboarding preview stays **v0.1.20-preview**; both package.json + lockfiles bumped `0.2.46 → 0.2.47`). Closes GitHub issues **#44/#45/#46/#47**. **#46 (read-only project-source adapters, agent):** new `agent/core/project-sources.mjs` + `agent/lib/markdown-todo.mjs` import from local Markdown todo files and public GitHub issues, both **strictly allowlisted, fail-closed**: local paths only under a configured `local_root` with traversal + symlink-escape rejected and file size bounded; GitHub only for `allow_github` `owner/repo` pairs, no credential needed, base URL pinned (`_ghPin`, `redirect:'error'`) as an SSRF guard, with bounded response bytes / `max_issues` (200) / `max_pages` / timeout / rate-limit detection. Records normalize to a stable-fingerprint shape (`{fingerprint, project, source{type,ref,label,id}, title, description, status, priority, labels[], url, readOnly:true, createdAt, updatedAt}`), `dedupeRecords` collapses repeats, and the last valid snapshot is cached per project + **retained on partial/offline/error**. Nothing logs content/tokens/full identifiers. New `project_sources:` config block validated in `loadConfig` (enabled-but-unallowlisted / markdown-without-`local_root` / malformed `allow_github` / unknown type all refuse boot); new admin-gated `GET /api/projects/:slug/sources` + rate-limited `POST /api/projects/:slug/sources/refresh`. **#45 (imported cards on the Kanban, frontend):** `src/views/board.js` keeps imported records in an **ephemeral per-slug `importState` Map, never written to the board store**, so manual cards are never overwritten and repeated refreshes never duplicate (keyed by fingerprint). Each renders as a `board-card imported` container — not draggable, no move/edit controls, source badge + priority/label pills, read-only `imported-badge`, `Read-only imported card:` aria-label, safe external link (`target:'_blank'`, `rel:'noopener noreferrer'`); `renderSyncBar` adds source + status filters, a manual Refresh button, a last-sync line, and stale/partial/error `sync-banner`s. New client fns `walletHealth`/`projectSources`/`refreshProjectSources` in `src/data/agent.js`. Mounted `/continuum/` base path, accessibility, mobile, dark styling, and login/dashboard/about behavior all preserved. **#47 (real Cashu wallet health, agent + frontend):** admin-gated `GET /api/wallet/health` backed by a **non-mutating** `wallet.health()` using cashu-ts (^3.7.1) v3 read primitives (`loadMint()` + `getMintInfo()` identity getters + `.isSupported(7)` NUT-07 + `groupProofsByState`) — no mint/melt/swap, serialized on-disk wallet untouched; honest disabled/healthy/degraded/unreachable states + sanitized reasons; never exposes tokens/proofs/secrets; `src/views/dashboard.js` adds a wallet-health row to the Provider card (polled alongside `healthModels()` via `Promise.all`). **#44 (docs):** todo/progress/handoff/README refreshed through v0.2.46 (stale "next-action" claims removed) then this release recorded with evidence; examples generic/configurable, no real hostnames/secrets, no cross-project queue contamination. Tests: new agent `project-sources.test.js` **29** / `markdown-todo.test.js` **12** / `project-sources-config.test.js` **9** (subprocess) / `wallet-health.test.js` **14**; extended `src/data/agent.test.js` **23** + `src/views/board-structure.test.js` **14**. **Root `vitest run` 853/853, agent `node --test` 168/168**, `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz), `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows **5 dev-only** findings (esbuild/vite/vitest toolchain, not shipped in `dist/`; fix is a breaking major-vite bump, deferred). Migration is additive/backward-compatible — `project_sources` defaults disabled; wallet probe reads existing state without mutation. Code + PR only — **not merged, tagged, or deployed.** **PR #48 review remediation (same branch, version held at v0.2.47-alpha):** a blocking review was addressed in place — **M1** partial-refresh data loss fixed (`refresh()` retains failed sources' last-valid records per-source via a `bySource` Map, replaces only healthy ones, preserves the full snapshot + `syncedAt` on total failure, honest `partial`/`stale`/`retained`); **M2** dedupe title-collapse scoped to Markdown-only so distinct/cross-repo same-title GitHub issues survive (native `fingerprint` wins); **M3** `mintHealth` extracted+exported and covered with a mocked cashu-ts v3 wallet (healthy/degraded/unreachable/timeout/no-NUT-07/spent-pending/no-mutation/no-leak); plus **L4** `GET /api/wallet/health` rate-limited (`wallet_health_per_min`, default 6) + 15 s server cache, **L5** `insertionIndex` ignores imported cards, **L6** absolute `local_root` enforced, **L7** numeric bounds hard-ceilinged (`CEILINGS`), **L8** dead `cache_ttl_sec` removed, **L9** `ok: anyOk` simplified, **L10** empty sync bar suppressed, **N11/N12** doc wording corrected (read-only identity/NUT-07 probe, source/mocked-unit guards not live-browser tests). Re-verified: agent 168/168, root 853/853, build clean, audits unchanged. No ops files touched. Prior slice: **v0.2.46-alpha shipped LOGIN-FIX: sidebar/demo Login "Could not reach agent: Bad Request"** (frontend-only; **no agent code changed**, agent stays **v0.2.43-alpha**, onboarding preview stays **v0.1.20-preview**). In the demo shell (`#/projects`), clicking the bottom-left sidebar **Login** with Plebeian Signer installed failed with *"Could not reach agent: Bad Request"*. **Root cause:** the SPA↔agent client `req()` in `src/data/agent.js` always sent `Content-Type: application/json`, even for the **bodyless** `POST /api/auth/challenge`; Fastify v5 rejects an empty body carrying that content-type with **400 `FST_ERR_CTP_EMPTY_JSON_BODY`** (`error:"Bad Request"`) before the handler runs — so the challenge never issued. The onboarding client's `postJson` was already hardened against exactly this (v0.2.32-alpha, pinned in `agent/test/fastify-v5-api.test.js`); the SPA client had **drifted** and never got the fix. **Fix:** `req()` now declares the JSON content-type **only when a body is actually sent** (mirrors onboarding `postJson`; challenge POST now 200s). Added `errorReason(json,status)` (pure, exported) to surface the specific Fastify `message` over the generic label (200-char cap, never stack traces). Both login entry points (`src/views/login.js` view + `src/shell.js` sidebar button) already share the single `startLogin()` in `src/auth.js` — no duplicate flow to merge; the drift was in the shared HTTP client. Privacy/keys unchanged (nsec stays in the extension; agent still verifies id+sig+admin-pubkey+challenge). Tests: new regression cases in `src/data/agent.test.js` (mock NIP-07 signer + fetch; assert challenge POST has no content-type/no body, verify POST sends exact `{event}` + JSON content-type + adopts token, full `errorReason` map). `vitest run` **843/843**, agent `node --test` **104/104**, ops shell suites green, `npm run build` clean. Root version → **v0.2.46-alpha**; agent NOT bumped (its empty-body 400 is correct standard Fastify behaviour — no protocol change). **Kanban discoverability (reported):** board is at `#/projects/:slug/board`, reached via the **Overview · Board** switcher after opening a project; all project routes are demo-browsable (only `/dashboard` is auth-gated), so the Board link **is** visible in demo mode — just not surfaced on the `#/projects` index cards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.44-alpha ships APP-ROUTING: application-first root (login + dashboard), sales page isolated to `#/about`** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Fulfils the operator ask *"separate out that sales page from the root of continuum and replace it with a login page and the dashboard."* The root (`/continuum/`) is now the application, never the marketing page. A pure, DOM-free guard layer (`src/nav-guard.js`: `rootTarget`/`guardRedirect`/`isProtectedPattern`, `PROTECTED_PATTERNS=['/dashboard']`) decides routing: **logged in → root redirects to `#/dashboard`; logged out → a branded Amber login page (`src/views/login.js`) renders in place at root** (shared `toriiSvg` mark, full-bleed `landing-mode`, drives the existing NIP-07/Plebeian-Signer `startLogin()` — no password auth, no key material in the DOM; small non-primary links to `#/about` + the demo shell). The former marketing view is reframed/exported as `renderAbout` in `src/views/landing.js` and lives only at **`#/about`** — never the root, never an onboarding-completion target. `#/dashboard` is guarded: a logged-out visitor (incl. refresh / deep link, since guards run on every router resolve including the initial one) is bounced to login at root; because root renders login **in place** (never itself bouncing when logged out), `protected → root → login` is terminal — **no redirect loop**. A session drop while on a protected view (sign-out / 401 expiry) reactively returns to login via the `continuum:session-changed` listener. `adoptOnboardingSession()` still runs before first render, so a freshly onboarded operator with a live `torii.session` lands authenticated on the dashboard (no login bounce); demo views (`#/projects`/`#/marketplace`/`#/routstr`) + all Amber nav preserved. No Google/gstatic/Fontshare/CDN/analytics added — local/system assets + inline SVG only. New `test/app-routing-auth.test.js` (**20** tests): unauth-root→login, auth-root→dashboard, dashboard protection, no-loop, `#/about`/demo isolation, onboarding adoption→authenticated root, expired/invalid→login, plus source-structure locks on `main.js`/views. Browser QA: no headed browser / no jsdom in the sandbox, so the contract is proven by the exhaustive pure guard tests + the source locks + the rebuilding no-external-request scan (guards run on the initial resolve = refresh/deep-link). Root `package.json` → **v0.2.44-alpha**. **Ops addendum in the same PR (v0.2.44-alpha):** a live v0.2.43 rerun hit one more exact bug — `group_vars/all.yml.example` shipped `torii_root_app: launcher`, but the installed CLI/API accepts `torii set-root <registered-name|none>` and returned **404 `{error: app_not_installed, name: launcher}`** (the Torii Suite launcher at `/` is the sentinel `none`, not a registered app), rolling the deploy back; rollback worked and the operator reran with `-e torii_root_app=none`. Fix: the example default is now **`torii_root_app: "none"`**, and the continuum role no longer calls `set-root` raw — it routes through **`ops/lib/continuum-adopt.sh set-root-safe`**, which `normalize_root_app`-maps legacy/empty/`launcher` (+ `torii`/`base`/`root`/`home`/`homepage`/`default`) → `none`, sources the admin token from `/opt/torii/env` silently (never printed), **skips** the call when `torii get-root` already matches, and returns 0 for the default so a normal install can never trip the rescue rollback. An explicit **registered** app name still passes through unchanged; pointing the root at `continuum` emits a discouraged-choice warning but is honoured when set deliberately — **Continuum is never made the root by its own installer.** `ops/README.md` gains a "Root-app selector correction (v0.2.44-alpha)" subsection; `continuum-adopt.test.sh` grew **188 → 219** (mock `torii` CLI reproducing the exact live 404; `launcher`→`none` normalization incl. synonyms/whitespace/case; default skip when already none + reset when not; explicit registered app honoured; continuum-warn; no-token-leak; example/role anti-drift greps). No-Google/no-CDN guarantees untouched. Tests: `vitest run` **795/795** (775 prior + 20), agent `node --test` **104/104**, `continuum-adopt.test.sh` **219** + ops regressions 18/10/9/13, root build clean (`dist/` free of external loading vectors, only the inert Plebeian-Signer store anchor remains), `npm audit --omit=dev` 0 vulns (root + agent). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.43-alpha ships OPS-HARDENING: encode three live-discovered cutover corrections** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). The v0.2.42-alpha cutover succeeded manually on the live box; three environment realities had to be fixed by hand and are now encoded permanently. **(1) Torii CLI `register` uses flags, not positionals** — the installed CLI is `torii register <name> [--display …] [--desc …] [--version …]`; the old positional form failed with `unknown flag: Continuum`. The role now calls `torii register {{ continuum_register_name }} --display "Continuum" --desc "App builder + agent" --version <bare-semver>` (bare version via `continuum_version | regex_replace('^v','')`), with new `continuum_register_*` defaults. **(2) `MemoryDenyWriteExecute` must be `no`** — Node 22's V8 JITs with W^X memory; `=yes` makes the kernel refuse the mprotect and V8 dies on startup (fatal `SetPermissions` / errno 12 / `SIGTRAP`). `continuum-agent.service.j2` now sets `MemoryDenyWriteExecute=no` with an inline rationale and keeps every other compatible directive (`NoNewPrivileges`, `Protect*`, `RestrictNamespaces`, `LockPersonality`, empty `CapabilityBoundingSet`). The role runs a **Node V8 JIT smoke test** under the rendered MDWE value via a transient `systemd-run` scope before starting the real service. **(3) nginx cannot traverse `/home/continuum`** (0750 home + `ProtectHome` → HTTP 500) — the build is now published to a **public webroot `/var/www/torii/continuum`** (root-owned, dirs 0755 / files 0644) via new `deploy_webroot` (atomic staged swap, prior webroot kept as timestamped backup); only the static bundle is copied out, source + encrypted state stay private under `/home/continuum/app`. A **single** `location /continuum/` prefix alias serves both the SPA entry and the hashed `assets/*` bundle (no regex-alias, no `/home`). The cutover `rescue` now also rolls the webroot back (`rollback_webroot`) + reloads nginx on failure. Re-running against the now-live v0.2.42 Ansible layout is idempotent (re-detects `existing-ansible`, preserves config, never rotates `session_secret`/touches the funded key). `ops/test/continuum-adopt.test.sh` grew **117 → 188** assertions (webroot deploy/rollback + perms + dangerous-target refusal; mock Torii CLI parser reproducing `unknown flag: Continuum` vs flag-form success; systemd MDWE=no contract + retained hardening; Node V8 WASM smoke; nginx section is now a **no-nested-assets regression lock**; existing-ansible upgrade idempotency). New `ops/README.md` v0.2.43 subsection + upgrade command. Root + agent version → v0.2.43-alpha (+ both lockfiles). Tests: adopt **188/188**, ops regressions green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. **AMENDMENT (pre-merge, same PR #40) — frontend zero-external-request + asset-404 fix:** (4) removed all production Google (`fonts.googleapis.com`/`fonts.gstatic.com`) **and** Fontshare (`api.fontshare.com`) preconnects/stylesheets from `index.html`; typography is now a **deliberate local system font stack** in `src/styles/theme.css`, so production makes **zero external requests** (no fonts downloaded/vendored). (5) the live black page was the nested `location /continuum/assets/` alias+`try_files` returning 404 for hashed `.js`/`.css`; both nginx templates now use a **single parent alias** only. (6) `ops/README.md` upgrade steps corrected to the real live paths (`/opt/deploy/torii-continuum-v0.2.42-alpha` checkout, app at `/home/continuum/app`; prefer a fresh version-specific public clone of the final tag) — the old `/opt/torii/src/torii-continuum` never existed. New frontend tests: `test/no-external-cdn.test.js` (builds `dist/`, asserts index + assets carry no banned CDN/font host and no external loading vector; same-origin `/continuum/api` allowed) and `test/nginx-continuum-routing.test.js` (HTTP server modelling nginx `alias`+`try_files`, using the **real hashed filenames** from `dist/index.html`, proves `/continuum/` index + hashed `.js`/`.css` = 200 and deep-route SPA fallback). `vitest run` now **775/775**. Browser QA: no headed browser in the sandbox — verified via the build + HTTP-200 routing + same-origin-only fetch scan; the app is a hash-router SPA (`#/`,`#/projects`,`#/marketplace`,`#/routstr`,`#/dashboard`) so the server contract is fully exercised, and the live box already rendered the Amber dashboard with zero console/network errors after the manual asset-route fix. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.42-alpha ships OPS-HARDENING: transactional adoption/cutover + partial-adoption recovery** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the live v0.2.41-alpha adoption failure: the role migrated live state INTO `/home/continuum/app/agent` and only THEN ran `ansible.builtin.git` into `/home/continuum/app` — cloning into a dir already populated with runtime state. It aborted, leaving the **original standalone layout intact** AND a **partial, non-git `/home/continuum/app`** holding a copy of the state; the untouched `torii-continuum-agent.service` was restored and health is good. **Fix (`ops/lib/continuum-adopt.sh` + `roles/continuum/tasks/main.yml`):** `layout_detect` gains a 3rd `app_dir` arg and a new **`partial-adoption`** mode (agent has state but `app/.git` absent while standalone is still present — a real git-backed Ansible install still wins; a non-git app dir carrying state is recovered, never built on top of). New `authoritative_state_dir` picks the untouched standalone as the live source over the partial copy. New `stage_reset`/`promote_release`/`rollback_release`: the `git checkout` + `npm ci` + `vite build` run in a **clean staging dir** (`/home/continuum/app.staging`) while the **old unit keeps serving**; authoritative state is copied in **only after a successful build**; the old unit is stopped only immediately before an **atomic same-filesystem swap** into `/home/continuum/app`, quarantining (never deleting) any pre-existing app dir to `app.quarantine-<UTC>`. The `block`/`rescue` cutover stops the new unit on any promotion/`/api/health` failure, rolls an existing-Ansible tree back from quarantine (or re-enables the standalone for adopt/partial), and prints backup + quarantine paths with an exact **secret-free** recovery command. Re-running after the exact observed failure is idempotent. `ops/test/continuum-adopt.test.sh` grew to **117** hermetic assertions (3-arg detect incl. partial-adoption + absent-app-dir; `authoritative_state_dir`; `stage_reset` refuses `/ /home /root /opt /opt/torii`; `promote_release` clean/quarantine/refuse-existing-quarantine/idempotent; `rollback_release` restore/refuse-without-failed-dir/no-quarantine; end-to-end partial-adoption recovery proving byte-for-byte config equality + funded-key retention + quarantine preservation + untouched standalone; v0.2.42 anti-drift greps that clone/build target staging, state is staged before promotion, promotion+rescue are gated on a swap fact, and the old unit stops only at cutover). New "Transactional cutover + partial-adoption recovery (v0.2.42-alpha)" subsection in `ops/README.md` with the exact recovery invocation for the current partial-adoption VPS (same vault-free localhost invocation, `continuum_version: v0.2.42-alpha`). Root + agent version → v0.2.42-alpha (+ both lockfiles). Tests this slice: adopt **117/117**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.41-alpha ships OPS-HARDENING: vault-free standalone adoption / redeploy** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes the blocker left by v0.2.40-alpha: on `adopt-standalone`/`existing-ansible` the live `config.yaml` is preserved byte-for-byte, but the preserve path still rendered a `.config.candidate.yaml` from `config.yaml.j2` (which references `{{ admin_npub }}`/`{{ session_secret }}`) for the drift diagnostic — so a truly vault-free run (no `group_vars/vault.yml`, no vault password) died with an undefined-variable error before finishing adoption. **Fix (in `roles/continuum/tasks/main.yml`):** a new `continuum_vault_vars_present` fact is computed defensively via the `default('')` filter (an undefined var yields `false`, never raises); the candidate render + `config-drift` + candidate removal + "differ" warning are all guarded behind `continuum_vault_vars_present | bool` and SKIP entirely when the vault vars are absent (a non-secret debug notes the diagnostic was skipped). Adoption still preserves the live config byte-for-byte, so the funded Routstr key stays decryptable. A fresh install or an explicit `continuum_allow_config_rotation=true` rotation still needs the vault vars, and their absence in those modes **fails closed** with a clear, non-secret `ansible.builtin.fail` *before* any state is written. `ops/test/continuum-adopt.test.sh` grew to **66** hermetic assertions (vault-free adopt end-to-end incl. byte-for-byte config preservation + funded-key migration + no-secret-in-output; existing-ansible redeploy; fresh missing-vars fail-closed; explicit-rotation missing-vars fail-closed; presence truth table; anti-drift greps for the new guards). New "Vault-free adoption / redeploy (v0.2.41-alpha)" subsection in `ops/README.md` gives the exact secret-free localhost invocation (inventory `torii→localhost ansible_connection: local`, `group_vars/all.yml` with `torii_domain`/`continuum_version`, `ansible-playbook -i inventory.yml site.yml --tags continuum` with NO `--ask-vault-pass`). Operator handles NO secrets and needs NO vault password for adoption. Tests this slice: adopt suite **66/66**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.40-alpha ships OPS-HARDENING: safe standalone→Ansible adoption, no config clobber, fail-closed backups** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the dangerous deployment mismatch that blocked the live v0.2.39-alpha rollout: the continuum Ansible role cloned to `/home/continuum/app` and **re-rendered `config.yaml`/`session_secret` unconditionally on every run**, while the live standalone box uses `/opt/torii/continuum-agent` + unit `torii-continuum-agent.service` on port 8787 — so running the old role over that box would have overwritten the session_secret (orphaning the funded Routstr key, encrypted at rest under a key derived from it) and/or double-bound 8787. **Fix:** the risky detect/backup/migrate/config-decision logic is factored into a sourceable, unit-tested lib `ops/lib/continuum-adopt.sh`, and `roles/continuum/tasks/main.yml` is now a thin guarded orchestrator that (1) **detects** layout (`fresh`/`adopt-standalone`/`existing-ansible`; an existing Ansible layout always wins); (2) takes a **fail-closed** timestamped **root-only 0700 backup** of `config.yaml`+`memory/`+`ciphertexts/`+`pending/` from both layouts under `/root/continuum-backup-<UTC>/` before any mutation (no backup ⇒ abort); (3) on adopt **stops+disables** the standalone unit (frees 8787) and **migrates config+state verbatim** — `session_secret` copied byte-for-byte, never regenerated, migration idempotent; (4) **never clobbers** an existing config on routine deploy — renders from vault ONLY on a fresh install or explicit `-e continuum_allow_config_rotation=true` (off by default), and on a preserve runs a one-way drift check that prints only `same`/`differ` (never a value) and surfaces a safe "rotation pending" notice; (5) wraps the service+nginx cutover in a `block`/`rescue` that on failure prints the backup path + exact recovery command. `git force:true` resets only tracked files, and all agent state is gitignored, so a re-checkout/build can't delete encrypted state. New `roles/continuum/defaults/main.yml` holds the safe tunables (`continuum_standalone_dir`, `continuum_standalone_service`, `continuum_backup_root`, `continuum_allow_config_rotation: false`); secret-touching tasks carry `no_log: true`. Pinned by `ops/test/continuum-adopt.test.sh` (**44** hermetic assertions: detection precedence, fail-closed backup, verbatim+idempotent migration, config-action matrix, no-secret drift, permissions, anti-drift greps). New "Adopting a standalone install with Ansible" runbook in `ops/README.md`. **Operator handles NO secrets** at any point; deploy is the normal `ansible-playbook … --tags continuum`. Tests this slice: adopt suite **44/44**; unchanged app/agent suites not re-run beyond the required build/audit. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.39-alpha ships ONBOARDING-GATING + APP-MOUNT: real claimed-state Step-3 gating, dup-pay race closed, completion lands on the actual app dashboard** (onboarding preview → **v0.1.20-preview**, `preview-assets/onboarding-v0.1.20/`). Two live defects on the v0.1.19/v0.2.38 stack plus the app-mount follow-through. **User-funded Routstr key, all encrypted agent memory, and prior payment state preserved — no invoice created/paid/re-submitted, no `memory/**` touched.** **(A) Claimed-state gating (root cause):** a verified funded key still rendered both setup cards, key-entry/reveal, *Verify & connect* and *Request an invoice* — a CSS cascade defect, not logic: author `display` rules (`.btn-primary{inline-flex}`, `.wallet-choices/.routstr-paths{flex}`, `.choice-card{grid}`) outrank the UA `[hidden]{display:none}`, so `el.hidden=true` visually no-op'd. Fix: canonical `[hidden]{display:none !important}` in `shared.css`; the status-only resume path funnels through `renderSuccessAdvance` via a single `showClaimed` helper (replaces `showConnected`); the panel carries `data-claimed="1"` as single source of truth, leaving one green summary + exactly one standalone Continue. **Dup-pay race closed:** `data-claimed` checked as the first line of quote/key-connect/pay-confirm handlers (pay button also permanently disabled once claimed) — a second invoice/payment is structurally impossible in claimed state. **(B) App mount:** `CONTINUUM_HOME` `/continuum/` → `/continuum/#/dashboard` (+ curtain anchor) so completion opens the multi-view Amber SPA dashboard, not the marketing root; open-redirect guard preserved. **Session handoff:** `adoptOnboardingSession()` (in `src/data/agent.js`, called once in `main.js` before `initStore()`) fails closed if the SPA already has a live session, else adopts the `localStorage['torii.session']` envelope token into `continuum.session.v1` only if `tokenLooksLive` (shape + unexpired), so a freshly onboarded user is not bounced to sales/login; `deriveSameOriginBase` makes `agentUrl()` fall back to same-origin `/continuum` under the mount (no CDN/third-party origin). **No separate torii-suite PR required** — the `/continuum/` alias + subpath refresh + `/continuum/api/*` proxy + SPA build + agent install + nginx fragment + launcher registration are all in this repo's `ops/ansible`; the root launcher is never overwritten. **Safe idempotent deploy (operator step, NOT run here, from `ops/ansible/`):** `ansible-playbook -i inventory.yml site.yml --ask-vault-pass --tags continuum`. **(C) Wording:** residual VPS/daemon/box copy in `routstr.js`/`landing.js`/`auth.js` scrubbed to "your Torii, your gateway". Tests: root vitest **762/762** (new `src/data/agent.test.js` 11 cases; preview v0.1.20 **153**), agent `node --test` **104/104**, `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Honest limitation: no jsdom/live agent here, so no live funded-agent click-through — gating proven by the CSS `!important` cascade guarantee + source/behaviour guards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.38-alpha ships ONBOARDING-FINISH: verified-state cleanup, correct balance units, inline key reveal, deterministic final curtain** (onboarding preview → **v0.1.19-preview**, `preview-assets/onboarding-v0.1.19/`). A focused live UX/completion pass from three screenshots; no character/animation/CDN/provider-pinning behaviour changed and no payment/NWC semantics changed beyond balance *display* and safe key *retrieval*; the funded key + prior state are preserved. **(A) Balance units (root cause):** Routstr denominates balances in MILLISATS but `extractBalanceSats` surfaced the raw `balance` verbatim, so a 10,000-sat key showed "10000000 sats"; the provider adapter now divides msat fields to whole sats (explicit `*_sats` trusted), a marker-guarded `displayBalanceSats` migration divides legacy stored envelopes (no `balance_units`) by 1000 on read while new envelopes carry `balance_units:'sat'`, and all balances render via `formatSats` (e.g. `10,000 sats`). **(B) Step 3 verified state:** once claimed/encrypted/verified, `collapseStep3Setup` hides both path cards, both forms, the invoice card, the progress bar and all three setup CTAs, leaving one green summary + a SINGLE Continue button outside it, no countdown/auto-advance (supersedes the v0.1.18 countdown), advance idempotent. **(C) Step 5 recovery kit:** removed the unbranded "Reveal full Routstr key (one-time)" button + its `window.confirm` flow (never revealed the key); the ROUTSTR KEY row now has branded inline eye + copy controls that fetch the key over the existing admin-authenticated no-store `export-key` endpoint, hold it in memory only (never localStorage/sessionStorage/URL/logs), and re-mask on toggle/step-leave/tab-hide/session-loss/timeout (`shouldMaskReveal`/`REVEAL_TIMEOUT_MS`); the Download click is itself the explicit confirmation and embeds the full key in the saved file while still excluding the NWC secret. **(D) Final curtain:** the old curtain only `console.log`'d a commented redirect and spun forever; `initCurtain` now navigates deterministically to the resolved same-origin `/continuum/` home (a `window.__toriiContinuumHome` override accepted only same-origin — open-redirect guard), reveals a real "Open Continuum now" fallback link on a bounded timer, honours `prefers-reduced-motion`, and drops the spinner shortly after the nav attempt so it can never hang. Tests: agent `node --test` **104/104**, root vitest **269/269** (preview v0.1.19 **145**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.37-alpha ships ONBOARDING-PAY-RECOVERY: paid-but-unclaimed Routstr recovery + payment progress state machine + working recovery kit** (onboarding preview → **v0.1.18-preview**, `preview-assets/onboarding-v0.1.18/`). Fixes a live incident where an operator connected NWC and paid 10,000 sats to fund a Routstr session, but the UI said "payment confirmed" without surfacing the `sk-` key, never auto-advanced, and the Recovery Kit "download" was a no-op that just stepped the deck. **No new invoice created or paid during diagnosis or fix.** Delivered a safe SSH recovery runbook first (inspect `memory/secrets/*.enc` by metadata only — never decrypt; reuse the operator's existing browser session token against the loopback admin API; claim the already-paid key with the **idempotent** `POST /api/onboarding/routstr/recover` sending `{}` — the agent re-submits the stored bolt11 and never re-pays; redacted status/verify; one-time no-store `export-key` only if a manual copy is required). Root fact: **payment-confirmed does NOT imply the key is stored** — the claim is a separate post-settlement step; recovery states are paid+claimed / paid+unclaimed (the live case) / unpaid-or-expired. **Agent (v0.2.37-alpha):** new admin-gated endpoints `GET /api/onboarding/recovery/state` (redacted resume snapshot; `claimable` true only when a pending invoice is stored with no key), `GET /api/onboarding/recovery-kit` (`Cache-Control: no-store`, secret-free by default), `POST /api/onboarding/routstr/export-key` (one-time full-key reveal; `confirm===true` required else 400, 409 when no key, `no-store`, rate-limited, persists `export_count`/`last_exported_at` audit, logs warn never the key). **Preview (v0.1.18):** pure `ONBOARD_PHASES`/`PHASE_META` state machine driving an accessible (role=status, aria-live) progress + scanning bar + elapsed timer across connect→quote→pay→claim→verify; **SUCCESS only when the agent reports `key_stored===true`** (`classifyPayResult`/`classifyRecoverResult` map bare paid/recoverable → retryable `PAID_UNCLAIMED`, never success); duplicate-payment prevention (Confirm button permanently disabled after first click); success renders the redacted key then auto-advances after a short countdown with an immediate Continue-now; refresh-resume reads `recovery/state` on load and finishes a `claimable` claim via empty-body `recover` (no re-payment); real Recovery Kit download via Blob (`buildRecoveryKit`) whose default excludes the NWC secret + full key and carries redacted previews + restoration instructions, with the full key included only via an explicit confirmed one-time `export-key` reveal; Step 5 markup no longer prints static secret-adjacent values. Preserved: character/animation behaviour, self-hosted Three.js (no CDN), provider pinning, redaction, rate limits, no autonomous spend, no nsec on server. Tests: agent `node --test` **102/102**, root vitest **453/453** (preview v0.1.18 **124**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.36-alpha ships ONBOARDING-ASSET: new Chiefmonkey GLB + forbidden-safe animation state machine** (onboarding preview → **v0.1.17-preview**, `preview-assets/onboarding-v0.1.17/`). A substantial asset + animation upgrade of the design-review onboarding mockup (NOT the production app). A fresh Chiefmonkey source GLB replaces the old `chiefmonkey6.glb` and is crushed for production by a new reproducible local optimizer `tools/optimize-glb.mjs` (gltf-transform 4.4.1 + draco3dgltf 1.5.7 + sharp 0.35.3, build-time only, no third-party runtime CDN): `dedup → weld → resample → textureCompress(webp,q82,≤1024²) → prune → draco(edgebreaker)`, dropping every forbidden locomotion/knock-down clip with the runtime's own `isForbiddenClip` predicate (single source of truth). Source 9,298,852 B (SHA `87b0048c…c37dd`) is kept OUT of git (build artifact only, see `tools/SOURCE.md`); the committed optimized `assets/chiefmonkey-onboarding.glb` is 2,347,780 B — **74.75% smaller** (target ≥60%), SHA `0253d5e1…e2fcb` — with a `.manifest.json` recording sizes/%reduction/deterministic SHA + retained/dropped clip inventory (13 retained, 5 forbidden dropped: `Clapping_Run`/`Knock_Down`/`Running`/`Stylish_Walk_inplace`/`Walking`; mesh/skin(24 joints)/material/texture preserved nonzero, clip names survive Draco). `character.js` now runs a dedicated forbidden-safe state machine — each of the 5 deck steps plays one dedicated clip from `STEP_CLIPS`/`selectStepClip` (1 Talk_with_Hands_Open, 2 Agree_Gesture, 3 mage_soell_cast_3, 4 Gentlemans_Bow, 5 Idle_10; dormant step-6 Victory_Cheer curtain), applied deterministically on entering/restoring via the v0.1.15 `resolveReadyStep` path (order-independent) — and a raycast hit-test (`Raycaster.intersectObject`, NDC from canvas rect) plays a random one-shot from a curated `CLICK_POOL` (`pickClickReaction`, no immediate repeat) then crossfades back to the current step clip on the mixer `finished` event, ignoring UI-control clicks / respecting `prefers-reduced-motion` / ignoring click-spam. Step-1/2/3 prompt/success/failure `onboarding:anim` overrides are transient and always return to the current step clip (no race can permanently overwrite step state). Deterministic offline tests parse the optimized GLB JSON chunk (counts nonzero, referenced clip names survive), assert no forbidden pattern is selectable, verify the committed asset matches manifest SHA + size budget (≤3 MB, ≥60%), and cover every step mapping / restore ordering / status→current-step return / click hit-no-hit + UI-guard + no-immediate-repeat + spam guard + crossfade return / reduced motion / final load-error. Verified target-file + full root vitest green and `npm run build` clean. Code + draft PR only — preview snapshot only, not deployed. Prior slices (onboarding preview arc): **v0.2.35-alpha / v0.1.16-preview** (PR #32) reworked Step 2 (wallet-agnostic NWC connect via NIP-47 `get_info` capability matrix, funding gated on `pay_invoice`) and Step 3 (Routstr existing-`sk-…`-key OR fund-a-new-session: quote → explicit confirm → NWC pay → claim minted key, with a RECOVERABLE poll-timeout carrying the bolt11 via `/lightning/recover`); secrets stored encrypted at rest (AES-256-GCM), admin-gated + rate-limited, pay refuses unless `confirm===true` AND `pay_invoice` advertised. **v0.2.34-alpha / v0.1.15-preview** (PR #31) added the deterministic character↔deck step-sync state machine (`createCharacterSync`/`recordStep`/`resolveReadyStep`/`markCharacterFailed`) that fixed the intermittent invisible-Chiefmonkey-after-soft-reload race by remembering steps before model readiness instead of dropping them then hard-applying Step 1. **v0.2.33-alpha / v0.1.14-preview** (PR #30) fixed refresh dropping Step 2 → Step 1 (`restoreSession`/`isSessionValid` fail-closed, `window.__toriiRestoredStep`) and Chiefmonkey invisible on reload (route both GLB stall AND error through `nextLoadAttempt`). Prior slice: **v0.2.32-alpha ships ONBOARDING-STEP1-FIX: "agent challenge failed (400)" on Sign with Plebeian Signer** (onboarding preview → **v0.1.13-preview**, `preview-assets/onboarding-v0.1.13/`). Client-only fix: `onboarding-client.js`'s `postJson` was sending `Content-Type: application/json` on the **bodyless** `POST /api/auth/challenge`, and Fastify v5 rejects an empty body carrying that header with `400 FST_ERR_CTP_EMPTY_JSON_BODY` — failing step 1 before the NIP-07 prompt (which only fires after a challenge is fetched), so Chiefmonkey animated but no signer opened. Fix: `postJson` only sets the JSON content-type + serialises a body when a body is actually present; the challenge call now goes out bodyless (→ 200), verify unchanged. No server endpoint added, no validation weakened, no bunker-connect endpoint; NIP-07 primary + browser-client NIP-46 secondary, `localStorage['torii.session']`, exact signer wording, and the three clips (`HandGesture_00`/`Idle_03`/`Confused_02`) all preserved. New offline vitest case (challenge POST has no body/no JSON content-type; verify still does) + new agent `fastify-v5-api` case pinning the empty-body-400 vs bodyless-200 contract. Prior slice: **v0.2.31-alpha shipped ONBOARDING-STEP1: live NIP-07/NIP-46 auth for onboarding step 1** (onboarding preview → **v0.1.12-preview**, `preview-assets/onboarding-v0.1.12/`). New self-contained `onboarding-client.js` (ES module, no build step, no third-party CDN) wires step 1 to the same-origin agent API. NIP-07 primary via `window.nostr` (button "Sign with Plebeian Signer"): `POST /api/auth/challenge` → sign the exact kind-22242 auth event `agent/core/auth.mjs` expects (`content == challenge`, `['challenge', …]` + `['relay', origin]` tags) → `POST /api/auth/verify` → persist to exactly `localStorage['torii.session']` as `{ token, expires_at, pubkey, method, created_at }` (token + public identity only, no secrets). Fails closed on malformed responses, expired challenges, pubkey/challenge mismatch, or absent token. NIP-46 secondary with the browser as the client (bunker46 architecture, "Use a different signer"): parses a pasted `bunker://` string and asks the remote signer to sign the same event over its relay — **no server bunker-connect endpoint**, no key/secret sent to the agent, no silent fallback. Chiefmonkey phases via the existing event channel: `HandGesture_00` prompting/signing, `Idle_03` success, `Confused_02` failure, each with an ordered fallback to an existing GLB clip (the GLB ships neither `HandGesture_00` nor `Confused_02`, so the fallback runs and never freezes). Desktop-only gate + all prior perf fixes preserved. 28 offline vitest cases; verified root vitest 28/28, root build, agent `node --test`, ops regressions, agent `npm audit --omit=dev`. Code + draft PR only. Prior slice: **v0.2.30-alpha shipped AGENT-SEC-OPT-TORII-PERMS: least-privilege fix for the shared `/opt/torii` parent.** The v0.2.29 deploy surfaced a production regression — `ops/install-agent.sh` created the shared parent with an unconditional `install -d -m 0750 -o continuum -g continuum /opt/torii`, and because `install -d` re-applies mode+owner on every run it clamped the parent that torii's *other* apps live under (torii-base launcher, quest tooling) to `0750 continuum:continuum`, stripping its world-execute bit. nginx (`www-data`) could then no longer traverse `/opt/torii` to reach `/opt/torii/launcher/index.html`, so `/` fell through to a default nginx 404 (Quest under `/var/www` stayed readable). Fix: create `/opt/torii` **only if absent**, `root:root 0755`, and **never re-own/re-mode an existing shared parent**; the agent's own subdir `/opt/torii/continuum-agent` stays locked `0750 continuum:continuum` (nginx never serves from it — the agent is loopback-proxied on 8787). New hermetic + anti-drift regression `ops/test/installer-shared-parent.test.sh` (10 assertions) pins that the installer no longer chowns the shared parent to the service user, guards creation with an existence check, and keeps `$INSTALL_DIR` locked. The previously-planned **onboarding** work moves to **v0.2.31-alpha**. Operator out-of-band unblock if needed before redeploy: `sudo chown root:root /opt/torii && sudo chmod 0755 /opt/torii` (restores o+x, leaves the agent subdir untouched). Prior slice: **v0.2.28-alpha shipped AGENT-SEC-CASHU-LTS: the money-path dependency migration flagged in v0.2.27.** `@cashu/cashu-ts` moved `2.5.3 → 3.7.1` (the maintained `v3-lts` "security-fixes-only" line; dist-tag `v3-lts` resolved to `3.7.1` at implementation time, latest was `4.7.0` — v4 deliberately not taken), pinned `^3.7.1`, lockfile regenerated with sha512 integrity and the deprecation notice cleared. Code adapted in `agent/core/wallet.mjs` only: class rename `CashuMint→Mint` / `CashuWallet→Wallet`; the boot warm-up `getMintInfo()` (async in v2, now a sync cached getter that throws pre-load in v3) replaced by `await wallet.loadMint()`; an idempotent `ensureLoaded()` guard added before `receive()`/`send()` to restore v2 lazy-load-on-demand (verified zero extra network once keysets are cached). Token/proof codec, `Token`/`Proof`/`SendResponse` shapes, and the `receive()→Proof[]` contract are unchanged between the versions (both default to token-v4 `cashuB`). Safety proof: a token encoded by the **real 2.5.3 library** decodes under 3.7.1 with all mint/unit/memo/id/amount/secret/C preserved AND **re-encodes byte-identically**; on-disk `memory/wallet/*.json` survives a JSON round-trip unchanged — no re-mint, conversion, deletion, or network mutation. New offline `agent/test/cashu-migration.test.js` (8 cases) pins every used Cashu boundary incl. malformed/truncated tokens failing closed without leaking secrets; agent `node --test` now **38/38**. `npm audit --omit=dev` and full audit both **0 vulnerabilities**; real `node index.mjs` boot on Node 20.20.1 clean with no new warnings and Fastify 5.10.0 intact; root build + ops regressions (13/13, 9/9) green; prod tree shrank 71→68 packages. **Runtime gate — RESOLVED in v0.2.29-alpha (AGENT-SEC-CASHU-LTS-RUNTIME):** cashu-ts 3.7.1 declares `engines.node >=22.4.0` across the whole v3-lts line. This is now treated as a **HARD deployment prerequisite for the money path, not an advisory** — an `EBADENGINE` warning during `npm ci` is not an acceptable production state for a wallet. v0.2.29 raised `agent/package.json` (+ agent lockfile) `engines.node` to `>=22.4.0` (root frontend `package.json` deliberately left engine-free — static vite/vitest, no agent runtime), rewrote the `ops/install-agent.sh` preflight to gate on a robust major.minor.patch check (`ops/lib/node-version.sh::node_version_ok`, rejecting 22.0.x–22.3.x, erroring with the floor and stopping before touching anything), added `ops/test/installer-preflight.test.sh` (18 host-independent boundary assertions), fixed the stale `wallet.mjs` comment, and made `ops/README` state Node 22 LTS is a hard prerequisite. Verified under a real **Node 22.11.0** runtime (sandbox default is Node 20.20.1): clean `npm ci` with no EBADENGINE, 0 vulns, agent 38/38, ops 18/9/13, root build + vitest green, and a `node index.mjs` boot → `/api/health` 200 (v0.2.29-alpha) with no warnings. **Deploy prerequisite:** upgrade the VPS to Node 22 LTS before re-running `ops/install-agent.sh`; it will refuse to run on Node 20. **NOT yet deployed** — v0.2.26 remains the live server; v0.2.27 remains the newest shipped code. Prior slice: **v0.2.27 shipped AGENT-SEC: production dependency remediation.** `npm audit --omit=dev` in `agent/` reported 5 HIGH advisories in the Fastify tree during the live v0.2.26 deploy — all rooted in `fast-uri` (path-traversal GHSA-q3j6-qgpj-74h6 + host-confusion GHSA-v39h-62p7-jpjc) plus a Fastify content-type body-validation-bypass (GHSA-jx2c-rxcm-jvmq, no v4 backport). Cleared by upgrading `fastify ^4.28.1 → ^5.10.0`, `@fastify/cors ^9 → ^11`, `@fastify/rate-limit ^9 → ^11` and regenerating the lockfile; `fast-uri` now resolves to patched 3.1.3/4.1.0. No app code change beyond dropping the explicit `disableRequestLogging: false` from the Fastify constructor — passing the top-level option at all (even the default `false`) trips Fastify v5's `FSTDEP023` deprecation (guard is `!== undefined`, verified in installed `fastify@5.10.0` source + under `--trace-deprecation`; warning says removed in `fastify@6`); the drop is a no-op that silences the warning, request logging still emits. `@cashu/cashu-ts` stays at 2.5.3 (**no published npm/GHSA advisory**, so it does not block this hotfix — but the deprecation points to the `v3-lts` "security-fixes-only" line, so a v3-lts/v4 migration is a **security-relevant money-path follow-up to prioritise**, deferred as a separate slice to avoid behaviour drift in money-handling code — not cosmetic). Post-fix `npm audit --omit=dev` = **0 vulnerabilities**. Added `agent/test/wallet.test.js` (offline wallet guard/failure paths + cashu-ts codec regression) and `agent/test/fastify-v5-api.test.js` (cors preflight + rate-limit route-config contract); agent `node --test` now 30/30, rate-limit smoke green, root build green, ops regression 13+9 green. **NOT yet deployed** — v0.2.26 remains the live server version until an operator re-runs `install-agent.sh`. Base-path awareness + Ollama fallback landed in v0.2.6. Docs hygiene sweep across v0.2.7 → v0.2.12. **v0.2.13 shipped CONT-HEALTH-1**: dashboard now has a live Provider card polling `/api/health/models` every 20s. **v0.2.14 shipped SUITE-VPS-READY-1 rate-limit slice** — the public auth surface is behind `@fastify/rate-limit@9` per-IP with 429 + `Retry-After`; the in-memory challenges Map is capped and LRU-by-expiry evicted with structured `[auth]` logs, all prefix-only. **v0.2.26 shipped SUITE-VPS-READY-2: agent deploy tooling + first-touch admin claim.** New `ops/` assets let an operator run the agent as a hardened standalone systemd service: `ops/systemd/torii-continuum-agent.service` (locked `continuum` user, `ProtectSystem=strict`, only `memory/` + `config.yaml` writable), `ops/nginx/torii-api.conf` (same-origin `/api/` proxy + edge `limit_req`), `ops/install-agent.sh` (idempotent strict-bash installer: locked user, `rsync` without clobbering state, `npm ci --omit=dev`, one-time config gen with a fresh `openssl` session_secret, config validation, nginx `-t`-before-reload wiring, bounded `/api/health` proof), and a refreshed `ops/README.md` runbook. `agent/core/auth.mjs` gained first-touch admin bootstrap: with an empty `admin_npub` the first valid NIP-07 verifier atomically claims admin (npub persisted to `config.yaml` 0600 via injected `persistAdminNpub` in `config.mjs`), race-safe and fail-closed; `/api/health` now reports `admin_claimed`. **The agent is NOT deployed by this work** — running `install-agent.sh` on a server is a separate operator step. **CONT-HEALTH-2 (real Cashu wallet health probe) shipped in v0.2.47-alpha** (see the Active focus above): a non-mutating `wallet.health()` + admin-gated `GET /api/wallet/health` + a Provider-card row, verifying mint identity/NUT-07 support and reporting balance without touching proofs or wallet state.

**Active focus:** **v0.2.50-alpha ships OPS-DEPLOY-2: safe unattended deployment via a server-side pull** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#54**. Problem: local-device automation cannot make outbound SSH and interactive VPS SSH prompts for a password, so a push deploy is impossible; the existing role's redeploy still needs a human to run `ansible-playbook` on the box. Design — a small, root-owned wrapper the VPS runs on a systemd timer that **delegates to the already-hardened `continuum` role** (never reimplements state backup, the atomic staging→swap cutover, the version-asserting health gate, rescue rollback, or cleanup). New files (all `ops/`): **`deploy-unattended.sh`** (→ `/usr/local/sbin/torii-continuum-deploy`): strict `^v<semver>(-prerelease)?$` tag grammar rejecting branches/SHAs/shell+YAML metacharacters (proven against injection strings), optional fail-closed allowlist, optional `git tag -v` signed-tag gate (`CONTINUUM_REQUIRE_SIGNED_TAGS=1`), **idempotent no-op** when live `/api/health` already serves the tag, fresh per-tag clone under `/opt/deploy`, renders localhost vault-free inventory (`continuum_version`+`torii_domain` only), runs `ansible-playbook --tags continuum`, **independently re-verifies** the live version on top of the role's gate, prunes old releases (keeps newest N, never the live one); `flock`-guarded, `set -euo pipefail`, refuses non-root, sourceable for in-process tests. **`systemd/torii-continuum-deploy.{service,timer}`** (oneshot + ~5min timer; no inbound SSH/ports). **`sudoers/torii-continuum-deploy.example`** (OPTIONAL remote/CI trigger: locked non-login `toriideploy` principal, `NOPASSWD` on **exactly the wrapper, no args** — never general passwordless sudo). **`deploy-bootstrap.sh`** (idempotent fail-closed one-time installer: wrapper 0755 root:root, units, root-only `0600` pin-file skeleton it never clobbers, optional principal+sudoers validated with `visudo -cf`, optional dedicated ed25519 key + host-key-pinning guidance). **After bootstrap, releasing a version is a single root-only edit of `/etc/torii/continuum-deploy.conf` (`CONTINUUM_TARGET_TAG=`); a bad release fails closed and self-reverts via the role's rollback — no SSH in the deploy loop.** Security: no secret read/written/logged (vault-free path preserves `config.yaml`/`session_secret`/funded key byte-for-byte); no auth weakened; no general sudo; no ports opened. Tests: new `ops/test/deploy-unattended.test.sh` **55/55** (tag grammar incl. injection, version gate, allowlist fail-closed, version extraction, localhost/vault-free inventory, prune-keeps-live, wrapper fail-closed guards, oneshot/timer wiring, scoped-sudo/no-`NOPASSWD: ALL`, bootstrap 0755/0600/visudo/locked-principal/host-key-pin/no-clobber); existing `deploy-restart` **25/25**, root `vitest run` **872/872**, `npm run build` clean (0.2.50-alpha). Both `package.json` bumped 0.2.49→0.2.50-alpha. Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.49-alpha ships CONT-LIVE-UI-1: surface live provider/wallet/kanban data in the UI** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#52**. Live v0.2.48-alpha authenticated acceptance found three UI regressions where correct backend data never reached the operator. **(1) Dashboard Providers panel blank** despite "Polling every 20s": `ProviderCard()` (`src/views/dashboard.js`) fired the first `tickProvider(body)` synchronously *before* the card was appended, so `body.isConnected` was false, the tick bailed (and cleared its own poll), and the body stayed empty until the first 20s interval — fixed with a synchronous "Checking providers…" placeholder + a deferred first tick via `queueMicrotask(() => tickProvider(body))` (card is in the DOM by then); interval unchanged. **(2) Routstr green `connected` pill but em-dash Cashu balance:** field-name mismatch — `GET /api/wallet/balance` returns `{ total_sats, ... }` but the poll read `r.data.balance_sats` → `undefined` → `formatSats(undefined)` → `—`, while still setting `connected:true` and persisting `cashuBalanceSats:undefined` (so the em dash stuck across re-renders); the top-up modal shared the bug (`received_sats`/`balance_sats`; the endpoint returns `added_sats` only). Fixed with a pure exported `readBalanceSats()` (prefers `total_sats`, `balance_sats` fallback, returns `null` — not `0` — when absent), an in-place balance-number update (`balanceNumEl.textContent`, no page tear), and a receive modal that reads `added_sats` then re-reads the authoritative balance. A configured-but-empty wallet now shows an honest `0 sats` (balance sums to 0 across mints) instead of `—`; a non-zero balance requires one or more `cashu.mints` on the agent AND redeemed proofs via top-up (`/api/wallet/receive`). **(3) Kanban header total + per-column counts excluded imported read-only cards:** `totalCards` and each `col-count` summed only native `cardsFor`, never `importedCardsFor`, so a board/column of only imported work read `0`; fixed to add the imported cards, with the distribution primitive extracted as a pure exported `filterImportedForColumn()`. **Security:** fail-closed posture unchanged — every wallet/health/source route stays `requireAdmin`, no proofs/tokens/secrets/full endpoints are rendered, the wallet probe stays a read-only NUT-07/identity check; these are pure client-side field/ordering fixes with no new network or storage surface. **Tests:** new `src/views/routstr.test.js` (8), `src/views/board-counts.test.js` (7), `src/views/dashboard-structure.test.js` (4). Verification (Node 20.x): root `vitest run` **872/872** (18 files, +19), agent `node --test` **168/168** (unchanged), `npm run build` clean (0.2.49-alpha), `npm audit --omit=dev` root prod **0** / agent prod **0**; root full audit **5** dev-only (vite/vitest/esbuild). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.48-alpha ships OPS-DEPLOY-1: restart the promoted agent + verify the deployed version** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#49**. **Root cause it fixes (diagnosed from the live 0.2.43-vs-0.2.47 incident):** the `continuum-agent` systemd unit file is version-independent, so a code-only redeploy atomically promotes a new release tree WITHOUT changing the unit; the role's `restart continuum-agent` handler is never notified and the old `state: started` task is a no-op against the already-running PID. The swap changed the code on disk but the live process kept serving the PREVIOUS release (stale `/api/health` version, new `/api/wallet/health` + `/api/projects/:slug/sources` routes 404). The old health gate accepted any HTTP 200, so the broken deploy passed. **Fix (`ops/ansible/roles/continuum/tasks/main.yml`):** the unit-install task now `register:`s `continuum_unit_install`; after the JIT smoke test the role (1) runs `meta: flush_handlers` so any unit-file-change restart fires BEFORE readiness (handlers run in definition order — `reload systemd` daemon-reload then `restart continuum-agent`), (2) explicitly `state: restarted` **only when `continuum_promote.changed` AND NOT `continuum_unit_install.changed`** (the exact code-only case the handler misses; the guard prevents a double restart when the unit itself changed — that path is the handler's), then (3) an idempotent enabled+`state: started` ensure. Readiness is strengthened: `uri` now uses `return_content: true` and the `until` requires HTTP 200 AND `agent_health.json.version == (continuum_version | regex_replace('^v',''))`, so a stale process left running by a missed restart reports the OLD version and FAILS readiness, tripping the existing block/rescue rollback instead of passing a broken deploy. Exactly one restart happens per real deploy; the health gate always inspects the post-restart process. Everything else is preserved verbatim: block/rescue rollback (quarantine restore + webroot rollback + standalone re-enable), fail-closed backup, config/state verbatim copy (`session_secret`/funded key never rotated), adoption modes, V8 JIT smoke test, standalone-unit disabling, and `no_log` secret hygiene. The unit template stays version-independent (that is WHY the explicit restart is needed) — a regression test locks that assumption. **Tests:** new `ops/test/deploy-restart.test.sh` (**25** hermetic assertions): restart-before-readiness wiring (register, flush_handlers, guarded `state: restarted`, idempotent ensure), the version-asserting health gate, strict ordering (flush < restart < ensure < health) + handler daemon-reload-before-restart ordering, the version-independent-unit lock, rescue/rollback/webroot/standalone/JIT/config-copy survival, `no_log` retention + no secret in any printed `msg`, and pure version-equality cases proving a **stale 0.2.43-alpha live version FAILS** readiness while the **correct 0.2.48-alpha PASSES** (leading `v` stripped, empty version fails). **Full verification (Node 20.20.1 sandbox; agent code unchanged):** ops shell `deploy-restart` **25/25**, `continuum-adopt` **219/219**, `installer-preflight` **18/18**, `installer-shared-parent` **10/10**, `installer-signal` **9/9**, `nginx-install` **13/13**; root `vitest run` **853/853**; agent `node --test` **168/168**; `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz, version stamps 0.2.48-alpha); `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows the same **5 dev-only** findings (`@vitest/mocker`/`esbuild`/`vite`/`vite-node`/`vitest` toolchain, not shipped in `dist/`; breaking major-vite fix deferred). Lockfile top-level `version` fields left as-is (prior releases never bumped them; `npm ci` validates the dependency tree, not that field — build re-ran clean). No hostnames/devices/secrets in code, tests, or docs. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.47-alpha ships KANBAN-SYNC + wallet health** (agent + frontend; onboarding preview stays **v0.1.20-preview**; both package.json + lockfiles bumped `0.2.46 → 0.2.47`). Closes GitHub issues **#44/#45/#46/#47**. **#46 (read-only project-source adapters, agent):** new `agent/core/project-sources.mjs` + `agent/lib/markdown-todo.mjs` import from local Markdown todo files and public GitHub issues, both **strictly allowlisted, fail-closed**: local paths only under a configured `local_root` with traversal + symlink-escape rejected and file size bounded; GitHub only for `allow_github` `owner/repo` pairs, no credential needed, base URL pinned (`_ghPin`, `redirect:'error'`) as an SSRF guard, with bounded response bytes / `max_issues` (200) / `max_pages` / timeout / rate-limit detection. Records normalize to a stable-fingerprint shape (`{fingerprint, project, source{type,ref,label,id}, title, description, status, priority, labels[], url, readOnly:true, createdAt, updatedAt}`), `dedupeRecords` collapses repeats, and the last valid snapshot is cached per project + **retained on partial/offline/error**. Nothing logs content/tokens/full identifiers. New `project_sources:` config block validated in `loadConfig` (enabled-but-unallowlisted / markdown-without-`local_root` / malformed `allow_github` / unknown type all refuse boot); new admin-gated `GET /api/projects/:slug/sources` + rate-limited `POST /api/projects/:slug/sources/refresh`. **#45 (imported cards on the Kanban, frontend):** `src/views/board.js` keeps imported records in an **ephemeral per-slug `importState` Map, never written to the board store**, so manual cards are never overwritten and repeated refreshes never duplicate (keyed by fingerprint). Each renders as a `board-card imported` container — not draggable, no move/edit controls, source badge + priority/label pills, read-only `imported-badge`, `Read-only imported card:` aria-label, safe external link (`target:'_blank'`, `rel:'noopener noreferrer'`); `renderSyncBar` adds source + status filters, a manual Refresh button, a last-sync line, and stale/partial/error `sync-banner`s. New client fns `walletHealth`/`projectSources`/`refreshProjectSources` in `src/data/agent.js`. Mounted `/continuum/` base path, accessibility, mobile, dark styling, and login/dashboard/about behavior all preserved. **#47 (real Cashu wallet health, agent + frontend):** admin-gated `GET /api/wallet/health` backed by a **non-mutating** `wallet.health()` using cashu-ts (^3.7.1) v3 read primitives (`loadMint()` + `getMintInfo()` identity getters + `.isSupported(7)` NUT-07 + `groupProofsByState`) — no mint/melt/swap, serialized on-disk wallet untouched; honest disabled/healthy/degraded/unreachable states + sanitized reasons; never exposes tokens/proofs/secrets; `src/views/dashboard.js` adds a wallet-health row to the Provider card (polled alongside `healthModels()` via `Promise.all`). **#44 (docs):** todo/progress/handoff/README refreshed through v0.2.46 (stale "next-action" claims removed) then this release recorded with evidence; examples generic/configurable, no real hostnames/secrets, no cross-project queue contamination. Tests: new agent `project-sources.test.js` **29** / `markdown-todo.test.js` **12** / `project-sources-config.test.js` **9** (subprocess) / `wallet-health.test.js` **14**; extended `src/data/agent.test.js` **23** + `src/views/board-structure.test.js` **14**. **Root `vitest run` 853/853, agent `node --test` 168/168**, `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz), `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows **5 dev-only** findings (esbuild/vite/vitest toolchain, not shipped in `dist/`; fix is a breaking major-vite bump, deferred). Migration is additive/backward-compatible — `project_sources` defaults disabled; wallet probe reads existing state without mutation. Code + PR only — **not merged, tagged, or deployed.** **PR #48 review remediation (same branch, version held at v0.2.47-alpha):** a blocking review was addressed in place — **M1** partial-refresh data loss fixed (`refresh()` retains failed sources' last-valid records per-source via a `bySource` Map, replaces only healthy ones, preserves the full snapshot + `syncedAt` on total failure, honest `partial`/`stale`/`retained`); **M2** dedupe title-collapse scoped to Markdown-only so distinct/cross-repo same-title GitHub issues survive (native `fingerprint` wins); **M3** `mintHealth` extracted+exported and covered with a mocked cashu-ts v3 wallet (healthy/degraded/unreachable/timeout/no-NUT-07/spent-pending/no-mutation/no-leak); plus **L4** `GET /api/wallet/health` rate-limited (`wallet_health_per_min`, default 6) + 15 s server cache, **L5** `insertionIndex` ignores imported cards, **L6** absolute `local_root` enforced, **L7** numeric bounds hard-ceilinged (`CEILINGS`), **L8** dead `cache_ttl_sec` removed, **L9** `ok: anyOk` simplified, **L10** empty sync bar suppressed, **N11/N12** doc wording corrected (read-only identity/NUT-07 probe, source/mocked-unit guards not live-browser tests). Re-verified: agent 168/168, root 853/853, build clean, audits unchanged. No ops files touched. Prior slice: **v0.2.46-alpha shipped LOGIN-FIX: sidebar/demo Login "Could not reach agent: Bad Request"** (frontend-only; **no agent code changed**, agent stays **v0.2.43-alpha**, onboarding preview stays **v0.1.20-preview**). In the demo shell (`#/projects`), clicking the bottom-left sidebar **Login** with Plebeian Signer installed failed with *"Could not reach agent: Bad Request"*. **Root cause:** the SPA↔agent client `req()` in `src/data/agent.js` always sent `Content-Type: application/json`, even for the **bodyless** `POST /api/auth/challenge`; Fastify v5 rejects an empty body carrying that content-type with **400 `FST_ERR_CTP_EMPTY_JSON_BODY`** (`error:"Bad Request"`) before the handler runs — so the challenge never issued. The onboarding client's `postJson` was already hardened against exactly this (v0.2.32-alpha, pinned in `agent/test/fastify-v5-api.test.js`); the SPA client had **drifted** and never got the fix. **Fix:** `req()` now declares the JSON content-type **only when a body is actually sent** (mirrors onboarding `postJson`; challenge POST now 200s). Added `errorReason(json,status)` (pure, exported) to surface the specific Fastify `message` over the generic label (200-char cap, never stack traces). Both login entry points (`src/views/login.js` view + `src/shell.js` sidebar button) already share the single `startLogin()` in `src/auth.js` — no duplicate flow to merge; the drift was in the shared HTTP client. Privacy/keys unchanged (nsec stays in the extension; agent still verifies id+sig+admin-pubkey+challenge). Tests: new regression cases in `src/data/agent.test.js` (mock NIP-07 signer + fetch; assert challenge POST has no content-type/no body, verify POST sends exact `{event}` + JSON content-type + adopts token, full `errorReason` map). `vitest run` **843/843**, agent `node --test` **104/104**, ops shell suites green, `npm run build` clean. Root version → **v0.2.46-alpha**; agent NOT bumped (its empty-body 400 is correct standard Fastify behaviour — no protocol change). **Kanban discoverability (reported):** board is at `#/projects/:slug/board`, reached via the **Overview · Board** switcher after opening a project; all project routes are demo-browsable (only `/dashboard` is auth-gated), so the Board link **is** visible in demo mode — just not surfaced on the `#/projects` index cards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.44-alpha ships APP-ROUTING: application-first root (login + dashboard), sales page isolated to `#/about`** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Fulfils the operator ask *"separate out that sales page from the root of continuum and replace it with a login page and the dashboard."* The root (`/continuum/`) is now the application, never the marketing page. A pure, DOM-free guard layer (`src/nav-guard.js`: `rootTarget`/`guardRedirect`/`isProtectedPattern`, `PROTECTED_PATTERNS=['/dashboard']`) decides routing: **logged in → root redirects to `#/dashboard`; logged out → a branded Amber login page (`src/views/login.js`) renders in place at root** (shared `toriiSvg` mark, full-bleed `landing-mode`, drives the existing NIP-07/Plebeian-Signer `startLogin()` — no password auth, no key material in the DOM; small non-primary links to `#/about` + the demo shell). The former marketing view is reframed/exported as `renderAbout` in `src/views/landing.js` and lives only at **`#/about`** — never the root, never an onboarding-completion target. `#/dashboard` is guarded: a logged-out visitor (incl. refresh / deep link, since guards run on every router resolve including the initial one) is bounced to login at root; because root renders login **in place** (never itself bouncing when logged out), `protected → root → login` is terminal — **no redirect loop**. A session drop while on a protected view (sign-out / 401 expiry) reactively returns to login via the `continuum:session-changed` listener. `adoptOnboardingSession()` still runs before first render, so a freshly onboarded operator with a live `torii.session` lands authenticated on the dashboard (no login bounce); demo views (`#/projects`/`#/marketplace`/`#/routstr`) + all Amber nav preserved. No Google/gstatic/Fontshare/CDN/analytics added — local/system assets + inline SVG only. New `test/app-routing-auth.test.js` (**20** tests): unauth-root→login, auth-root→dashboard, dashboard protection, no-loop, `#/about`/demo isolation, onboarding adoption→authenticated root, expired/invalid→login, plus source-structure locks on `main.js`/views. Browser QA: no headed browser / no jsdom in the sandbox, so the contract is proven by the exhaustive pure guard tests + the source locks + the rebuilding no-external-request scan (guards run on the initial resolve = refresh/deep-link). Root `package.json` → **v0.2.44-alpha**. **Ops addendum in the same PR (v0.2.44-alpha):** a live v0.2.43 rerun hit one more exact bug — `group_vars/all.yml.example` shipped `torii_root_app: launcher`, but the installed CLI/API accepts `torii set-root <registered-name|none>` and returned **404 `{error: app_not_installed, name: launcher}`** (the Torii Suite launcher at `/` is the sentinel `none`, not a registered app), rolling the deploy back; rollback worked and the operator reran with `-e torii_root_app=none`. Fix: the example default is now **`torii_root_app: "none"`**, and the continuum role no longer calls `set-root` raw — it routes through **`ops/lib/continuum-adopt.sh set-root-safe`**, which `normalize_root_app`-maps legacy/empty/`launcher` (+ `torii`/`base`/`root`/`home`/`homepage`/`default`) → `none`, sources the admin token from `/opt/torii/env` silently (never printed), **skips** the call when `torii get-root` already matches, and returns 0 for the default so a normal install can never trip the rescue rollback. An explicit **registered** app name still passes through unchanged; pointing the root at `continuum` emits a discouraged-choice warning but is honoured when set deliberately — **Continuum is never made the root by its own installer.** `ops/README.md` gains a "Root-app selector correction (v0.2.44-alpha)" subsection; `continuum-adopt.test.sh` grew **188 → 219** (mock `torii` CLI reproducing the exact live 404; `launcher`→`none` normalization incl. synonyms/whitespace/case; default skip when already none + reset when not; explicit registered app honoured; continuum-warn; no-token-leak; example/role anti-drift greps). No-Google/no-CDN guarantees untouched. Tests: `vitest run` **795/795** (775 prior + 20), agent `node --test` **104/104**, `continuum-adopt.test.sh` **219** + ops regressions 18/10/9/13, root build clean (`dist/` free of external loading vectors, only the inert Plebeian-Signer store anchor remains), `npm audit --omit=dev` 0 vulns (root + agent). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.43-alpha ships OPS-HARDENING: encode three live-discovered cutover corrections** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). The v0.2.42-alpha cutover succeeded manually on the live box; three environment realities had to be fixed by hand and are now encoded permanently. **(1) Torii CLI `register` uses flags, not positionals** — the installed CLI is `torii register <name> [--display …] [--desc …] [--version …]`; the old positional form failed with `unknown flag: Continuum`. The role now calls `torii register {{ continuum_register_name }} --display "Continuum" --desc "App builder + agent" --version <bare-semver>` (bare version via `continuum_version | regex_replace('^v','')`), with new `continuum_register_*` defaults. **(2) `MemoryDenyWriteExecute` must be `no`** — Node 22's V8 JITs with W^X memory; `=yes` makes the kernel refuse the mprotect and V8 dies on startup (fatal `SetPermissions` / errno 12 / `SIGTRAP`). `continuum-agent.service.j2` now sets `MemoryDenyWriteExecute=no` with an inline rationale and keeps every other compatible directive (`NoNewPrivileges`, `Protect*`, `RestrictNamespaces`, `LockPersonality`, empty `CapabilityBoundingSet`). The role runs a **Node V8 JIT smoke test** under the rendered MDWE value via a transient `systemd-run` scope before starting the real service. **(3) nginx cannot traverse `/home/continuum`** (0750 home + `ProtectHome` → HTTP 500) — the build is now published to a **public webroot `/var/www/torii/continuum`** (root-owned, dirs 0755 / files 0644) via new `deploy_webroot` (atomic staged swap, prior webroot kept as timestamped backup); only the static bundle is copied out, source + encrypted state stay private under `/home/continuum/app`. A **single** `location /continuum/` prefix alias serves both the SPA entry and the hashed `assets/*` bundle (no regex-alias, no `/home`). The cutover `rescue` now also rolls the webroot back (`rollback_webroot`) + reloads nginx on failure. Re-running against the now-live v0.2.42 Ansible layout is idempotent (re-detects `existing-ansible`, preserves config, never rotates `session_secret`/touches the funded key). `ops/test/continuum-adopt.test.sh` grew **117 → 188** assertions (webroot deploy/rollback + perms + dangerous-target refusal; mock Torii CLI parser reproducing `unknown flag: Continuum` vs flag-form success; systemd MDWE=no contract + retained hardening; Node V8 WASM smoke; nginx section is now a **no-nested-assets regression lock**; existing-ansible upgrade idempotency). New `ops/README.md` v0.2.43 subsection + upgrade command. Root + agent version → v0.2.43-alpha (+ both lockfiles). Tests: adopt **188/188**, ops regressions green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. **AMENDMENT (pre-merge, same PR #40) — frontend zero-external-request + asset-404 fix:** (4) removed all production Google (`fonts.googleapis.com`/`fonts.gstatic.com`) **and** Fontshare (`api.fontshare.com`) preconnects/stylesheets from `index.html`; typography is now a **deliberate local system font stack** in `src/styles/theme.css`, so production makes **zero external requests** (no fonts downloaded/vendored). (5) the live black page was the nested `location /continuum/assets/` alias+`try_files` returning 404 for hashed `.js`/`.css`; both nginx templates now use a **single parent alias** only. (6) `ops/README.md` upgrade steps corrected to the real live paths (`/opt/deploy/torii-continuum-v0.2.42-alpha` checkout, app at `/home/continuum/app`; prefer a fresh version-specific public clone of the final tag) — the old `/opt/torii/src/torii-continuum` never existed. New frontend tests: `test/no-external-cdn.test.js` (builds `dist/`, asserts index + assets carry no banned CDN/font host and no external loading vector; same-origin `/continuum/api` allowed) and `test/nginx-continuum-routing.test.js` (HTTP server modelling nginx `alias`+`try_files`, using the **real hashed filenames** from `dist/index.html`, proves `/continuum/` index + hashed `.js`/`.css` = 200 and deep-route SPA fallback). `vitest run` now **775/775**. Browser QA: no headed browser in the sandbox — verified via the build + HTTP-200 routing + same-origin-only fetch scan; the app is a hash-router SPA (`#/`,`#/projects`,`#/marketplace`,`#/routstr`,`#/dashboard`) so the server contract is fully exercised, and the live box already rendered the Amber dashboard with zero console/network errors after the manual asset-route fix. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.42-alpha ships OPS-HARDENING: transactional adoption/cutover + partial-adoption recovery** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the live v0.2.41-alpha adoption failure: the role migrated live state INTO `/home/continuum/app/agent` and only THEN ran `ansible.builtin.git` into `/home/continuum/app` — cloning into a dir already populated with runtime state. It aborted, leaving the **original standalone layout intact** AND a **partial, non-git `/home/continuum/app`** holding a copy of the state; the untouched `torii-continuum-agent.service` was restored and health is good. **Fix (`ops/lib/continuum-adopt.sh` + `roles/continuum/tasks/main.yml`):** `layout_detect` gains a 3rd `app_dir` arg and a new **`partial-adoption`** mode (agent has state but `app/.git` absent while standalone is still present — a real git-backed Ansible install still wins; a non-git app dir carrying state is recovered, never built on top of). New `authoritative_state_dir` picks the untouched standalone as the live source over the partial copy. New `stage_reset`/`promote_release`/`rollback_release`: the `git checkout` + `npm ci` + `vite build` run in a **clean staging dir** (`/home/continuum/app.staging`) while the **old unit keeps serving**; authoritative state is copied in **only after a successful build**; the old unit is stopped only immediately before an **atomic same-filesystem swap** into `/home/continuum/app`, quarantining (never deleting) any pre-existing app dir to `app.quarantine-<UTC>`. The `block`/`rescue` cutover stops the new unit on any promotion/`/api/health` failure, rolls an existing-Ansible tree back from quarantine (or re-enables the standalone for adopt/partial), and prints backup + quarantine paths with an exact **secret-free** recovery command. Re-running after the exact observed failure is idempotent. `ops/test/continuum-adopt.test.sh` grew to **117** hermetic assertions (3-arg detect incl. partial-adoption + absent-app-dir; `authoritative_state_dir`; `stage_reset` refuses `/ /home /root /opt /opt/torii`; `promote_release` clean/quarantine/refuse-existing-quarantine/idempotent; `rollback_release` restore/refuse-without-failed-dir/no-quarantine; end-to-end partial-adoption recovery proving byte-for-byte config equality + funded-key retention + quarantine preservation + untouched standalone; v0.2.42 anti-drift greps that clone/build target staging, state is staged before promotion, promotion+rescue are gated on a swap fact, and the old unit stops only at cutover). New "Transactional cutover + partial-adoption recovery (v0.2.42-alpha)" subsection in `ops/README.md` with the exact recovery invocation for the current partial-adoption VPS (same vault-free localhost invocation, `continuum_version: v0.2.42-alpha`). Root + agent version → v0.2.42-alpha (+ both lockfiles). Tests this slice: adopt **117/117**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.41-alpha ships OPS-HARDENING: vault-free standalone adoption / redeploy** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes the blocker left by v0.2.40-alpha: on `adopt-standalone`/`existing-ansible` the live `config.yaml` is preserved byte-for-byte, but the preserve path still rendered a `.config.candidate.yaml` from `config.yaml.j2` (which references `{{ admin_npub }}`/`{{ session_secret }}`) for the drift diagnostic — so a truly vault-free run (no `group_vars/vault.yml`, no vault password) died with an undefined-variable error before finishing adoption. **Fix (in `roles/continuum/tasks/main.yml`):** a new `continuum_vault_vars_present` fact is computed defensively via the `default('')` filter (an undefined var yields `false`, never raises); the candidate render + `config-drift` + candidate removal + "differ" warning are all guarded behind `continuum_vault_vars_present | bool` and SKIP entirely when the vault vars are absent (a non-secret debug notes the diagnostic was skipped). Adoption still preserves the live config byte-for-byte, so the funded Routstr key stays decryptable. A fresh install or an explicit `continuum_allow_config_rotation=true` rotation still needs the vault vars, and their absence in those modes **fails closed** with a clear, non-secret `ansible.builtin.fail` *before* any state is written. `ops/test/continuum-adopt.test.sh` grew to **66** hermetic assertions (vault-free adopt end-to-end incl. byte-for-byte config preservation + funded-key migration + no-secret-in-output; existing-ansible redeploy; fresh missing-vars fail-closed; explicit-rotation missing-vars fail-closed; presence truth table; anti-drift greps for the new guards). New "Vault-free adoption / redeploy (v0.2.41-alpha)" subsection in `ops/README.md` gives the exact secret-free localhost invocation (inventory `torii→localhost ansible_connection: local`, `group_vars/all.yml` with `torii_domain`/`continuum_version`, `ansible-playbook -i inventory.yml site.yml --tags continuum` with NO `--ask-vault-pass`). Operator handles NO secrets and needs NO vault password for adoption. Tests this slice: adopt suite **66/66**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.40-alpha ships OPS-HARDENING: safe standalone→Ansible adoption, no config clobber, fail-closed backups** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the dangerous deployment mismatch that blocked the live v0.2.39-alpha rollout: the continuum Ansible role cloned to `/home/continuum/app` and **re-rendered `config.yaml`/`session_secret` unconditionally on every run**, while the live standalone box uses `/opt/torii/continuum-agent` + unit `torii-continuum-agent.service` on port 8787 — so running the old role over that box would have overwritten the session_secret (orphaning the funded Routstr key, encrypted at rest under a key derived from it) and/or double-bound 8787. **Fix:** the risky detect/backup/migrate/config-decision logic is factored into a sourceable, unit-tested lib `ops/lib/continuum-adopt.sh`, and `roles/continuum/tasks/main.yml` is now a thin guarded orchestrator that (1) **detects** layout (`fresh`/`adopt-standalone`/`existing-ansible`; an existing Ansible layout always wins); (2) takes a **fail-closed** timestamped **root-only 0700 backup** of `config.yaml`+`memory/`+`ciphertexts/`+`pending/` from both layouts under `/root/continuum-backup-<UTC>/` before any mutation (no backup ⇒ abort); (3) on adopt **stops+disables** the standalone unit (frees 8787) and **migrates config+state verbatim** — `session_secret` copied byte-for-byte, never regenerated, migration idempotent; (4) **never clobbers** an existing config on routine deploy — renders from vault ONLY on a fresh install or explicit `-e continuum_allow_config_rotation=true` (off by default), and on a preserve runs a one-way drift check that prints only `same`/`differ` (never a value) and surfaces a safe "rotation pending" notice; (5) wraps the service+nginx cutover in a `block`/`rescue` that on failure prints the backup path + exact recovery command. `git force:true` resets only tracked files, and all agent state is gitignored, so a re-checkout/build can't delete encrypted state. New `roles/continuum/defaults/main.yml` holds the safe tunables (`continuum_standalone_dir`, `continuum_standalone_service`, `continuum_backup_root`, `continuum_allow_config_rotation: false`); secret-touching tasks carry `no_log: true`. Pinned by `ops/test/continuum-adopt.test.sh` (**44** hermetic assertions: detection precedence, fail-closed backup, verbatim+idempotent migration, config-action matrix, no-secret drift, permissions, anti-drift greps). New "Adopting a standalone install with Ansible" runbook in `ops/README.md`. **Operator handles NO secrets** at any point; deploy is the normal `ansible-playbook … --tags continuum`. Tests this slice: adopt suite **44/44**; unchanged app/agent suites not re-run beyond the required build/audit. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.39-alpha ships ONBOARDING-GATING + APP-MOUNT: real claimed-state Step-3 gating, dup-pay race closed, completion lands on the actual app dashboard** (onboarding preview → **v0.1.20-preview**, `preview-assets/onboarding-v0.1.20/`). Two live defects on the v0.1.19/v0.2.38 stack plus the app-mount follow-through. **User-funded Routstr key, all encrypted agent memory, and prior payment state preserved — no invoice created/paid/re-submitted, no `memory/**` touched.** **(A) Claimed-state gating (root cause):** a verified funded key still rendered both setup cards, key-entry/reveal, *Verify & connect* and *Request an invoice* — a CSS cascade defect, not logic: author `display` rules (`.btn-primary{inline-flex}`, `.wallet-choices/.routstr-paths{flex}`, `.choice-card{grid}`) outrank the UA `[hidden]{display:none}`, so `el.hidden=true` visually no-op'd. Fix: canonical `[hidden]{display:none !important}` in `shared.css`; the status-only resume path funnels through `renderSuccessAdvance` via a single `showClaimed` helper (replaces `showConnected`); the panel carries `data-claimed="1"` as single source of truth, leaving one green summary + exactly one standalone Continue. **Dup-pay race closed:** `data-claimed` checked as the first line of quote/key-connect/pay-confirm handlers (pay button also permanently disabled once claimed) — a second invoice/payment is structurally impossible in claimed state. **(B) App mount:** `CONTINUUM_HOME` `/continuum/` → `/continuum/#/dashboard` (+ curtain anchor) so completion opens the multi-view Amber SPA dashboard, not the marketing root; open-redirect guard preserved. **Session handoff:** `adoptOnboardingSession()` (in `src/data/agent.js`, called once in `main.js` before `initStore()`) fails closed if the SPA already has a live session, else adopts the `localStorage['torii.session']` envelope token into `continuum.session.v1` only if `tokenLooksLive` (shape + unexpired), so a freshly onboarded user is not bounced to sales/login; `deriveSameOriginBase` makes `agentUrl()` fall back to same-origin `/continuum` under the mount (no CDN/third-party origin). **No separate torii-suite PR required** — the `/continuum/` alias + subpath refresh + `/continuum/api/*` proxy + SPA build + agent install + nginx fragment + launcher registration are all in this repo's `ops/ansible`; the root launcher is never overwritten. **Safe idempotent deploy (operator step, NOT run here, from `ops/ansible/`):** `ansible-playbook -i inventory.yml site.yml --ask-vault-pass --tags continuum`. **(C) Wording:** residual VPS/daemon/box copy in `routstr.js`/`landing.js`/`auth.js` scrubbed to "your Torii, your gateway". Tests: root vitest **762/762** (new `src/data/agent.test.js` 11 cases; preview v0.1.20 **153**), agent `node --test` **104/104**, `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Honest limitation: no jsdom/live agent here, so no live funded-agent click-through — gating proven by the CSS `!important` cascade guarantee + source/behaviour guards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.38-alpha ships ONBOARDING-FINISH: verified-state cleanup, correct balance units, inline key reveal, deterministic final curtain** (onboarding preview → **v0.1.19-preview**, `preview-assets/onboarding-v0.1.19/`). A focused live UX/completion pass from three screenshots; no character/animation/CDN/provider-pinning behaviour changed and no payment/NWC semantics changed beyond balance *display* and safe key *retrieval*; the funded key + prior state are preserved. **(A) Balance units (root cause):** Routstr denominates balances in MILLISATS but `extractBalanceSats` surfaced the raw `balance` verbatim, so a 10,000-sat key showed "10000000 sats"; the provider adapter now divides msat fields to whole sats (explicit `*_sats` trusted), a marker-guarded `displayBalanceSats` migration divides legacy stored envelopes (no `balance_units`) by 1000 on read while new envelopes carry `balance_units:'sat'`, and all balances render via `formatSats` (e.g. `10,000 sats`). **(B) Step 3 verified state:** once claimed/encrypted/verified, `collapseStep3Setup` hides both path cards, both forms, the invoice card, the progress bar and all three setup CTAs, leaving one green summary + a SINGLE Continue button outside it, no countdown/auto-advance (supersedes the v0.1.18 countdown), advance idempotent. **(C) Step 5 recovery kit:** removed the unbranded "Reveal full Routstr key (one-time)" button + its `window.confirm` flow (never revealed the key); the ROUTSTR KEY row now has branded inline eye + copy controls that fetch the key over the existing admin-authenticated no-store `export-key` endpoint, hold it in memory only (never localStorage/sessionStorage/URL/logs), and re-mask on toggle/step-leave/tab-hide/session-loss/timeout (`shouldMaskReveal`/`REVEAL_TIMEOUT_MS`); the Download click is itself the explicit confirmation and embeds the full key in the saved file while still excluding the NWC secret. **(D) Final curtain:** the old curtain only `console.log`'d a commented redirect and spun forever; `initCurtain` now navigates deterministically to the resolved same-origin `/continuum/` home (a `window.__toriiContinuumHome` override accepted only same-origin — open-redirect guard), reveals a real "Open Continuum now" fallback link on a bounded timer, honours `prefers-reduced-motion`, and drops the spinner shortly after the nav attempt so it can never hang. Tests: agent `node --test` **104/104**, root vitest **269/269** (preview v0.1.19 **145**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.37-alpha ships ONBOARDING-PAY-RECOVERY: paid-but-unclaimed Routstr recovery + payment progress state machine + working recovery kit** (onboarding preview → **v0.1.18-preview**, `preview-assets/onboarding-v0.1.18/`). Fixes a live incident where an operator connected NWC and paid 10,000 sats to fund a Routstr session, but the UI said "payment confirmed" without surfacing the `sk-` key, never auto-advanced, and the Recovery Kit "download" was a no-op that just stepped the deck. **No new invoice created or paid during diagnosis or fix.** Delivered a safe SSH recovery runbook first (inspect `memory/secrets/*.enc` by metadata only — never decrypt; reuse the operator's existing browser session token against the loopback admin API; claim the already-paid key with the **idempotent** `POST /api/onboarding/routstr/recover` sending `{}` — the agent re-submits the stored bolt11 and never re-pays; redacted status/verify; one-time no-store `export-key` only if a manual copy is required). Root fact: **payment-confirmed does NOT imply the key is stored** — the claim is a separate post-settlement step; recovery states are paid+claimed / paid+unclaimed (the live case) / unpaid-or-expired. **Agent (v0.2.37-alpha):** new admin-gated endpoints `GET /api/onboarding/recovery/state` (redacted resume snapshot; `claimable` true only when a pending invoice is stored with no key), `GET /api/onboarding/recovery-kit` (`Cache-Control: no-store`, secret-free by default), `POST /api/onboarding/routstr/export-key` (one-time full-key reveal; `confirm===true` required else 400, 409 when no key, `no-store`, rate-limited, persists `export_count`/`last_exported_at` audit, logs warn never the key). **Preview (v0.1.18):** pure `ONBOARD_PHASES`/`PHASE_META` state machine driving an accessible (role=status, aria-live) progress + scanning bar + elapsed timer across connect→quote→pay→claim→verify; **SUCCESS only when the agent reports `key_stored===true`** (`classifyPayResult`/`classifyRecoverResult` map bare paid/recoverable → retryable `PAID_UNCLAIMED`, never success); duplicate-payment prevention (Confirm button permanently disabled after first click); success renders the redacted key then auto-advances after a short countdown with an immediate Continue-now; refresh-resume reads `recovery/state` on load and finishes a `claimable` claim via empty-body `recover` (no re-payment); real Recovery Kit download via Blob (`buildRecoveryKit`) whose default excludes the NWC secret + full key and carries redacted previews + restoration instructions, with the full key included only via an explicit confirmed one-time `export-key` reveal; Step 5 markup no longer prints static secret-adjacent values. Preserved: character/animation behaviour, self-hosted Three.js (no CDN), provider pinning, redaction, rate limits, no autonomous spend, no nsec on server. Tests: agent `node --test` **102/102**, root vitest **453/453** (preview v0.1.18 **124**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.36-alpha ships ONBOARDING-ASSET: new Chiefmonkey GLB + forbidden-safe animation state machine** (onboarding preview → **v0.1.17-preview**, `preview-assets/onboarding-v0.1.17/`). A substantial asset + animation upgrade of the design-review onboarding mockup (NOT the production app). A fresh Chiefmonkey source GLB replaces the old `chiefmonkey6.glb` and is crushed for production by a new reproducible local optimizer `tools/optimize-glb.mjs` (gltf-transform 4.4.1 + draco3dgltf 1.5.7 + sharp 0.35.3, build-time only, no third-party runtime CDN): `dedup → weld → resample → textureCompress(webp,q82,≤1024²) → prune → draco(edgebreaker)`, dropping every forbidden locomotion/knock-down clip with the runtime's own `isForbiddenClip` predicate (single source of truth). Source 9,298,852 B (SHA `87b0048c…c37dd`) is kept OUT of git (build artifact only, see `tools/SOURCE.md`); the committed optimized `assets/chiefmonkey-onboarding.glb` is 2,347,780 B — **74.75% smaller** (target ≥60%), SHA `0253d5e1…e2fcb` — with a `.manifest.json` recording sizes/%reduction/deterministic SHA + retained/dropped clip inventory (13 retained, 5 forbidden dropped: `Clapping_Run`/`Knock_Down`/`Running`/`Stylish_Walk_inplace`/`Walking`; mesh/skin(24 joints)/material/texture preserved nonzero, clip names survive Draco). `character.js` now runs a dedicated forbidden-safe state machine — each of the 5 deck steps plays one dedicated clip from `STEP_CLIPS`/`selectStepClip` (1 Talk_with_Hands_Open, 2 Agree_Gesture, 3 mage_soell_cast_3, 4 Gentlemans_Bow, 5 Idle_10; dormant step-6 Victory_Cheer curtain), applied deterministically on entering/restoring via the v0.1.15 `resolveReadyStep` path (order-independent) — and a raycast hit-test (`Raycaster.intersectObject`, NDC from canvas rect) plays a random one-shot from a curated `CLICK_POOL` (`pickClickReaction`, no immediate repeat) then crossfades back to the current step clip on the mixer `finished` event, ignoring UI-control clicks / respecting `prefers-reduced-motion` / ignoring click-spam. Step-1/2/3 prompt/success/failure `onboarding:anim` overrides are transient and always return to the current step clip (no race can permanently overwrite step state). Deterministic offline tests parse the optimized GLB JSON chunk (counts nonzero, referenced clip names survive), assert no forbidden pattern is selectable, verify the committed asset matches manifest SHA + size budget (≤3 MB, ≥60%), and cover every step mapping / restore ordering / status→current-step return / click hit-no-hit + UI-guard + no-immediate-repeat + spam guard + crossfade return / reduced motion / final load-error. Verified target-file + full root vitest green and `npm run build` clean. Code + draft PR only — preview snapshot only, not deployed. Prior slices (onboarding preview arc): **v0.2.35-alpha / v0.1.16-preview** (PR #32) reworked Step 2 (wallet-agnostic NWC connect via NIP-47 `get_info` capability matrix, funding gated on `pay_invoice`) and Step 3 (Routstr existing-`sk-…`-key OR fund-a-new-session: quote → explicit confirm → NWC pay → claim minted key, with a RECOVERABLE poll-timeout carrying the bolt11 via `/lightning/recover`); secrets stored encrypted at rest (AES-256-GCM), admin-gated + rate-limited, pay refuses unless `confirm===true` AND `pay_invoice` advertised. **v0.2.34-alpha / v0.1.15-preview** (PR #31) added the deterministic character↔deck step-sync state machine (`createCharacterSync`/`recordStep`/`resolveReadyStep`/`markCharacterFailed`) that fixed the intermittent invisible-Chiefmonkey-after-soft-reload race by remembering steps before model readiness instead of dropping them then hard-applying Step 1. **v0.2.33-alpha / v0.1.14-preview** (PR #30) fixed refresh dropping Step 2 → Step 1 (`restoreSession`/`isSessionValid` fail-closed, `window.__toriiRestoredStep`) and Chiefmonkey invisible on reload (route both GLB stall AND error through `nextLoadAttempt`). Prior slice: **v0.2.32-alpha ships ONBOARDING-STEP1-FIX: "agent challenge failed (400)" on Sign with Plebeian Signer** (onboarding preview → **v0.1.13-preview**, `preview-assets/onboarding-v0.1.13/`). Client-only fix: `onboarding-client.js`'s `postJson` was sending `Content-Type: application/json` on the **bodyless** `POST /api/auth/challenge`, and Fastify v5 rejects an empty body carrying that header with `400 FST_ERR_CTP_EMPTY_JSON_BODY` — failing step 1 before the NIP-07 prompt (which only fires after a challenge is fetched), so Chiefmonkey animated but no signer opened. Fix: `postJson` only sets the JSON content-type + serialises a body when a body is actually present; the challenge call now goes out bodyless (→ 200), verify unchanged. No server endpoint added, no validation weakened, no bunker-connect endpoint; NIP-07 primary + browser-client NIP-46 secondary, `localStorage['torii.session']`, exact signer wording, and the three clips (`HandGesture_00`/`Idle_03`/`Confused_02`) all preserved. New offline vitest case (challenge POST has no body/no JSON content-type; verify still does) + new agent `fastify-v5-api` case pinning the empty-body-400 vs bodyless-200 contract. Prior slice: **v0.2.31-alpha shipped ONBOARDING-STEP1: live NIP-07/NIP-46 auth for onboarding step 1** (onboarding preview → **v0.1.12-preview**, `preview-assets/onboarding-v0.1.12/`). New self-contained `onboarding-client.js` (ES module, no build step, no third-party CDN) wires step 1 to the same-origin agent API. NIP-07 primary via `window.nostr` (button "Sign with Plebeian Signer"): `POST /api/auth/challenge` → sign the exact kind-22242 auth event `agent/core/auth.mjs` expects (`content == challenge`, `['challenge', …]` + `['relay', origin]` tags) → `POST /api/auth/verify` → persist to exactly `localStorage['torii.session']` as `{ token, expires_at, pubkey, method, created_at }` (token + public identity only, no secrets). Fails closed on malformed responses, expired challenges, pubkey/challenge mismatch, or absent token. NIP-46 secondary with the browser as the client (bunker46 architecture, "Use a different signer"): parses a pasted `bunker://` string and asks the remote signer to sign the same event over its relay — **no server bunker-connect endpoint**, no key/secret sent to the agent, no silent fallback. Chiefmonkey phases via the existing event channel: `HandGesture_00` prompting/signing, `Idle_03` success, `Confused_02` failure, each with an ordered fallback to an existing GLB clip (the GLB ships neither `HandGesture_00` nor `Confused_02`, so the fallback runs and never freezes). Desktop-only gate + all prior perf fixes preserved. 28 offline vitest cases; verified root vitest 28/28, root build, agent `node --test`, ops regressions, agent `npm audit --omit=dev`. Code + draft PR only. Prior slice: **v0.2.30-alpha shipped AGENT-SEC-OPT-TORII-PERMS: least-privilege fix for the shared `/opt/torii` parent.** The v0.2.29 deploy surfaced a production regression — `ops/install-agent.sh` created the shared parent with an unconditional `install -d -m 0750 -o continuum -g continuum /opt/torii`, and because `install -d` re-applies mode+owner on every run it clamped the parent that torii's *other* apps live under (torii-base launcher, quest tooling) to `0750 continuum:continuum`, stripping its world-execute bit. nginx (`www-data`) could then no longer traverse `/opt/torii` to reach `/opt/torii/launcher/index.html`, so `/` fell through to a default nginx 404 (Quest under `/var/www` stayed readable). Fix: create `/opt/torii` **only if absent**, `root:root 0755`, and **never re-own/re-mode an existing shared parent**; the agent's own subdir `/opt/torii/continuum-agent` stays locked `0750 continuum:continuum` (nginx never serves from it — the agent is loopback-proxied on 8787). New hermetic + anti-drift regression `ops/test/installer-shared-parent.test.sh` (10 assertions) pins that the installer no longer chowns the shared parent to the service user, guards creation with an existence check, and keeps `$INSTALL_DIR` locked. The previously-planned **onboarding** work moves to **v0.2.31-alpha**. Operator out-of-band unblock if needed before redeploy: `sudo chown root:root /opt/torii && sudo chmod 0755 /opt/torii` (restores o+x, leaves the agent subdir untouched). Prior slice: **v0.2.28-alpha shipped AGENT-SEC-CASHU-LTS: the money-path dependency migration flagged in v0.2.27.** `@cashu/cashu-ts` moved `2.5.3 → 3.7.1` (the maintained `v3-lts` "security-fixes-only" line; dist-tag `v3-lts` resolved to `3.7.1` at implementation time, latest was `4.7.0` — v4 deliberately not taken), pinned `^3.7.1`, lockfile regenerated with sha512 integrity and the deprecation notice cleared. Code adapted in `agent/core/wallet.mjs` only: class rename `CashuMint→Mint` / `CashuWallet→Wallet`; the boot warm-up `getMintInfo()` (async in v2, now a sync cached getter that throws pre-load in v3) replaced by `await wallet.loadMint()`; an idempotent `ensureLoaded()` guard added before `receive()`/`send()` to restore v2 lazy-load-on-demand (verified zero extra network once keysets are cached). Token/proof codec, `Token`/`Proof`/`SendResponse` shapes, and the `receive()→Proof[]` contract are unchanged between the versions (both default to token-v4 `cashuB`). Safety proof: a token encoded by the **real 2.5.3 library** decodes under 3.7.1 with all mint/unit/memo/id/amount/secret/C preserved AND **re-encodes byte-identically**; on-disk `memory/wallet/*.json` survives a JSON round-trip unchanged — no re-mint, conversion, deletion, or network mutation. New offline `agent/test/cashu-migration.test.js` (8 cases) pins every used Cashu boundary incl. malformed/truncated tokens failing closed without leaking secrets; agent `node --test` now **38/38**. `npm audit --omit=dev` and full audit both **0 vulnerabilities**; real `node index.mjs` boot on Node 20.20.1 clean with no new warnings and Fastify 5.10.0 intact; root build + ops regressions (13/13, 9/9) green; prod tree shrank 71→68 packages. **Runtime gate — RESOLVED in v0.2.29-alpha (AGENT-SEC-CASHU-LTS-RUNTIME):** cashu-ts 3.7.1 declares `engines.node >=22.4.0` across the whole v3-lts line. This is now treated as a **HARD deployment prerequisite for the money path, not an advisory** — an `EBADENGINE` warning during `npm ci` is not an acceptable production state for a wallet. v0.2.29 raised `agent/package.json` (+ agent lockfile) `engines.node` to `>=22.4.0` (root frontend `package.json` deliberately left engine-free — static vite/vitest, no agent runtime), rewrote the `ops/install-agent.sh` preflight to gate on a robust major.minor.patch check (`ops/lib/node-version.sh::node_version_ok`, rejecting 22.0.x–22.3.x, erroring with the floor and stopping before touching anything), added `ops/test/installer-preflight.test.sh` (18 host-independent boundary assertions), fixed the stale `wallet.mjs` comment, and made `ops/README` state Node 22 LTS is a hard prerequisite. Verified under a real **Node 22.11.0** runtime (sandbox default is Node 20.20.1): clean `npm ci` with no EBADENGINE, 0 vulns, agent 38/38, ops 18/9/13, root build + vitest green, and a `node index.mjs` boot → `/api/health` 200 (v0.2.29-alpha) with no warnings. **Deploy prerequisite:** upgrade the VPS to Node 22 LTS before re-running `ops/install-agent.sh`; it will refuse to run on Node 20. **NOT yet deployed** — v0.2.26 remains the live server; v0.2.27 remains the newest shipped code. Prior slice: **v0.2.27 shipped AGENT-SEC: production dependency remediation.** `npm audit --omit=dev` in `agent/` reported 5 HIGH advisories in the Fastify tree during the live v0.2.26 deploy — all rooted in `fast-uri` (path-traversal GHSA-q3j6-qgpj-74h6 + host-confusion GHSA-v39h-62p7-jpjc) plus a Fastify content-type body-validation-bypass (GHSA-jx2c-rxcm-jvmq, no v4 backport). Cleared by upgrading `fastify ^4.28.1 → ^5.10.0`, `@fastify/cors ^9 → ^11`, `@fastify/rate-limit ^9 → ^11` and regenerating the lockfile; `fast-uri` now resolves to patched 3.1.3/4.1.0. No app code change beyond dropping the explicit `disableRequestLogging: false` from the Fastify constructor — passing the top-level option at all (even the default `false`) trips Fastify v5's `FSTDEP023` deprecation (guard is `!== undefined`, verified in installed `fastify@5.10.0` source + under `--trace-deprecation`; warning says removed in `fastify@6`); the drop is a no-op that silences the warning, request logging still emits. `@cashu/cashu-ts` stays at 2.5.3 (**no published npm/GHSA advisory**, so it does not block this hotfix — but the deprecation points to the `v3-lts` "security-fixes-only" line, so a v3-lts/v4 migration is a **security-relevant money-path follow-up to prioritise**, deferred as a separate slice to avoid behaviour drift in money-handling code — not cosmetic). Post-fix `npm audit --omit=dev` = **0 vulnerabilities**. Added `agent/test/wallet.test.js` (offline wallet guard/failure paths + cashu-ts codec regression) and `agent/test/fastify-v5-api.test.js` (cors preflight + rate-limit route-config contract); agent `node --test` now 30/30, rate-limit smoke green, root build green, ops regression 13+9 green. **NOT yet deployed** — v0.2.26 remains the live server version until an operator re-runs `install-agent.sh`. Base-path awareness + Ollama fallback landed in v0.2.6. Docs hygiene sweep across v0.2.7 → v0.2.12. **v0.2.13 shipped CONT-HEALTH-1**: dashboard now has a live Provider card polling `/api/health/models` every 20s. **v0.2.14 shipped SUITE-VPS-READY-1 rate-limit slice** — the public auth surface is behind `@fastify/rate-limit@9` per-IP with 429 + `Retry-After`; the in-memory challenges Map is capped and LRU-by-expiry evicted with structured `[auth]` logs, all prefix-only. **v0.2.26 shipped SUITE-VPS-READY-2: agent deploy tooling + first-touch admin claim.** New `ops/` assets let an operator run the agent as a hardened standalone systemd service: `ops/systemd/torii-continuum-agent.service` (locked `continuum` user, `ProtectSystem=strict`, only `memory/` + `config.yaml` writable), `ops/nginx/torii-api.conf` (same-origin `/api/` proxy + edge `limit_req`), `ops/install-agent.sh` (idempotent strict-bash installer: locked user, `rsync` without clobbering state, `npm ci --omit=dev`, one-time config gen with a fresh `openssl` session_secret, config validation, nginx `-t`-before-reload wiring, bounded `/api/health` proof), and a refreshed `ops/README.md` runbook. `agent/core/auth.mjs` gained first-touch admin bootstrap: with an empty `admin_npub` the first valid NIP-07 verifier atomically claims admin (npub persisted to `config.yaml` 0600 via injected `persistAdminNpub` in `config.mjs`), race-safe and fail-closed; `/api/health` now reports `admin_claimed`. **The agent is NOT deployed by this work** — running `install-agent.sh` on a server is a separate operator step. **CONT-HEALTH-2 (real Cashu wallet health probe) shipped in v0.2.47-alpha** (see the Active focus above): a non-mutating `wallet.health()` + admin-gated `GET /api/wallet/health` + a Provider-card row, verifying mint identity/NUT-07 support and reporting balance without touching proofs or wallet state.

**Active focus:** **v0.2.51-alpha ships ONBOARD-UI-1: clearer signer-extension login copy + stronger numbered-step wayfinding in the onboarding preview** (onboarding preview → **v0.1.21-preview**, `preview-assets/onboarding-v0.1.21/`; **no app/agent runtime code changed** — root + agent package.json bumped 0.2.50→0.2.51-alpha for release discipline). Closes GitHub issue **#57**. Design-review mockup only (vendored under `preview-assets/`, never bundled by `vite build`). **(1) Login button copy:** the Step-1 primary CTA now reads exactly **"Sign in with browser extension"** (was "Sign with Plebeian Signer"); the lede names the signer context plainly ("a browser extension such as Plebeian Signer, nos2x or Alby") and the ghost button reads "Use a remote signer instead"; the no-signer-found message became "No browser extension signer found — install a NIP-07 extension (e.g. Plebeian Signer, nos2x or Alby)". No tracking, no CDN, no key material added — `onboarding-client.js` routing/Routstr logic is byte-identical to v0.1.20 except that one message string (`CONTINUUM_HOME`, `data-claimed`, `renderSuccessAdvance` and the duplicate-pay guards intact). **(2) Numbered-step wayfinding (`shared.css`):** completed steps are now **filled** (bronze `--bronze` numeral chip, dark numeral, weight 600); the current step gets a **brighter** amber-bright 3px ring + glow + weight 600 (was a 2px `--amber` ring), preserving the 36px chip size. Non-colour cues (size + fill + ring) keep it colour-blind safe; added an explicit `.step-dot:focus-visible` amber-bright outline for keyboard nav; **no new animation** (reduced-motion safe). WCAG audit: completed numeral on bronze **5.78:1** (AA small text), current numeral on amber **10.75:1**, current/focus ring vs bar **14.15:1** (≥3 UI). **(3) Preserved:** claimed-Routstr simplified terminal state + successful routing to app/login/dashboard unchanged (verified against v0.1.20 diff). **(4) Doc fix:** the stale onboarding `README.md` version header (`0.1.19-preview`) corrected to `0.1.21-preview`; top-level `preview-assets/README.md` "Current preview" pointer advanced to v0.1.21 and the deploy example rewritten for the new dir. **Tests:** the v0.1.21 test suite asserts the new button copy (and `not.toContain('Sign with Plebeian Signer')`), the browser-extension/Plebeian-Signer step-1 context, the new signer-not-found message + NIP-07 mention, the step-dot wayfinding states (bronze fill + dark numeral, amber-bright 3px ring + 36px width, focus-visible outline, no animation), and the README version header. Verification (Node 20.x): root `vitest run` **1032/1032** (19 files, +160 from the new version dir), `npm run build` clean (0.2.51-alpha; `dist/` free of `preview-assets/`). Static QA artifact (no headed browser in sandbox): desktop + mobile step strips in all states + programmatic WCAG audit. Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.50-alpha ships OPS-DEPLOY-2: safe unattended deployment via a server-side pull** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#54**. Problem: local-device automation cannot make outbound SSH and interactive VPS SSH prompts for a password, so a push deploy is impossible; the existing role's redeploy still needs a human to run `ansible-playbook` on the box. Design — a small, root-owned wrapper the VPS runs on a systemd timer that **delegates to the already-hardened `continuum` role** (never reimplements state backup, the atomic staging→swap cutover, the version-asserting health gate, rescue rollback, or cleanup). New files (all `ops/`): **`deploy-unattended.sh`** (→ `/usr/local/sbin/torii-continuum-deploy`): strict `^v<semver>(-prerelease)?$` tag grammar rejecting branches/SHAs/shell+YAML metacharacters (proven against injection strings), optional fail-closed allowlist, optional `git tag -v` signed-tag gate (`CONTINUUM_REQUIRE_SIGNED_TAGS=1`), **idempotent no-op** when live `/api/health` already serves the tag, fresh per-tag clone under `/opt/deploy`, renders localhost vault-free inventory (`continuum_version`+`torii_domain` only), runs `ansible-playbook --tags continuum`, **independently re-verifies** the live version on top of the role's gate, prunes old releases (keeps newest N, never the live one); `flock`-guarded, `set -euo pipefail`, refuses non-root, sourceable for in-process tests. **`systemd/torii-continuum-deploy.{service,timer}`** (oneshot + ~5min timer; no inbound SSH/ports). **`sudoers/torii-continuum-deploy.example`** (OPTIONAL remote/CI trigger: locked non-login `toriideploy` principal, `NOPASSWD` on **exactly the wrapper, no args** — never general passwordless sudo). **`deploy-bootstrap.sh`** (idempotent fail-closed one-time installer: wrapper 0755 root:root, units, root-only `0600` pin-file skeleton it never clobbers, optional principal+sudoers validated with `visudo -cf`, optional dedicated ed25519 key + host-key-pinning guidance). **After bootstrap, releasing a version is a single root-only edit of `/etc/torii/continuum-deploy.conf` (`CONTINUUM_TARGET_TAG=`); a bad release fails closed and self-reverts via the role's rollback — no SSH in the deploy loop.** Security: no secret read/written/logged (vault-free path preserves `config.yaml`/`session_secret`/funded key byte-for-byte); no auth weakened; no general sudo; no ports opened. Tests: new `ops/test/deploy-unattended.test.sh` **55/55** (tag grammar incl. injection, version gate, allowlist fail-closed, version extraction, localhost/vault-free inventory, prune-keeps-live, wrapper fail-closed guards, oneshot/timer wiring, scoped-sudo/no-`NOPASSWD: ALL`, bootstrap 0755/0600/visudo/locked-principal/host-key-pin/no-clobber); existing `deploy-restart` **25/25**, root `vitest run` **872/872**, `npm run build` clean (0.2.50-alpha). Both `package.json` bumped 0.2.49→0.2.50-alpha. Sole attribution Chiefmonkey. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.49-alpha ships CONT-LIVE-UI-1: surface live provider/wallet/kanban data in the UI** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#52**. Live v0.2.48-alpha authenticated acceptance found three UI regressions where correct backend data never reached the operator. **(1) Dashboard Providers panel blank** despite "Polling every 20s": `ProviderCard()` (`src/views/dashboard.js`) fired the first `tickProvider(body)` synchronously *before* the card was appended, so `body.isConnected` was false, the tick bailed (and cleared its own poll), and the body stayed empty until the first 20s interval — fixed with a synchronous "Checking providers…" placeholder + a deferred first tick via `queueMicrotask(() => tickProvider(body))` (card is in the DOM by then); interval unchanged. **(2) Routstr green `connected` pill but em-dash Cashu balance:** field-name mismatch — `GET /api/wallet/balance` returns `{ total_sats, ... }` but the poll read `r.data.balance_sats` → `undefined` → `formatSats(undefined)` → `—`, while still setting `connected:true` and persisting `cashuBalanceSats:undefined` (so the em dash stuck across re-renders); the top-up modal shared the bug (`received_sats`/`balance_sats`; the endpoint returns `added_sats` only). Fixed with a pure exported `readBalanceSats()` (prefers `total_sats`, `balance_sats` fallback, returns `null` — not `0` — when absent), an in-place balance-number update (`balanceNumEl.textContent`, no page tear), and a receive modal that reads `added_sats` then re-reads the authoritative balance. A configured-but-empty wallet now shows an honest `0 sats` (balance sums to 0 across mints) instead of `—`; a non-zero balance requires one or more `cashu.mints` on the agent AND redeemed proofs via top-up (`/api/wallet/receive`). **(3) Kanban header total + per-column counts excluded imported read-only cards:** `totalCards` and each `col-count` summed only native `cardsFor`, never `importedCardsFor`, so a board/column of only imported work read `0`; fixed to add the imported cards, with the distribution primitive extracted as a pure exported `filterImportedForColumn()`. **Security:** fail-closed posture unchanged — every wallet/health/source route stays `requireAdmin`, no proofs/tokens/secrets/full endpoints are rendered, the wallet probe stays a read-only NUT-07/identity check; these are pure client-side field/ordering fixes with no new network or storage surface. **Tests:** new `src/views/routstr.test.js` (8), `src/views/board-counts.test.js` (7), `src/views/dashboard-structure.test.js` (4). Verification (Node 20.x): root `vitest run` **872/872** (18 files, +19), agent `node --test` **168/168** (unchanged), `npm run build` clean (0.2.49-alpha), `npm audit --omit=dev` root prod **0** / agent prod **0**; root full audit **5** dev-only (vite/vitest/esbuild). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.48-alpha ships OPS-DEPLOY-1: restart the promoted agent + verify the deployed version** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes GitHub issue **#49**. **Root cause it fixes (diagnosed from the live 0.2.43-vs-0.2.47 incident):** the `continuum-agent` systemd unit file is version-independent, so a code-only redeploy atomically promotes a new release tree WITHOUT changing the unit; the role's `restart continuum-agent` handler is never notified and the old `state: started` task is a no-op against the already-running PID. The swap changed the code on disk but the live process kept serving the PREVIOUS release (stale `/api/health` version, new `/api/wallet/health` + `/api/projects/:slug/sources` routes 404). The old health gate accepted any HTTP 200, so the broken deploy passed. **Fix (`ops/ansible/roles/continuum/tasks/main.yml`):** the unit-install task now `register:`s `continuum_unit_install`; after the JIT smoke test the role (1) runs `meta: flush_handlers` so any unit-file-change restart fires BEFORE readiness (handlers run in definition order — `reload systemd` daemon-reload then `restart continuum-agent`), (2) explicitly `state: restarted` **only when `continuum_promote.changed` AND NOT `continuum_unit_install.changed`** (the exact code-only case the handler misses; the guard prevents a double restart when the unit itself changed — that path is the handler's), then (3) an idempotent enabled+`state: started` ensure. Readiness is strengthened: `uri` now uses `return_content: true` and the `until` requires HTTP 200 AND `agent_health.json.version == (continuum_version | regex_replace('^v',''))`, so a stale process left running by a missed restart reports the OLD version and FAILS readiness, tripping the existing block/rescue rollback instead of passing a broken deploy. Exactly one restart happens per real deploy; the health gate always inspects the post-restart process. Everything else is preserved verbatim: block/rescue rollback (quarantine restore + webroot rollback + standalone re-enable), fail-closed backup, config/state verbatim copy (`session_secret`/funded key never rotated), adoption modes, V8 JIT smoke test, standalone-unit disabling, and `no_log` secret hygiene. The unit template stays version-independent (that is WHY the explicit restart is needed) — a regression test locks that assumption. **Tests:** new `ops/test/deploy-restart.test.sh` (**25** hermetic assertions): restart-before-readiness wiring (register, flush_handlers, guarded `state: restarted`, idempotent ensure), the version-asserting health gate, strict ordering (flush < restart < ensure < health) + handler daemon-reload-before-restart ordering, the version-independent-unit lock, rescue/rollback/webroot/standalone/JIT/config-copy survival, `no_log` retention + no secret in any printed `msg`, and pure version-equality cases proving a **stale 0.2.43-alpha live version FAILS** readiness while the **correct 0.2.48-alpha PASSES** (leading `v` stripped, empty version fails). **Full verification (Node 20.20.1 sandbox; agent code unchanged):** ops shell `deploy-restart` **25/25**, `continuum-adopt` **219/219**, `installer-preflight` **18/18**, `installer-shared-parent` **10/10**, `installer-signal` **9/9**, `nginx-install` **13/13**; root `vitest run` **853/853**; agent `node --test` **168/168**; `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz, version stamps 0.2.48-alpha); `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows the same **5 dev-only** findings (`@vitest/mocker`/`esbuild`/`vite`/`vite-node`/`vitest` toolchain, not shipped in `dist/`; breaking major-vite fix deferred). Lockfile top-level `version` fields left as-is (prior releases never bumped them; `npm ci` validates the dependency tree, not that field — build re-ran clean). No hostnames/devices/secrets in code, tests, or docs. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.47-alpha ships KANBAN-SYNC + wallet health** (agent + frontend; onboarding preview stays **v0.1.20-preview**; both package.json + lockfiles bumped `0.2.46 → 0.2.47`). Closes GitHub issues **#44/#45/#46/#47**. **#46 (read-only project-source adapters, agent):** new `agent/core/project-sources.mjs` + `agent/lib/markdown-todo.mjs` import from local Markdown todo files and public GitHub issues, both **strictly allowlisted, fail-closed**: local paths only under a configured `local_root` with traversal + symlink-escape rejected and file size bounded; GitHub only for `allow_github` `owner/repo` pairs, no credential needed, base URL pinned (`_ghPin`, `redirect:'error'`) as an SSRF guard, with bounded response bytes / `max_issues` (200) / `max_pages` / timeout / rate-limit detection. Records normalize to a stable-fingerprint shape (`{fingerprint, project, source{type,ref,label,id}, title, description, status, priority, labels[], url, readOnly:true, createdAt, updatedAt}`), `dedupeRecords` collapses repeats, and the last valid snapshot is cached per project + **retained on partial/offline/error**. Nothing logs content/tokens/full identifiers. New `project_sources:` config block validated in `loadConfig` (enabled-but-unallowlisted / markdown-without-`local_root` / malformed `allow_github` / unknown type all refuse boot); new admin-gated `GET /api/projects/:slug/sources` + rate-limited `POST /api/projects/:slug/sources/refresh`. **#45 (imported cards on the Kanban, frontend):** `src/views/board.js` keeps imported records in an **ephemeral per-slug `importState` Map, never written to the board store**, so manual cards are never overwritten and repeated refreshes never duplicate (keyed by fingerprint). Each renders as a `board-card imported` container — not draggable, no move/edit controls, source badge + priority/label pills, read-only `imported-badge`, `Read-only imported card:` aria-label, safe external link (`target:'_blank'`, `rel:'noopener noreferrer'`); `renderSyncBar` adds source + status filters, a manual Refresh button, a last-sync line, and stale/partial/error `sync-banner`s. New client fns `walletHealth`/`projectSources`/`refreshProjectSources` in `src/data/agent.js`. Mounted `/continuum/` base path, accessibility, mobile, dark styling, and login/dashboard/about behavior all preserved. **#47 (real Cashu wallet health, agent + frontend):** admin-gated `GET /api/wallet/health` backed by a **non-mutating** `wallet.health()` using cashu-ts (^3.7.1) v3 read primitives (`loadMint()` + `getMintInfo()` identity getters + `.isSupported(7)` NUT-07 + `groupProofsByState`) — no mint/melt/swap, serialized on-disk wallet untouched; honest disabled/healthy/degraded/unreachable states + sanitized reasons; never exposes tokens/proofs/secrets; `src/views/dashboard.js` adds a wallet-health row to the Provider card (polled alongside `healthModels()` via `Promise.all`). **#44 (docs):** todo/progress/handoff/README refreshed through v0.2.46 (stale "next-action" claims removed) then this release recorded with evidence; examples generic/configurable, no real hostnames/secrets, no cross-project queue contamination. Tests: new agent `project-sources.test.js` **29** / `markdown-todo.test.js` **12** / `project-sources-config.test.js` **9** (subprocess) / `wallet-health.test.js` **14**; extended `src/data/agent.test.js` **23** + `src/views/board-structure.test.js` **14**. **Root `vitest run` 853/853, agent `node --test` 168/168**, `npm run build` clean (index.html 1.31 kB, CSS 39.56 kB/7.30 gz, JS 81.15 kB/25.20 gz), `npm audit` **0 vulns** on root prod (`--omit=dev`), agent prod, and agent full; root **full** audit shows **5 dev-only** findings (esbuild/vite/vitest toolchain, not shipped in `dist/`; fix is a breaking major-vite bump, deferred). Migration is additive/backward-compatible — `project_sources` defaults disabled; wallet probe reads existing state without mutation. Code + PR only — **not merged, tagged, or deployed.** **PR #48 review remediation (same branch, version held at v0.2.47-alpha):** a blocking review was addressed in place — **M1** partial-refresh data loss fixed (`refresh()` retains failed sources' last-valid records per-source via a `bySource` Map, replaces only healthy ones, preserves the full snapshot + `syncedAt` on total failure, honest `partial`/`stale`/`retained`); **M2** dedupe title-collapse scoped to Markdown-only so distinct/cross-repo same-title GitHub issues survive (native `fingerprint` wins); **M3** `mintHealth` extracted+exported and covered with a mocked cashu-ts v3 wallet (healthy/degraded/unreachable/timeout/no-NUT-07/spent-pending/no-mutation/no-leak); plus **L4** `GET /api/wallet/health` rate-limited (`wallet_health_per_min`, default 6) + 15 s server cache, **L5** `insertionIndex` ignores imported cards, **L6** absolute `local_root` enforced, **L7** numeric bounds hard-ceilinged (`CEILINGS`), **L8** dead `cache_ttl_sec` removed, **L9** `ok: anyOk` simplified, **L10** empty sync bar suppressed, **N11/N12** doc wording corrected (read-only identity/NUT-07 probe, source/mocked-unit guards not live-browser tests). Re-verified: agent 168/168, root 853/853, build clean, audits unchanged. No ops files touched. Prior slice: **v0.2.46-alpha shipped LOGIN-FIX: sidebar/demo Login "Could not reach agent: Bad Request"** (frontend-only; **no agent code changed**, agent stays **v0.2.43-alpha**, onboarding preview stays **v0.1.20-preview**). In the demo shell (`#/projects`), clicking the bottom-left sidebar **Login** with Plebeian Signer installed failed with *"Could not reach agent: Bad Request"*. **Root cause:** the SPA↔agent client `req()` in `src/data/agent.js` always sent `Content-Type: application/json`, even for the **bodyless** `POST /api/auth/challenge`; Fastify v5 rejects an empty body carrying that content-type with **400 `FST_ERR_CTP_EMPTY_JSON_BODY`** (`error:"Bad Request"`) before the handler runs — so the challenge never issued. The onboarding client's `postJson` was already hardened against exactly this (v0.2.32-alpha, pinned in `agent/test/fastify-v5-api.test.js`); the SPA client had **drifted** and never got the fix. **Fix:** `req()` now declares the JSON content-type **only when a body is actually sent** (mirrors onboarding `postJson`; challenge POST now 200s). Added `errorReason(json,status)` (pure, exported) to surface the specific Fastify `message` over the generic label (200-char cap, never stack traces). Both login entry points (`src/views/login.js` view + `src/shell.js` sidebar button) already share the single `startLogin()` in `src/auth.js` — no duplicate flow to merge; the drift was in the shared HTTP client. Privacy/keys unchanged (nsec stays in the extension; agent still verifies id+sig+admin-pubkey+challenge). Tests: new regression cases in `src/data/agent.test.js` (mock NIP-07 signer + fetch; assert challenge POST has no content-type/no body, verify POST sends exact `{event}` + JSON content-type + adopts token, full `errorReason` map). `vitest run` **843/843**, agent `node --test` **104/104**, ops shell suites green, `npm run build` clean. Root version → **v0.2.46-alpha**; agent NOT bumped (its empty-body 400 is correct standard Fastify behaviour — no protocol change). **Kanban discoverability (reported):** board is at `#/projects/:slug/board`, reached via the **Overview · Board** switcher after opening a project; all project routes are demo-browsable (only `/dashboard` is auth-gated), so the Board link **is** visible in demo mode — just not surfaced on the `#/projects` index cards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.44-alpha ships APP-ROUTING: application-first root (login + dashboard), sales page isolated to `#/about`** (frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**). Fulfils the operator ask *"separate out that sales page from the root of continuum and replace it with a login page and the dashboard."* The root (`/continuum/`) is now the application, never the marketing page. A pure, DOM-free guard layer (`src/nav-guard.js`: `rootTarget`/`guardRedirect`/`isProtectedPattern`, `PROTECTED_PATTERNS=['/dashboard']`) decides routing: **logged in → root redirects to `#/dashboard`; logged out → a branded Amber login page (`src/views/login.js`) renders in place at root** (shared `toriiSvg` mark, full-bleed `landing-mode`, drives the existing NIP-07/Plebeian-Signer `startLogin()` — no password auth, no key material in the DOM; small non-primary links to `#/about` + the demo shell). The former marketing view is reframed/exported as `renderAbout` in `src/views/landing.js` and lives only at **`#/about`** — never the root, never an onboarding-completion target. `#/dashboard` is guarded: a logged-out visitor (incl. refresh / deep link, since guards run on every router resolve including the initial one) is bounced to login at root; because root renders login **in place** (never itself bouncing when logged out), `protected → root → login` is terminal — **no redirect loop**. A session drop while on a protected view (sign-out / 401 expiry) reactively returns to login via the `continuum:session-changed` listener. `adoptOnboardingSession()` still runs before first render, so a freshly onboarded operator with a live `torii.session` lands authenticated on the dashboard (no login bounce); demo views (`#/projects`/`#/marketplace`/`#/routstr`) + all Amber nav preserved. No Google/gstatic/Fontshare/CDN/analytics added — local/system assets + inline SVG only. New `test/app-routing-auth.test.js` (**20** tests): unauth-root→login, auth-root→dashboard, dashboard protection, no-loop, `#/about`/demo isolation, onboarding adoption→authenticated root, expired/invalid→login, plus source-structure locks on `main.js`/views. Browser QA: no headed browser / no jsdom in the sandbox, so the contract is proven by the exhaustive pure guard tests + the source locks + the rebuilding no-external-request scan (guards run on the initial resolve = refresh/deep-link). Root `package.json` → **v0.2.44-alpha**. **Ops addendum in the same PR (v0.2.44-alpha):** a live v0.2.43 rerun hit one more exact bug — `group_vars/all.yml.example` shipped `torii_root_app: launcher`, but the installed CLI/API accepts `torii set-root <registered-name|none>` and returned **404 `{error: app_not_installed, name: launcher}`** (the Torii Suite launcher at `/` is the sentinel `none`, not a registered app), rolling the deploy back; rollback worked and the operator reran with `-e torii_root_app=none`. Fix: the example default is now **`torii_root_app: "none"`**, and the continuum role no longer calls `set-root` raw — it routes through **`ops/lib/continuum-adopt.sh set-root-safe`**, which `normalize_root_app`-maps legacy/empty/`launcher` (+ `torii`/`base`/`root`/`home`/`homepage`/`default`) → `none`, sources the admin token from `/opt/torii/env` silently (never printed), **skips** the call when `torii get-root` already matches, and returns 0 for the default so a normal install can never trip the rescue rollback. An explicit **registered** app name still passes through unchanged; pointing the root at `continuum` emits a discouraged-choice warning but is honoured when set deliberately — **Continuum is never made the root by its own installer.** `ops/README.md` gains a "Root-app selector correction (v0.2.44-alpha)" subsection; `continuum-adopt.test.sh` grew **188 → 219** (mock `torii` CLI reproducing the exact live 404; `launcher`→`none` normalization incl. synonyms/whitespace/case; default skip when already none + reset when not; explicit registered app honoured; continuum-warn; no-token-leak; example/role anti-drift greps). No-Google/no-CDN guarantees untouched. Tests: `vitest run` **795/795** (775 prior + 20), agent `node --test` **104/104**, `continuum-adopt.test.sh` **219** + ops regressions 18/10/9/13, root build clean (`dist/` free of external loading vectors, only the inert Plebeian-Signer store anchor remains), `npm audit --omit=dev` 0 vulns (root + agent). Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.43-alpha ships OPS-HARDENING: encode three live-discovered cutover corrections** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). The v0.2.42-alpha cutover succeeded manually on the live box; three environment realities had to be fixed by hand and are now encoded permanently. **(1) Torii CLI `register` uses flags, not positionals** — the installed CLI is `torii register <name> [--display …] [--desc …] [--version …]`; the old positional form failed with `unknown flag: Continuum`. The role now calls `torii register {{ continuum_register_name }} --display "Continuum" --desc "App builder + agent" --version <bare-semver>` (bare version via `continuum_version | regex_replace('^v','')`), with new `continuum_register_*` defaults. **(2) `MemoryDenyWriteExecute` must be `no`** — Node 22's V8 JITs with W^X memory; `=yes` makes the kernel refuse the mprotect and V8 dies on startup (fatal `SetPermissions` / errno 12 / `SIGTRAP`). `continuum-agent.service.j2` now sets `MemoryDenyWriteExecute=no` with an inline rationale and keeps every other compatible directive (`NoNewPrivileges`, `Protect*`, `RestrictNamespaces`, `LockPersonality`, empty `CapabilityBoundingSet`). The role runs a **Node V8 JIT smoke test** under the rendered MDWE value via a transient `systemd-run` scope before starting the real service. **(3) nginx cannot traverse `/home/continuum`** (0750 home + `ProtectHome` → HTTP 500) — the build is now published to a **public webroot `/var/www/torii/continuum`** (root-owned, dirs 0755 / files 0644) via new `deploy_webroot` (atomic staged swap, prior webroot kept as timestamped backup); only the static bundle is copied out, source + encrypted state stay private under `/home/continuum/app`. A **single** `location /continuum/` prefix alias serves both the SPA entry and the hashed `assets/*` bundle (no regex-alias, no `/home`). The cutover `rescue` now also rolls the webroot back (`rollback_webroot`) + reloads nginx on failure. Re-running against the now-live v0.2.42 Ansible layout is idempotent (re-detects `existing-ansible`, preserves config, never rotates `session_secret`/touches the funded key). `ops/test/continuum-adopt.test.sh` grew **117 → 188** assertions (webroot deploy/rollback + perms + dangerous-target refusal; mock Torii CLI parser reproducing `unknown flag: Continuum` vs flag-form success; systemd MDWE=no contract + retained hardening; Node V8 WASM smoke; nginx section is now a **no-nested-assets regression lock**; existing-ansible upgrade idempotency). New `ops/README.md` v0.2.43 subsection + upgrade command. Root + agent version → v0.2.43-alpha (+ both lockfiles). Tests: adopt **188/188**, ops regressions green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. **AMENDMENT (pre-merge, same PR #40) — frontend zero-external-request + asset-404 fix:** (4) removed all production Google (`fonts.googleapis.com`/`fonts.gstatic.com`) **and** Fontshare (`api.fontshare.com`) preconnects/stylesheets from `index.html`; typography is now a **deliberate local system font stack** in `src/styles/theme.css`, so production makes **zero external requests** (no fonts downloaded/vendored). (5) the live black page was the nested `location /continuum/assets/` alias+`try_files` returning 404 for hashed `.js`/`.css`; both nginx templates now use a **single parent alias** only. (6) `ops/README.md` upgrade steps corrected to the real live paths (`/opt/deploy/torii-continuum-v0.2.42-alpha` checkout, app at `/home/continuum/app`; prefer a fresh version-specific public clone of the final tag) — the old `/opt/torii/src/torii-continuum` never existed. New frontend tests: `test/no-external-cdn.test.js` (builds `dist/`, asserts index + assets carry no banned CDN/font host and no external loading vector; same-origin `/continuum/api` allowed) and `test/nginx-continuum-routing.test.js` (HTTP server modelling nginx `alias`+`try_files`, using the **real hashed filenames** from `dist/index.html`, proves `/continuum/` index + hashed `.js`/`.css` = 200 and deep-route SPA fallback). `vitest run` now **775/775**. Browser QA: no headed browser in the sandbox — verified via the build + HTTP-200 routing + same-origin-only fetch scan; the app is a hash-router SPA (`#/`,`#/projects`,`#/marketplace`,`#/routstr`,`#/dashboard`) so the server contract is fully exercised, and the live box already rendered the Amber dashboard with zero console/network errors after the manual asset-route fix. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.42-alpha ships OPS-HARDENING: transactional adoption/cutover + partial-adoption recovery** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the live v0.2.41-alpha adoption failure: the role migrated live state INTO `/home/continuum/app/agent` and only THEN ran `ansible.builtin.git` into `/home/continuum/app` — cloning into a dir already populated with runtime state. It aborted, leaving the **original standalone layout intact** AND a **partial, non-git `/home/continuum/app`** holding a copy of the state; the untouched `torii-continuum-agent.service` was restored and health is good. **Fix (`ops/lib/continuum-adopt.sh` + `roles/continuum/tasks/main.yml`):** `layout_detect` gains a 3rd `app_dir` arg and a new **`partial-adoption`** mode (agent has state but `app/.git` absent while standalone is still present — a real git-backed Ansible install still wins; a non-git app dir carrying state is recovered, never built on top of). New `authoritative_state_dir` picks the untouched standalone as the live source over the partial copy. New `stage_reset`/`promote_release`/`rollback_release`: the `git checkout` + `npm ci` + `vite build` run in a **clean staging dir** (`/home/continuum/app.staging`) while the **old unit keeps serving**; authoritative state is copied in **only after a successful build**; the old unit is stopped only immediately before an **atomic same-filesystem swap** into `/home/continuum/app`, quarantining (never deleting) any pre-existing app dir to `app.quarantine-<UTC>`. The `block`/`rescue` cutover stops the new unit on any promotion/`/api/health` failure, rolls an existing-Ansible tree back from quarantine (or re-enables the standalone for adopt/partial), and prints backup + quarantine paths with an exact **secret-free** recovery command. Re-running after the exact observed failure is idempotent. `ops/test/continuum-adopt.test.sh` grew to **117** hermetic assertions (3-arg detect incl. partial-adoption + absent-app-dir; `authoritative_state_dir`; `stage_reset` refuses `/ /home /root /opt /opt/torii`; `promote_release` clean/quarantine/refuse-existing-quarantine/idempotent; `rollback_release` restore/refuse-without-failed-dir/no-quarantine; end-to-end partial-adoption recovery proving byte-for-byte config equality + funded-key retention + quarantine preservation + untouched standalone; v0.2.42 anti-drift greps that clone/build target staging, state is staged before promotion, promotion+rescue are gated on a swap fact, and the old unit stops only at cutover). New "Transactional cutover + partial-adoption recovery (v0.2.42-alpha)" subsection in `ops/README.md` with the exact recovery invocation for the current partial-adoption VPS (same vault-free localhost invocation, `continuum_version: v0.2.42-alpha`). Root + agent version → v0.2.42-alpha (+ both lockfiles). Tests this slice: adopt **117/117**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.41-alpha ships OPS-HARDENING: vault-free standalone adoption / redeploy** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Closes the blocker left by v0.2.40-alpha: on `adopt-standalone`/`existing-ansible` the live `config.yaml` is preserved byte-for-byte, but the preserve path still rendered a `.config.candidate.yaml` from `config.yaml.j2` (which references `{{ admin_npub }}`/`{{ session_secret }}`) for the drift diagnostic — so a truly vault-free run (no `group_vars/vault.yml`, no vault password) died with an undefined-variable error before finishing adoption. **Fix (in `roles/continuum/tasks/main.yml`):** a new `continuum_vault_vars_present` fact is computed defensively via the `default('')` filter (an undefined var yields `false`, never raises); the candidate render + `config-drift` + candidate removal + "differ" warning are all guarded behind `continuum_vault_vars_present | bool` and SKIP entirely when the vault vars are absent (a non-secret debug notes the diagnostic was skipped). Adoption still preserves the live config byte-for-byte, so the funded Routstr key stays decryptable. A fresh install or an explicit `continuum_allow_config_rotation=true` rotation still needs the vault vars, and their absence in those modes **fails closed** with a clear, non-secret `ansible.builtin.fail` *before* any state is written. `ops/test/continuum-adopt.test.sh` grew to **66** hermetic assertions (vault-free adopt end-to-end incl. byte-for-byte config preservation + funded-key migration + no-secret-in-output; existing-ansible redeploy; fresh missing-vars fail-closed; explicit-rotation missing-vars fail-closed; presence truth table; anti-drift greps for the new guards). New "Vault-free adoption / redeploy (v0.2.41-alpha)" subsection in `ops/README.md` gives the exact secret-free localhost invocation (inventory `torii→localhost ansible_connection: local`, `group_vars/all.yml` with `torii_domain`/`continuum_version`, `ansible-playbook -i inventory.yml site.yml --tags continuum` with NO `--ask-vault-pass`). Operator handles NO secrets and needs NO vault password for adoption. Tests this slice: adopt suite **66/66**, ops regressions 18/10/9/13 green, `bash -n` clean, agent `node --test` + root build/vitest + `npm audit --omit=dev` re-run once green. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.40-alpha ships OPS-HARDENING: safe standalone→Ansible adoption, no config clobber, fail-closed backups** (ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**). Fixes the dangerous deployment mismatch that blocked the live v0.2.39-alpha rollout: the continuum Ansible role cloned to `/home/continuum/app` and **re-rendered `config.yaml`/`session_secret` unconditionally on every run**, while the live standalone box uses `/opt/torii/continuum-agent` + unit `torii-continuum-agent.service` on port 8787 — so running the old role over that box would have overwritten the session_secret (orphaning the funded Routstr key, encrypted at rest under a key derived from it) and/or double-bound 8787. **Fix:** the risky detect/backup/migrate/config-decision logic is factored into a sourceable, unit-tested lib `ops/lib/continuum-adopt.sh`, and `roles/continuum/tasks/main.yml` is now a thin guarded orchestrator that (1) **detects** layout (`fresh`/`adopt-standalone`/`existing-ansible`; an existing Ansible layout always wins); (2) takes a **fail-closed** timestamped **root-only 0700 backup** of `config.yaml`+`memory/`+`ciphertexts/`+`pending/` from both layouts under `/root/continuum-backup-<UTC>/` before any mutation (no backup ⇒ abort); (3) on adopt **stops+disables** the standalone unit (frees 8787) and **migrates config+state verbatim** — `session_secret` copied byte-for-byte, never regenerated, migration idempotent; (4) **never clobbers** an existing config on routine deploy — renders from vault ONLY on a fresh install or explicit `-e continuum_allow_config_rotation=true` (off by default), and on a preserve runs a one-way drift check that prints only `same`/`differ` (never a value) and surfaces a safe "rotation pending" notice; (5) wraps the service+nginx cutover in a `block`/`rescue` that on failure prints the backup path + exact recovery command. `git force:true` resets only tracked files, and all agent state is gitignored, so a re-checkout/build can't delete encrypted state. New `roles/continuum/defaults/main.yml` holds the safe tunables (`continuum_standalone_dir`, `continuum_standalone_service`, `continuum_backup_root`, `continuum_allow_config_rotation: false`); secret-touching tasks carry `no_log: true`. Pinned by `ops/test/continuum-adopt.test.sh` (**44** hermetic assertions: detection precedence, fail-closed backup, verbatim+idempotent migration, config-action matrix, no-secret drift, permissions, anti-drift greps). New "Adopting a standalone install with Ansible" runbook in `ops/README.md`. **Operator handles NO secrets** at any point; deploy is the normal `ansible-playbook … --tags continuum`. Tests this slice: adopt suite **44/44**; unchanged app/agent suites not re-run beyond the required build/audit. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.39-alpha ships ONBOARDING-GATING + APP-MOUNT: real claimed-state Step-3 gating, dup-pay race closed, completion lands on the actual app dashboard** (onboarding preview → **v0.1.20-preview**, `preview-assets/onboarding-v0.1.20/`). Two live defects on the v0.1.19/v0.2.38 stack plus the app-mount follow-through. **User-funded Routstr key, all encrypted agent memory, and prior payment state preserved — no invoice created/paid/re-submitted, no `memory/**` touched.** **(A) Claimed-state gating (root cause):** a verified funded key still rendered both setup cards, key-entry/reveal, *Verify & connect* and *Request an invoice* — a CSS cascade defect, not logic: author `display` rules (`.btn-primary{inline-flex}`, `.wallet-choices/.routstr-paths{flex}`, `.choice-card{grid}`) outrank the UA `[hidden]{display:none}`, so `el.hidden=true` visually no-op'd. Fix: canonical `[hidden]{display:none !important}` in `shared.css`; the status-only resume path funnels through `renderSuccessAdvance` via a single `showClaimed` helper (replaces `showConnected`); the panel carries `data-claimed="1"` as single source of truth, leaving one green summary + exactly one standalone Continue. **Dup-pay race closed:** `data-claimed` checked as the first line of quote/key-connect/pay-confirm handlers (pay button also permanently disabled once claimed) — a second invoice/payment is structurally impossible in claimed state. **(B) App mount:** `CONTINUUM_HOME` `/continuum/` → `/continuum/#/dashboard` (+ curtain anchor) so completion opens the multi-view Amber SPA dashboard, not the marketing root; open-redirect guard preserved. **Session handoff:** `adoptOnboardingSession()` (in `src/data/agent.js`, called once in `main.js` before `initStore()`) fails closed if the SPA already has a live session, else adopts the `localStorage['torii.session']` envelope token into `continuum.session.v1` only if `tokenLooksLive` (shape + unexpired), so a freshly onboarded user is not bounced to sales/login; `deriveSameOriginBase` makes `agentUrl()` fall back to same-origin `/continuum` under the mount (no CDN/third-party origin). **No separate torii-suite PR required** — the `/continuum/` alias + subpath refresh + `/continuum/api/*` proxy + SPA build + agent install + nginx fragment + launcher registration are all in this repo's `ops/ansible`; the root launcher is never overwritten. **Safe idempotent deploy (operator step, NOT run here, from `ops/ansible/`):** `ansible-playbook -i inventory.yml site.yml --ask-vault-pass --tags continuum`. **(C) Wording:** residual VPS/daemon/box copy in `routstr.js`/`landing.js`/`auth.js` scrubbed to "your Torii, your gateway". Tests: root vitest **762/762** (new `src/data/agent.test.js` 11 cases; preview v0.1.20 **153**), agent `node --test` **104/104**, `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Honest limitation: no jsdom/live agent here, so no live funded-agent click-through — gating proven by the CSS `!important` cascade guarantee + source/behaviour guards. Code + PR only — **not merged, tagged, or deployed.** Prior slice: **v0.2.38-alpha ships ONBOARDING-FINISH: verified-state cleanup, correct balance units, inline key reveal, deterministic final curtain** (onboarding preview → **v0.1.19-preview**, `preview-assets/onboarding-v0.1.19/`). A focused live UX/completion pass from three screenshots; no character/animation/CDN/provider-pinning behaviour changed and no payment/NWC semantics changed beyond balance *display* and safe key *retrieval*; the funded key + prior state are preserved. **(A) Balance units (root cause):** Routstr denominates balances in MILLISATS but `extractBalanceSats` surfaced the raw `balance` verbatim, so a 10,000-sat key showed "10000000 sats"; the provider adapter now divides msat fields to whole sats (explicit `*_sats` trusted), a marker-guarded `displayBalanceSats` migration divides legacy stored envelopes (no `balance_units`) by 1000 on read while new envelopes carry `balance_units:'sat'`, and all balances render via `formatSats` (e.g. `10,000 sats`). **(B) Step 3 verified state:** once claimed/encrypted/verified, `collapseStep3Setup` hides both path cards, both forms, the invoice card, the progress bar and all three setup CTAs, leaving one green summary + a SINGLE Continue button outside it, no countdown/auto-advance (supersedes the v0.1.18 countdown), advance idempotent. **(C) Step 5 recovery kit:** removed the unbranded "Reveal full Routstr key (one-time)" button + its `window.confirm` flow (never revealed the key); the ROUTSTR KEY row now has branded inline eye + copy controls that fetch the key over the existing admin-authenticated no-store `export-key` endpoint, hold it in memory only (never localStorage/sessionStorage/URL/logs), and re-mask on toggle/step-leave/tab-hide/session-loss/timeout (`shouldMaskReveal`/`REVEAL_TIMEOUT_MS`); the Download click is itself the explicit confirmation and embeds the full key in the saved file while still excluding the NWC secret. **(D) Final curtain:** the old curtain only `console.log`'d a commented redirect and spun forever; `initCurtain` now navigates deterministically to the resolved same-origin `/continuum/` home (a `window.__toriiContinuumHome` override accepted only same-origin — open-redirect guard), reveals a real "Open Continuum now" fallback link on a bounded timer, honours `prefers-reduced-motion`, and drops the spinner shortly after the nav attempt so it can never hang. Tests: agent `node --test` **104/104**, root vitest **269/269** (preview v0.1.19 **145**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.37-alpha ships ONBOARDING-PAY-RECOVERY: paid-but-unclaimed Routstr recovery + payment progress state machine + working recovery kit** (onboarding preview → **v0.1.18-preview**, `preview-assets/onboarding-v0.1.18/`). Fixes a live incident where an operator connected NWC and paid 10,000 sats to fund a Routstr session, but the UI said "payment confirmed" without surfacing the `sk-` key, never auto-advanced, and the Recovery Kit "download" was a no-op that just stepped the deck. **No new invoice created or paid during diagnosis or fix.** Delivered a safe SSH recovery runbook first (inspect `memory/secrets/*.enc` by metadata only — never decrypt; reuse the operator's existing browser session token against the loopback admin API; claim the already-paid key with the **idempotent** `POST /api/onboarding/routstr/recover` sending `{}` — the agent re-submits the stored bolt11 and never re-pays; redacted status/verify; one-time no-store `export-key` only if a manual copy is required). Root fact: **payment-confirmed does NOT imply the key is stored** — the claim is a separate post-settlement step; recovery states are paid+claimed / paid+unclaimed (the live case) / unpaid-or-expired. **Agent (v0.2.37-alpha):** new admin-gated endpoints `GET /api/onboarding/recovery/state` (redacted resume snapshot; `claimable` true only when a pending invoice is stored with no key), `GET /api/onboarding/recovery-kit` (`Cache-Control: no-store`, secret-free by default), `POST /api/onboarding/routstr/export-key` (one-time full-key reveal; `confirm===true` required else 400, 409 when no key, `no-store`, rate-limited, persists `export_count`/`last_exported_at` audit, logs warn never the key). **Preview (v0.1.18):** pure `ONBOARD_PHASES`/`PHASE_META` state machine driving an accessible (role=status, aria-live) progress + scanning bar + elapsed timer across connect→quote→pay→claim→verify; **SUCCESS only when the agent reports `key_stored===true`** (`classifyPayResult`/`classifyRecoverResult` map bare paid/recoverable → retryable `PAID_UNCLAIMED`, never success); duplicate-payment prevention (Confirm button permanently disabled after first click); success renders the redacted key then auto-advances after a short countdown with an immediate Continue-now; refresh-resume reads `recovery/state` on load and finishes a `claimable` claim via empty-body `recover` (no re-payment); real Recovery Kit download via Blob (`buildRecoveryKit`) whose default excludes the NWC secret + full key and carries redacted previews + restoration instructions, with the full key included only via an explicit confirmed one-time `export-key` reveal; Step 5 markup no longer prints static secret-adjacent values. Preserved: character/animation behaviour, self-hosted Three.js (no CDN), provider pinning, redaction, rate limits, no autonomous spend, no nsec on server. Tests: agent `node --test` **102/102**, root vitest **453/453** (preview v0.1.18 **124**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed. Prior slice: **v0.2.36-alpha ships ONBOARDING-ASSET: new Chiefmonkey GLB + forbidden-safe animation state machine** (onboarding preview → **v0.1.17-preview**, `preview-assets/onboarding-v0.1.17/`). A substantial asset + animation upgrade of the design-review onboarding mockup (NOT the production app). A fresh Chiefmonkey source GLB replaces the old `chiefmonkey6.glb` and is crushed for production by a new reproducible local optimizer `tools/optimize-glb.mjs` (gltf-transform 4.4.1 + draco3dgltf 1.5.7 + sharp 0.35.3, build-time only, no third-party runtime CDN): `dedup → weld → resample → textureCompress(webp,q82,≤1024²) → prune → draco(edgebreaker)`, dropping every forbidden locomotion/knock-down clip with the runtime's own `isForbiddenClip` predicate (single source of truth). Source 9,298,852 B (SHA `87b0048c…c37dd`) is kept OUT of git (build artifact only, see `tools/SOURCE.md`); the committed optimized `assets/chiefmonkey-onboarding.glb` is 2,347,780 B — **74.75% smaller** (target ≥60%), SHA `0253d5e1…e2fcb` — with a `.manifest.json` recording sizes/%reduction/deterministic SHA + retained/dropped clip inventory (13 retained, 5 forbidden dropped: `Clapping_Run`/`Knock_Down`/`Running`/`Stylish_Walk_inplace`/`Walking`; mesh/skin(24 joints)/material/texture preserved nonzero, clip names survive Draco). `character.js` now runs a dedicated forbidden-safe state machine — each of the 5 deck steps plays one dedicated clip from `STEP_CLIPS`/`selectStepClip` (1 Talk_with_Hands_Open, 2 Agree_Gesture, 3 mage_soell_cast_3, 4 Gentlemans_Bow, 5 Idle_10; dormant step-6 Victory_Cheer curtain), applied deterministically on entering/restoring via the v0.1.15 `resolveReadyStep` path (order-independent) — and a raycast hit-test (`Raycaster.intersectObject`, NDC from canvas rect) plays a random one-shot from a curated `CLICK_POOL` (`pickClickReaction`, no immediate repeat) then crossfades back to the current step clip on the mixer `finished` event, ignoring UI-control clicks / respecting `prefers-reduced-motion` / ignoring click-spam. Step-1/2/3 prompt/success/failure `onboarding:anim` overrides are transient and always return to the current step clip (no race can permanently overwrite step state). Deterministic offline tests parse the optimized GLB JSON chunk (counts nonzero, referenced clip names survive), assert no forbidden pattern is selectable, verify the committed asset matches manifest SHA + size budget (≤3 MB, ≥60%), and cover every step mapping / restore ordering / status→current-step return / click hit-no-hit + UI-guard + no-immediate-repeat + spam guard + crossfade return / reduced motion / final load-error. Verified target-file + full root vitest green and `npm run build` clean. Code + draft PR only — preview snapshot only, not deployed. Prior slices (onboarding preview arc): **v0.2.35-alpha / v0.1.16-preview** (PR #32) reworked Step 2 (wallet-agnostic NWC connect via NIP-47 `get_info` capability matrix, funding gated on `pay_invoice`) and Step 3 (Routstr existing-`sk-…`-key OR fund-a-new-session: quote → explicit confirm → NWC pay → claim minted key, with a RECOVERABLE poll-timeout carrying the bolt11 via `/lightning/recover`); secrets stored encrypted at rest (AES-256-GCM), admin-gated + rate-limited, pay refuses unless `confirm===true` AND `pay_invoice` advertised. **v0.2.34-alpha / v0.1.15-preview** (PR #31) added the deterministic character↔deck step-sync state machine (`createCharacterSync`/`recordStep`/`resolveReadyStep`/`markCharacterFailed`) that fixed the intermittent invisible-Chiefmonkey-after-soft-reload race by remembering steps before model readiness instead of dropping them then hard-applying Step 1. **v0.2.33-alpha / v0.1.14-preview** (PR #30) fixed refresh dropping Step 2 → Step 1 (`restoreSession`/`isSessionValid` fail-closed, `window.__toriiRestoredStep`) and Chiefmonkey invisible on reload (route both GLB stall AND error through `nextLoadAttempt`). Prior slice: **v0.2.32-alpha ships ONBOARDING-STEP1-FIX: "agent challenge failed (400)" on Sign with Plebeian Signer** (onboarding preview → **v0.1.13-preview**, `preview-assets/onboarding-v0.1.13/`). Client-only fix: `onboarding-client.js`'s `postJson` was sending `Content-Type: application/json` on the **bodyless** `POST /api/auth/challenge`, and Fastify v5 rejects an empty body carrying that header with `400 FST_ERR_CTP_EMPTY_JSON_BODY` — failing step 1 before the NIP-07 prompt (which only fires after a challenge is fetched), so Chiefmonkey animated but no signer opened. Fix: `postJson` only sets the JSON content-type + serialises a body when a body is actually present; the challenge call now goes out bodyless (→ 200), verify unchanged. No server endpoint added, no validation weakened, no bunker-connect endpoint; NIP-07 primary + browser-client NIP-46 secondary, `localStorage['torii.session']`, exact signer wording, and the three clips (`HandGesture_00`/`Idle_03`/`Confused_02`) all preserved. New offline vitest case (challenge POST has no body/no JSON content-type; verify still does) + new agent `fastify-v5-api` case pinning the empty-body-400 vs bodyless-200 contract. Prior slice: **v0.2.31-alpha shipped ONBOARDING-STEP1: live NIP-07/NIP-46 auth for onboarding step 1** (onboarding preview → **v0.1.12-preview**, `preview-assets/onboarding-v0.1.12/`). New self-contained `onboarding-client.js` (ES module, no build step, no third-party CDN) wires step 1 to the same-origin agent API. NIP-07 primary via `window.nostr` (button "Sign with Plebeian Signer"): `POST /api/auth/challenge` → sign the exact kind-22242 auth event `agent/core/auth.mjs` expects (`content == challenge`, `['challenge', …]` + `['relay', origin]` tags) → `POST /api/auth/verify` → persist to exactly `localStorage['torii.session']` as `{ token, expires_at, pubkey, method, created_at }` (token + public identity only, no secrets). Fails closed on malformed responses, expired challenges, pubkey/challenge mismatch, or absent token. NIP-46 secondary with the browser as the client (bunker46 architecture, "Use a different signer"): parses a pasted `bunker://` string and asks the remote signer to sign the same event over its relay — **no server bunker-connect endpoint**, no key/secret sent to the agent, no silent fallback. Chiefmonkey phases via the existing event channel: `HandGesture_00` prompting/signing, `Idle_03` success, `Confused_02` failure, each with an ordered fallback to an existing GLB clip (the GLB ships neither `HandGesture_00` nor `Confused_02`, so the fallback runs and never freezes). Desktop-only gate + all prior perf fixes preserved. 28 offline vitest cases; verified root vitest 28/28, root build, agent `node --test`, ops regressions, agent `npm audit --omit=dev`. Code + draft PR only. Prior slice: **v0.2.30-alpha shipped AGENT-SEC-OPT-TORII-PERMS: least-privilege fix for the shared `/opt/torii` parent.** The v0.2.29 deploy surfaced a production regression — `ops/install-agent.sh` created the shared parent with an unconditional `install -d -m 0750 -o continuum -g continuum /opt/torii`, and because `install -d` re-applies mode+owner on every run it clamped the parent that torii's *other* apps live under (torii-base launcher, quest tooling) to `0750 continuum:continuum`, stripping its world-execute bit. nginx (`www-data`) could then no longer traverse `/opt/torii` to reach `/opt/torii/launcher/index.html`, so `/` fell through to a default nginx 404 (Quest under `/var/www` stayed readable). Fix: create `/opt/torii` **only if absent**, `root:root 0755`, and **never re-own/re-mode an existing shared parent**; the agent's own subdir `/opt/torii/continuum-agent` stays locked `0750 continuum:continuum` (nginx never serves from it — the agent is loopback-proxied on 8787). New hermetic + anti-drift regression `ops/test/installer-shared-parent.test.sh` (10 assertions) pins that the installer no longer chowns the shared parent to the service user, guards creation with an existence check, and keeps `$INSTALL_DIR` locked. The previously-planned **onboarding** work moves to **v0.2.31-alpha**. Operator out-of-band unblock if needed before redeploy: `sudo chown root:root /opt/torii && sudo chmod 0755 /opt/torii` (restores o+x, leaves the agent subdir untouched). Prior slice: **v0.2.28-alpha shipped AGENT-SEC-CASHU-LTS: the money-path dependency migration flagged in v0.2.27.** `@cashu/cashu-ts` moved `2.5.3 → 3.7.1` (the maintained `v3-lts` "security-fixes-only" line; dist-tag `v3-lts` resolved to `3.7.1` at implementation time, latest was `4.7.0` — v4 deliberately not taken), pinned `^3.7.1`, lockfile regenerated with sha512 integrity and the deprecation notice cleared. Code adapted in `agent/core/wallet.mjs` only: class rename `CashuMint→Mint` / `CashuWallet→Wallet`; the boot warm-up `getMintInfo()` (async in v2, now a sync cached getter that throws pre-load in v3) replaced by `await wallet.loadMint()`; an idempotent `ensureLoaded()` guard added before `receive()`/`send()` to restore v2 lazy-load-on-demand (verified zero extra network once keysets are cached). Token/proof codec, `Token`/`Proof`/`SendResponse` shapes, and the `receive()→Proof[]` contract are unchanged between the versions (both default to token-v4 `cashuB`). Safety proof: a token encoded by the **real 2.5.3 library** decodes under 3.7.1 with all mint/unit/memo/id/amount/secret/C preserved AND **re-encodes byte-identically**; on-disk `memory/wallet/*.json` survives a JSON round-trip unchanged — no re-mint, conversion, deletion, or network mutation. New offline `agent/test/cashu-migration.test.js` (8 cases) pins every used Cashu boundary incl. malformed/truncated tokens failing closed without leaking secrets; agent `node --test` now **38/38**. `npm audit --omit=dev` and full audit both **0 vulnerabilities**; real `node index.mjs` boot on Node 20.20.1 clean with no new warnings and Fastify 5.10.0 intact; root build + ops regressions (13/13, 9/9) green; prod tree shrank 71→68 packages. **Runtime gate — RESOLVED in v0.2.29-alpha (AGENT-SEC-CASHU-LTS-RUNTIME):** cashu-ts 3.7.1 declares `engines.node >=22.4.0` across the whole v3-lts line. This is now treated as a **HARD deployment prerequisite for the money path, not an advisory** — an `EBADENGINE` warning during `npm ci` is not an acceptable production state for a wallet. v0.2.29 raised `agent/package.json` (+ agent lockfile) `engines.node` to `>=22.4.0` (root frontend `package.json` deliberately left engine-free — static vite/vitest, no agent runtime), rewrote the `ops/install-agent.sh` preflight to gate on a robust major.minor.patch check (`ops/lib/node-version.sh::node_version_ok`, rejecting 22.0.x–22.3.x, erroring with the floor and stopping before touching anything), added `ops/test/installer-preflight.test.sh` (18 host-independent boundary assertions), fixed the stale `wallet.mjs` comment, and made `ops/README` state Node 22 LTS is a hard prerequisite. Verified under a real **Node 22.11.0** runtime (sandbox default is Node 20.20.1): clean `npm ci` with no EBADENGINE, 0 vulns, agent 38/38, ops 18/9/13, root build + vitest green, and a `node index.mjs` boot → `/api/health` 200 (v0.2.29-alpha) with no warnings. **Deploy prerequisite:** upgrade the VPS to Node 22 LTS before re-running `ops/install-agent.sh`; it will refuse to run on Node 20. **NOT yet deployed** — v0.2.26 remains the live server; v0.2.27 remains the newest shipped code. Prior slice: **v0.2.27 shipped AGENT-SEC: production dependency remediation.** `npm audit --omit=dev` in `agent/` reported 5 HIGH advisories in the Fastify tree during the live v0.2.26 deploy — all rooted in `fast-uri` (path-traversal GHSA-q3j6-qgpj-74h6 + host-confusion GHSA-v39h-62p7-jpjc) plus a Fastify content-type body-validation-bypass (GHSA-jx2c-rxcm-jvmq, no v4 backport). Cleared by upgrading `fastify ^4.28.1 → ^5.10.0`, `@fastify/cors ^9 → ^11`, `@fastify/rate-limit ^9 → ^11` and regenerating the lockfile; `fast-uri` now resolves to patched 3.1.3/4.1.0. No app code change beyond dropping the explicit `disableRequestLogging: false` from the Fastify constructor — passing the top-level option at all (even the default `false`) trips Fastify v5's `FSTDEP023` deprecation (guard is `!== undefined`, verified in installed `fastify@5.10.0` source + under `--trace-deprecation`; warning says removed in `fastify@6`); the drop is a no-op that silences the warning, request logging still emits. `@cashu/cashu-ts` stays at 2.5.3 (**no published npm/GHSA advisory**, so it does not block this hotfix — but the deprecation points to the `v3-lts` "security-fixes-only" line, so a v3-lts/v4 migration is a **security-relevant money-path follow-up to prioritise**, deferred as a separate slice to avoid behaviour drift in money-handling code — not cosmetic). Post-fix `npm audit --omit=dev` = **0 vulnerabilities**. Added `agent/test/wallet.test.js` (offline wallet guard/failure paths + cashu-ts codec regression) and `agent/test/fastify-v5-api.test.js` (cors preflight + rate-limit route-config contract); agent `node --test` now 30/30, rate-limit smoke green, root build green, ops regression 13+9 green. **NOT yet deployed** — v0.2.26 remains the live server version until an operator re-runs `install-agent.sh`. Base-path awareness + Ollama fallback landed in v0.2.6. Docs hygiene sweep across v0.2.7 → v0.2.12. **v0.2.13 shipped CONT-HEALTH-1**: dashboard now has a live Provider card polling `/api/health/models` every 20s. **v0.2.14 shipped SUITE-VPS-READY-1 rate-limit slice** — the public auth surface is behind `@fastify/rate-limit@9` per-IP with 429 + `Retry-After`; the in-memory challenges Map is capped and LRU-by-expiry evicted with structured `[auth]` logs, all prefix-only. **v0.2.26 shipped SUITE-VPS-READY-2: agent deploy tooling + first-touch admin claim.** New `ops/` assets let an operator run the agent as a hardened standalone systemd service: `ops/systemd/torii-continuum-agent.service` (locked `continuum` user, `ProtectSystem=strict`, only `memory/` + `config.yaml` writable), `ops/nginx/torii-api.conf` (same-origin `/api/` proxy + edge `limit_req`), `ops/install-agent.sh` (idempotent strict-bash installer: locked user, `rsync` without clobbering state, `npm ci --omit=dev`, one-time config gen with a fresh `openssl` session_secret, config validation, nginx `-t`-before-reload wiring, bounded `/api/health` proof), and a refreshed `ops/README.md` runbook. `agent/core/auth.mjs` gained first-touch admin bootstrap: with an empty `admin_npub` the first valid NIP-07 verifier atomically claims admin (npub persisted to `config.yaml` 0600 via injected `persistAdminNpub` in `config.mjs`), race-safe and fail-closed; `/api/health` now reports `admin_claimed`. **The agent is NOT deployed by this work** — running `install-agent.sh` on a server is a separate operator step. **CONT-HEALTH-2 (real Cashu wallet health probe) shipped in v0.2.47-alpha** (see the Active focus above): a non-mutating `wallet.health()` + admin-gated `GET /api/wallet/health` + a Provider-card row, verifying mint identity/NUT-07 support and reporting balance without touching proofs or wallet state.

---

## Standing operating rules (project-wide, across all Torii repos)

1. Each Torii app lives in a fully separate GitHub repo (`torii-quest`, `torii-continuum`, `torii-de`, `torii-base`, `torii-suite`); files carry ONLY that repo's project name — Continuum files say "continuum", Quest files say "quest", DE files say "de". Never cross-name.
2. Bump the version on EVERY change without exception — including doc-only changes, comment tweaks, filename renames, and typo fixes. There is no "too small to bump" change.
3. Push everything to GitHub immediately via a PR that lands on `main`. No local-only work.
4. Never publish device names, hostnames, or local machine identifiers to GitHub (commits, PR titles, PR bodies, code, docs). Use generic terms like "your local machine".

---

## Project

- **Name:** Continuum — a local-first project engine + marketplace shell for Torii Quest and related nostr-shaped work.
- **Repo:** `ChiefmonkeyArt/torii-continuum` on the Perplexity git proxy.
- **Local workspace path:** `/home/user/workspace/torii-continuum`
- **Live URL:** https://continuum-torii.pplx.app
- **Publish site_id:** `00acfee3-6cd6-477f-8d0f-36f84a6f6963`  ← ALWAYS pass this on publish updates
- **Publish app_slug:** `continuum-torii`
- **App asset_id (Perplexity preview):** `c82245ab-ac3c-4283-847b-f0e604adde1d`
- **Visibility settings URL:** https://www.perplexity.ai/computer/a/c82245ab-ac3c-4283-847b-f0e604adde1d?open-publish=true
- **Old (legacy) URL still up separately:** https://continuum.pplx.app — read-only oversight surface from earlier build, unrelated to this app's publish chain.

## Stack

- **Frontend:** Vite (dev + build), Vitest (tests). Vanilla JS SPA + hash router. Static bundle. LocalStorage for theme + nostr-shaped events + session token.
- **Agent (`agent/`, v0.2.0-alpha+):** Node 22 LTS (hard floor since v0.2.29; `engines.node >=22.4.0`) + Fastify (`fastify@^5.10.0` since v0.2.27), `nostr-tools@^2.7.2` for NIP-07 verify, `@cashu/cashu-ts@^3.7.1` (v3-lts, since v0.2.28) for the Cashu wallet, `yaml@^2.5.1` for config. Read-only project-source adapters (`agent/core/project-sources.mjs` + `agent/lib/markdown-todo.mjs`) added in v0.2.47. Runs as a systemd service on the operator's VPS. Frontend points at it via `VITE_AGENT_URL` (build-time) or `window.__CONTINUUM_AGENT_URL__` (runtime). Demo build on pplx.app intentionally omits this so it stays offline/mock.
- Auth: NIP-07 challenge (kind 22242) verified server-side, session token is `iat.exp.pk.hmacSig` HMAC-SHA256, 24h TTL.

## Design system (matches https://continuum.pplx.app aesthetic)

- **Palette (dark, HSL space-separated so alpha ops work):**
  - `--background: 30 12% 8%`
  - `--foreground: 40 16% 93%`
  - `--card: 32 10% 12%` / `--card-border: 32 10% 20%`
  - `--primary: 38 92% 58%` (amber)
  - `--muted-foreground: 38 8% 62%`
  - `--sidebar: 30 12% 10%` / `--sidebar-accent: 32 10% 18%`
- **Palette (light):**
  - `--background: 40 33% 97%`
  - `--foreground: 36 14% 13%`
  - `--primary: 36 92% 46%` (deeper amber)
- **Fonts (loaded in `index.html`):**
  - Display: Cabinet Grotesk (Fontshare)
  - Body: Satoshi (Fontshare)
  - Mono: JetBrains Mono (Google Fonts)
- **Radius:** `0.75rem`
- **Theme toggle:** sun/moon SVG in sidebar; respects `prefers-color-scheme`.

## File map

- `index.html` — Fontshare + Google Fonts preconnect + stylesheets, `theme-color="#1a1613"`.
- `src/main.js` — bootstrap.
- `src/shell.js` — sidebar, theme toggle, footer note ("Local-first…").
- `src/router.js` — hash router.
- `src/views/projects.js` — Projects list.
- `src/views/projectHome.js` — single project page (milestones + todos). Exports `renderProjectTabs(slug, active)` — the Overview · Board view switcher shared with the board.
- `src/views/board.js` — per-project Kanban board (columns 30083 / cards 30084): default Todo/Doing/Done, custom columns, drag + accessible arrow moves, card modal. Route `#/projects/:slug/board`.
- `src/views/marketplace.js` — bounty rows, "ours" highlighting.
- `src/views/dashboard.js` — oversight cards + by-project progress.
- `src/views/routstr.js` — Routstr AI model picker + Cashu wallet mock.
- `src/data/{schema,store,seed}.js` — mock nostr-shaped event store. Board logic (columns/cards CRUD, `moveCard`, `ensureBoard`, `BOARD_LIMITS`) lives in `store.js`; tested by `src/data/board.test.js`.
- `src/styles/theme.css` — HSL tokens, both themes.
- `src/styles/layout.css` — sidebar + page-header (amber eyebrow + display title).
- `src/styles/pages.css` — cards, milestones, todos, marketplace rows.
- `src/styles/chat.css` — bottom chat dock (mock).

## Disk retention

- `ops/torii-disk-retention.sh` — post-success sweep run by the cutover role. Prunes source clones, cutover staging, state backups, `app.staging` residue, rotates logs, and (v0.2.85-alpha) **prunes `/home/continuum/app.quarantine-*`**: keeps the newest `RET_QUARANTINE_KEEP` (default 3) regardless of age, deletes the rest only once older than `RET_QUARANTINE_AGE_DAYS` (default 3). Fix for the 2026-07-20 incident where nine quarantines (~5.5 GiB) filled `/dev/sda1` and broke the v0.2.84-alpha deploy. Ansible knobs: `continuum_retention_quarantine_{parent,age_days,keep}`.

## Build + deploy commands

```bash
# Local dev
cd /home/user/workspace/torii-continuum
npm ci
npm run dev            # vite dev server
npm test               # vitest

# Production build
npm run build          # outputs to /home/user/workspace/torii-continuum/dist

# Preview deploy (thread-attached app card)
pplx-tool deploy_website <<'JSON'
{
  "project_path": "/home/user/workspace/torii-continuum/dist",
  "site_name": "Continuum",
  "entry_point": "index.html",
  "should_validate": true
}
JSON
# api_credentials=["pplx-tool:deploy_website"]

# Publish update to live URL (ALWAYS pass site_id — no subdomain picker)
pplx-tool publish_website <<'JSON'
{
  "project_path": "/home/user/workspace/torii-continuum",
  "dist_path": "/home/user/workspace/torii-continuum/dist",
  "app_name": "Continuum",
  "site_id": "00acfee3-6cd6-477f-8d0f-36f84a6f6963"
}
JSON
# api_credentials=["pplx-tool:publish_website"]

# Commit + push
cd /home/user/workspace/torii-continuum && \
  git add -A && \
  git -c user.email=chiefmonkey@hodlr.rocks -c user.name="Chiefmonkey" \
    commit -m "…" && \
  git push origin main
# api_credentials=["github"]
```

## Publishing rules to remember

- **Preview vs publish:** `deploy_website` is the thread-attached preview (safe, unlimited). `publish_website` updates the live `continuum-torii.pplx.app` URL — do it only when the user asks.
- **Always pass `site_id`** on updates. Without it, the tool shows a subdomain picker and can create a duplicate site.
- **Order matters:** `deploy_website` must be called first with the same `dist_path` before `publish_website` in the same session.
- **Security review required before every publish:** run a subagent with `/home/user/workspace/skills/website-building/website-publishing/security_subagent_prompt.md` and pass BLOCK findings back to the user.
- **Published-site limits:** no `api_credentials`, no LLM APIs, no external tool connectors at runtime. This app is static-only so unaffected.
- **Do not use `publish_website` to unpublish.** If user asks to unpublish, direct them to the app card's Unpublish button.

## Git state (as of handover)

- Branch: `main`
- Remote: `https://git-agent-proxy.perplexity.ai/ChiefmonkeyArt/torii-continuum.git`
- Recent commits (top of `main`):
  - v0.2.13-alpha released — CONT-HEALTH-1 dashboard provider card (#9)
  - `<pending>` release: v0.2.14-alpha — SUITE-VPS-READY-1 rate-limit slice (rate-limit auth surface + bounded challenges Map + structured [auth] logs)
  - `043ad7c` release: v0.2.12-alpha — finish Space-scoped file naming migration (#8)
  - `8ceb3cd` release: v0.2.11-alpha — refresh torii-continuum-handoff.md (#7)
  - `13f1769` release: v0.2.10-alpha — scrub local-machine class mentions from docs (#6)
  - `e0c7259` release: v0.2.9-alpha — rename HANDOVER.md → torii-continuum-handoff.md (#5)
  - `74e3812` release: v0.2.8-alpha — cross-name audit: clean up stale Quest references (#4)
  - `40f5b27` release: v0.2.7-alpha — mirror standing operating rules into HANDOVER.md (#3)
  - `f185e67` CONT-INSTALLER-1 + CONT-AGENT-1b: base-path awareness + Ollama fallback (v0.2.6-alpha)
  - `dc4124d` panic key: make 30097 explicitly optional (v0.2.5-alpha)
  - `907c05e` CONT-CHARACTER-1: sealed character + memory infrastructure (v0.2.4-alpha)
  - `94e5269` feat: v0.2.3-alpha — ornate Myōjin torii SVG
  - `d830ec5` chore: v0.2.2-alpha — new H1 'The Gateway Project.'
  - `51482e2` chore: v0.2.1-alpha — dark default + security hardening
  - `cb7d4eb` feat: v0.2.0-alpha — CONT-AGENT-1 invariants + landing (mashed)

## Related projects (for cross-linking)

- `ChiefmonkeyArt/torii-quest` — the Three.js/Rapier arena shooter. Live at `torii-quest.pplx.app`. Separate repo, separate publish chain, separate release cadence.
- `torii-quest`'s dashboard source module (renamed to `toriiQuestDashboardData.js` in Quest v0.2.351) — the legacy source of the bronze/amber aesthetic that was ported here.

## Space context

- Perplexity Space: **Torii** (canonical URL `https://www.perplexity.ai/spaces/torii-8qN21IWsQ7.yuEGH9oNihw`).
- Space instructions: source-of-truth files are Space-scoped, one set per project. For Continuum work, use:
  - `torii-continuum-strategy.md` — strategy
  - `torii-continuum-todo.md` — active task list
  - `torii-continuum-progress.md` — progress log
  - `torii-continuum-handoff.md` — this file
- Never load a Quest / DE / Base todo or strategy file for Continuum work — cross-naming is a standing-rule violation (rule #1).
- Optimize for efficiency, security, file size, and speed on every change.

## Next likely tasks

Code slices (in rough priority order):

- ~~**Dashboard header → `/api/health/models`.**~~ **DONE v0.2.13-alpha** — Provider card renders under the KPI strip on `#/dashboard`, polls every 20s, three states per provider (`Enabled` / `Reachable`+`Unreachable` / `Disabled`), self-cleans on hashchange, honest about the fact that Routstr has no reachability probe yet.
- ~~**Real Cashu wallet health probe.**~~ **DONE v0.2.47-alpha** — non-mutating `wallet.health()` (cashu-ts v3 read primitives: `loadMint`/`getMintInfo` identity getters, `.isSupported(7)` NUT-07, `groupProofsByState`) behind admin-gated `GET /api/wallet/health`, rendered as a wallet row in the dashboard Provider card. Verifies mint identity and reports balance without minting/melting/swapping — serialized wallet state untouched.
- **Project-source import (read-only).** **DONE v0.2.47-alpha** (#45/#46) — allowlisted, fail-closed local-Markdown + public-GitHub adapters (`agent/core/project-sources.mjs`) surfaced as read-only, ephemeral, non-duplicating cards on each per-project Kanban with source/status filters + manual refresh. Future: write-back is intentionally out of scope for this release.
- **Consent gate on `/api/chat`.** The agent will currently spend sats on any authenticated request. Add a per-session consent flag mirroring Quest's SEC-1 pattern before wiring any UI that fires chats implicitly (e.g. on page load).
- **Multi-model Ollama routing.** Router currently picks Ollama vs Routstr but not Ollama-model-A vs Ollama-model-B. Config shape needs a small model list.
- **Live cold-boot smoke of the Ansible playbook** on a fresh Ubuntu 24.04 VPS. Check-mode has been run; end-to-end has not.
- **IndexedDB persistence** for the frontend store (currently in-memory + localStorage seed).
- **Real nostr relay publish** for NIP-07 events (currently mocked).

Housekeeping / infra:

- Consider mirroring the torii-suite Playwright `?mock=1` walkthrough as a GitHub Action for Continuum so doc-only regressions can't sneak past the version-bump rule.
- User's stated intent: self-host eventually via `ops/ansible`; use `continuum-torii.pplx.app` until then.

## User preferences (confirmed)

- Loves the bronze/amber aesthetic of `continuum.pplx.app` — this app must match it.
- Wants both light AND dark themes.
- Local-first + nostr from day one.
- Optimize for efficiency, security, file size, and speed.
- Terminology: never say "scrape/crawl"; prefer "collect/gather/read".
- Uses ZorinOS + Comet on their primary local machine; a secondary local machine is available in `<devices>` for browser tasks.

## Resume checklist for the new session

1. Confirm the URL is still live: `curl -sI https://continuum-torii.pplx.app | head -1`
2. Pull latest: `cd /home/user/workspace/torii-continuum && git pull origin main`
3. Read this file first, then check `src/data/store.js` and `src/router.js` for structure.
4. For any publish, use `site_id: "00acfee3-6cd6-477f-8d0f-36f84a6f6963"`.
