{
  "name": "torii-continuum-agent",
  "version": "0.2.89-alpha",
  "description": "Torii Continuum agent daemon — sovereign personal AI + project engine. VPS-only in v1; NIP-07 login; Cashu-paid Routstr model layer; no key material on the box.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.4.0"
  },
  "scripts": {
    "start": "node index.mjs",
    "dev": "node --watch index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@cashu/cashu-ts": "^3.7.1",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^11.0.0",
    "fastify": "^5.10.0",
    "nostr-tools": "^2.7.2",
    "yaml": "^2.5.1"
  }
}
