# Torii Continuum — Master TODO

> Torii Continuum is a **separate app** from Torii Quest. Continuum is the sovereign dashboard / project engine / personal AI. Quest is the game.

> This file is the **active task list and source of truth for Torii Continuum**. Update it whenever Continuum tasks are added, changed, completed, removed, or reprioritised.

> Continuum docs (this repo, per `Torii` Space instructions): `torii-continuum-strategy.md`, `torii-continuum-todo.md`, `torii-continuum-progress.md`, `torii-continuum-handoff.md`, `README.md`.
> Quest tasks live in the `torii-quest` repo (`torii-quest-todo.md`), not here. Do not merge queues.

> Older reports (`torii-v-*.md`, Nostr Arena docs, legacy snapshots) that still live in the Quest repo are **archival only**. Do not use them as task queues.

### Active tasks

- **KANBAN-BOARD (Continuum PR slice) — production Kanban board for every project. DONE v0.2.45-alpha (NOT yet deployed).** Frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**. Every project gets an **Overview · Board** view switcher (`renderProjectTabs`, shared by `views/projectHome.js` + `views/board.js`) and a board route `#/projects/:slug/board`. Boards start with default columns **Todo · Doing · Done**, materialised lazily per project via `ensureBoard(slug)` (so existing/migrated projects gain a board on first access with **no bulk state rewrite**). Per-project **custom columns**: add / rename / reorder (drag + accessible `‹ ›` controls via `moveColumn`) / delete — deleting a **non-empty** column is blocked unless the caller supplies a destination (`deleteColumn(slug,colId,moveToColumnId)` relocates every card first; the UI forces a destination `<select>`), and the **final** column can never be deleted — **cards are never lost**. **Cards** carry title + description + optional assignee + due date (lean model; no comments/attachments/history this slice); create/edit/delete via modal, move within/between columns by **HTML5 drag-drop** *or* accessible `‹ ↑ ↓ ›` arrow buttons for keyboard/touch — both paths funnel through the single `moveCard(slug,cardId,toColumnId,toIndex)` primitive (dense `order` reindex on both source + destination). New Nostr-shaped kinds **30083 (column, `d=<slug>:col:<id>`)** + **30084 (card, `d=<slug>:card:<id>`)**, persisted through the existing localStorage store (`continuum.v1`); `deleteProject` cascades columns + cards. Migration is additive (`initStore` coerces missing `columns`/`cards` arrays; fail-closed seed on unreadable state). **Validation/limits** (`BOARD_LIMITS`): ≤20 columns/board, ≤200 cards/column, title ≤120, description ≤2000, assignee ≤80, name ≤40, due date accepts only `YYYY-MM-DD` else dropped; all rendering flows through the XSS-safe `util.h()` (textContent only — no raw HTML). Responsive horizontal scroller with mobile snap, dark-Amber styling in `pages.css`, empty states per column + board. Tests: new `src/data/board.test.js` (**32 cases** — defaults, project isolation, column add/rename/reorder/move/delete + safe-delete-with-move + block + final-column guard, card CRUD + dense reindex, `moveCard` intra/cross/clamp, input validation + payload caps + due-date sanitising, persistence-across-reload + legacy-state migration, deleteProject cascade + no-regression to todos/milestones); `vitest run` **43/43** (11 pre-existing + 32 new), `npm run build` clean (bundle 74.6 kB / 23.2 kB gzip). No headed browser/jsdom in sandbox — board logic proven via the pure store layer (repo's established no-DOM test convention); view wired through the same primitives. Root version → **v0.2.45-alpha** (+ lockfile); agent version unchanged (no agent/protocol change). Code + PR only — not merged, tagged, or deployed.
- **APP-ROUTING/AUTH (Continuum PR slice) — application-first root: login + dashboard, sales page isolated to #/about. DONE v0.2.44-alpha (NOT yet deployed).** Frontend-only; **no agent code changed**, onboarding preview stays **v0.1.20-preview**. Implements the operator ask "separate out that sales page from the root of continuum and replace it with a login page and the dashboard." **Root is now application-first**, never the marketing page: logged out → a dedicated branded Continuum Amber **login page rendered in place at root** (no redirect, no bounce), using the existing NIP-07 / Plebeian Signer flow (`startLogin`) — **no invented password auth, no secrets in the DOM**; a valid `continuum.session.v1` (or a safely-adopted onboarding `torii.session` via existing `adoptOnboardingSession()`) → root routes straight to `#/dashboard`. All routing/auth decisions live in a new **pure, DOM-free `src/nav-guard.js`** (`rootTarget`, `guardRedirect`, `isProtectedPattern`, `PROTECTED_PATTERNS=['/dashboard']`, `ROOT_PATH=LOGIN_PATH='/'`, `DASHBOARD_PATH='/dashboard'`) so the contract is exhaustively testable without jsdom. `#/dashboard` is guarded (`guardRedirect` before `renderDashboard`); an expired/invalid session bounces to login **without a loop** (protected→root→render-login-in-place is terminal). The sales/marketing content moved verbatim to an explicit **`#/about` route** (`renderLanding` → `renderAbout`; `toriiSvg` now exported for reuse by the login mark), reachable only from small non-primary links on the login card, never the app root or an onboarding completion target. `main.js` rewired (app-first `route('/')`, `route('/about')`, guarded `route('/dashboard')`, reactive `continuum:session-changed` handler that re-renders login / bounces protected views on session drop); onboarding completion still lands on `/continuum/#/dashboard` with working handoff; all Amber views + browser refresh/deep-link behaviour preserved (guards run on the initial router resolve too). New login styles in `src/styles/landing.css`. **Zero external requests** — no Google/gstatic/Fontshare/CDN/analytics/scripts in any new source (dist carries only the pre-existing inert `chromewebstore.google.com` Plebeian-Signer anchor, which loads nothing). Tests: new `test/app-routing-auth.test.js` (20 cases — unauth root→login, auth root→dashboard, onboarding adoption live/dead, expired/invalid→login, dashboard protection, no-loop, `#/about` isolation, main.js/views source-structure wiring locks, no-external-loader scan); `vitest run` **795/795**, agent `node --test` **104/104**, ops (`continuum-adopt` 188 + `installer-preflight` 18 + `installer-shared-parent` 10 + `installer-signal` 9 + `nginx-install` 13), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). No headed browser/jsdom in sandbox — routing proven via the pure guard layer + source-structure regression locks (repo's established no-DOM convention). Root version → v0.2.44-alpha. **Ops addendum (same PR):** a live v0.2.43 rerun hit one more exact bug — `group_vars/all.yml.example` default `torii_root_app: launcher` made the role call `torii set-root launcher`, and the installed CLI/API returned **404 `{error: app_not_installed, name: launcher}`** (the launcher at `/` is the sentinel `none`, not a registered app), rolling the deploy back; operator reran with `-e torii_root_app=none`. Fixed: example default → **`none`**; role now calls **`continuum-adopt.sh set-root-safe`** which normalizes legacy/empty/`launcher`(+synonyms)→`none`, sources the admin token from `/opt/torii/env` silently, skips when `torii get-root` already matches, and returns 0 for the default so no rollback fires; an explicit registered app still works and `continuum`-as-root warns but is honoured only when deliberately set — **Continuum is never made root by its installer**. `ops/README.md` v0.2.44 subsection added; `continuum-adopt.test.sh` 188 → **219** (mock CLI reproducing the exact 404 + normalization/skip/explicit/no-leak/anti-drift). No-Google/no-CDN guarantees preserved. Code + PR only — not merged, tagged, or deployed.
- **OPS-HARDENING (Continuum PR slice) — encode three live-discovered cutover corrections. DONE v0.2.43-alpha (NOT yet deployed).** Ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**. The v0.2.42-alpha cutover succeeded manually on the live box; three environment realities were fixed by hand and are now encoded permanently. **(1) Torii CLI `register` uses flags, not positionals** — installed CLI is `torii register <name> [--display …] [--desc …] [--version …]`; the old positional form failed with `unknown flag: Continuum`. The role now calls `torii register {{ continuum_register_name }} --display "Continuum" --desc "App builder + agent" --version <bare-semver>` (bare via `continuum_version | regex_replace('^v','')`), new `continuum_register_*` defaults. **(2) `MemoryDenyWriteExecute` must be `no`** — Node 22's V8 JITs with W^X memory; `=yes` makes the kernel refuse the mprotect and V8 dies on startup (fatal `SetPermissions` / errno 12 / `SIGTRAP`). `continuum-agent.service.j2` sets `MemoryDenyWriteExecute=no` with inline rationale, retaining every other compatible directive (`NoNewPrivileges`, `Protect*`, `RestrictNamespaces`, `LockPersonality`, empty `CapabilityBoundingSet`); the role runs a **Node V8 JIT smoke test** under the rendered MDWE value (transient `systemd-run` scope) before starting the real service. **(3) nginx cannot traverse `/home/continuum`** (0750 home + `ProtectHome` → HTTP 500) — the build is published to a **public webroot `/var/www/torii/continuum`** (root-owned, dirs 0755 / files 0644) via new `deploy_webroot` (atomic staged swap, prior webroot kept as timestamped backup); only the static bundle is copied out, source + encrypted state stay private under `/home/continuum/app`. A **single** `location /continuum/` prefix alias serves both the SPA entry and the hashed `assets/*` bundle (no regex-alias, no `/home`). The cutover `rescue` now also rolls the webroot back (`rollback_webroot`) + reloads nginx on failure. Re-running against the now-live v0.2.42 layout is idempotent (re-detects `existing-ansible`, preserves config, never rotates `session_secret`/touches the funded key). `ops/test/continuum-adopt.test.sh` **117 → 188** assertions (webroot deploy/rollback + perms + dangerous-target refusal; mock Torii CLI parser reproducing the live `unknown flag` bug vs flag-form success; systemd MDWE=no contract + retained hardening; Node V8 WASM smoke; nginx section is a **no-nested-assets regression lock**; existing-ansible upgrade idempotency). New `ops/README.md` v0.2.43 subsection + upgrade command. Root + agent version → v0.2.43-alpha (+ both lockfiles). **AMENDMENT (pre-merge, same PR #40):** (4) removed all production Google (`fonts.googleapis.com`/`fonts.gstatic.com`) + Fontshare (`api.fontshare.com`) preconnects/stylesheets from `index.html`; typography is now a **local system font stack** (`src/styles/theme.css`) → production makes zero external requests (no fonts downloaded/vendored). (5) the live black page was the nested `location /continuum/assets/` alias+`try_files` 404'ing hashed `.js`/`.css`; both nginx templates now use a single parent alias only. (6) upgrade docs corrected to the real live paths (`/opt/deploy/torii-continuum-v0.2.42-alpha` checkout, app at `/home/continuum/app`; fresh version-specific public clone of the final tag) — `/opt/torii/src/torii-continuum` never existed. New `test/no-external-cdn.test.js` + `test/nginx-continuum-routing.test.js` (real hashed filenames, 200 + SPA fallback); `vitest run` **775/775**. No headed browser in sandbox — verified via build + HTTP-200 routing + same-origin-only fetch scan (hash-router SPA, so the server contract is fully exercised). Code + PR only — not merged/tagged/deployed.
- **OPS-HARDENING (Continuum PR slice) — transactional adoption/cutover + partial-adoption recovery. DONE v0.2.42-alpha (NOT yet deployed).** Ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**. Fixes the live v0.2.41-alpha adoption failure: the role migrated live state INTO `/home/continuum/app/agent` and only THEN ran `ansible.builtin.git` into `/home/continuum/app` — cloning into a dir already populated with runtime state. It aborted, leaving the **original standalone layout intact** AND a **partial, non-git `/home/continuum/app`** holding a copy of the state (the untouched `torii-continuum-agent.service` was restored, health good). **Fix (`ops/lib/continuum-adopt.sh` + `roles/continuum/tasks/main.yml`):** `layout_detect` gains a 3rd `app_dir` arg + a new **`partial-adoption`** mode (agent has state but `app/.git` absent while standalone is present — a real git-backed Ansible install still wins; a non-git app dir carrying state is recovered, never built on top of). New `authoritative_state_dir` prefers the untouched standalone over the partial copy. New `stage_reset`/`promote_release`/`rollback_release`: `git checkout` + `npm ci` + `vite build` run in a **clean staging dir** (`/home/continuum/app.staging`) while the **old unit keeps serving**; authoritative state is copied in **only after a successful build**; the old unit stops only immediately before an **atomic same-filesystem swap** into `/home/continuum/app`, quarantining (never deleting) any pre-existing app dir to `app.quarantine-<UTC>`. The `block`/`rescue` cutover stops the new unit on any promotion/`/api/health` failure, rolls an existing-Ansible tree back from quarantine (or re-enables the standalone for adopt/partial), and prints backup + quarantine paths with an exact **secret-free** recovery command; re-running after the exact observed failure is idempotent. `ops/test/continuum-adopt.test.sh` extended to **117** hermetic assertions (3-arg detect incl. partial-adoption + absent-app-dir; `authoritative_state_dir`; `stage_reset` refuses dangerous targets; `promote_release` clean/quarantine/refuse/idempotent; `rollback_release` restore/refuse/no-quarantine; end-to-end partial-adoption recovery proving byte-for-byte config equality + funded-key retention + quarantine preservation + untouched standalone; v0.2.42 anti-drift greps). New "Transactional cutover + partial-adoption recovery (v0.2.42-alpha)" subsection in `ops/README.md` with the exact recovery invocation for the current partial-adoption VPS. Code + PR only — not merged/tagged/deployed.
- **OPS-HARDENING (Continuum PR slice) — vault-free standalone adoption / redeploy. DONE v0.2.41-alpha (NOT yet deployed).** Ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**. Closes the blocker left by v0.2.40-alpha: even on `adopt-standalone`/`existing-ansible` (where the live `config.yaml` is preserved byte-for-byte), the role's preserve path still rendered a `.config.candidate.yaml` from `config.yaml.j2` for the drift diagnostic — and that template references `{{ admin_npub }}`/`{{ session_secret }}`, so a truly vault-free run (no `group_vars/vault.yml`, no vault password) died with an undefined-variable error before finishing adoption. **Fix (in `roles/continuum/tasks/main.yml`):** a new `continuum_vault_vars_present` fact computed defensively via the `default('')` filter (an undefined var yields `false`, never raises); the candidate render + `config-drift` + candidate removal + "differ" warning are all guarded behind `continuum_vault_vars_present | bool` and simply SKIP when the vault vars are absent (a non-secret debug notes the diagnostic was skipped). Config is still preserved byte-for-byte on adoption. A fresh install or an explicit `continuum_allow_config_rotation=true` rotation still needs the vault vars, and their absence in those modes now **fails closed** with a clear, non-secret `ansible.builtin.fail` *before* any state is written. `ops/test/continuum-adopt.test.sh` extended to **66** hermetic assertions (vault-free adopt end-to-end incl. byte-for-byte config preservation + funded-key migration + no-secret-in-output; existing-ansible redeploy; fresh missing-vars fail-closed; explicit-rotation missing-vars fail-closed; presence truth table; anti-drift greps for the new guards). New "Vault-free adoption / redeploy (v0.2.41-alpha)" subsection in `ops/README.md` with the exact secret-free localhost invocation. Operator handles NO secrets and needs NO vault password for adoption. Code + PR only — not merged/tagged/deployed.
- **OPS-HARDENING (Continuum PR slice) — safe standalone→Ansible adoption, no config clobber, fail-closed backups. DONE v0.2.40-alpha (NOT yet deployed).** Ops-only; **no app/agent code changed**, onboarding preview stays **v0.1.20-preview**. Fixes the deployment mismatch that blocked the live v0.2.39-alpha rollout: the continuum Ansible role cloned to `/home/continuum/app` and **re-rendered `config.yaml`/`session_secret` unconditionally every run**, while the live standalone box uses `/opt/torii/continuum-agent` + unit `torii-continuum-agent.service` on port 8787 — so the old role would have overwritten the session_secret (orphaning the funded Routstr key, which is encrypted at rest under a key derived from it) and/or double-bound 8787. **Fix:** risky detect/backup/migrate/config logic factored into sourceable, unit-tested `ops/lib/continuum-adopt.sh`; `roles/continuum/tasks/main.yml` is now a thin guarded orchestrator that (1) **detects** layout (`fresh`/`adopt-standalone`/`existing-ansible`; existing Ansible always wins); (2) takes a **fail-closed** timestamped root-only 0700 backup of `config.yaml`+`memory/`+`ciphertexts/`+`pending/` from both layouts under `/root/continuum-backup-<UTC>/` before any mutation (no backup ⇒ abort); (3) on adopt **stops+disables** the standalone unit (frees 8787) and **migrates config+state verbatim** (session_secret copied byte-for-byte, never regenerated; idempotent); (4) **never clobbers** an existing config on routine deploy — renders from vault ONLY on fresh install or explicit `-e continuum_allow_config_rotation=true` (off by default), and on preserve runs a one-way drift check that prints only `same`/`differ` (never a value) and surfaces a safe "rotation pending" notice; (5) wraps service+nginx cutover in `block`/`rescue` that on failure prints backup path + exact recovery command. `git force:true` resets only tracked files and all agent state is gitignored, so a re-checkout can't delete encrypted state. New `roles/continuum/defaults/main.yml` (`continuum_standalone_dir`/`continuum_standalone_service`/`continuum_backup_root`/`continuum_allow_config_rotation: false`); secret-touching tasks carry `no_log: true`. Pinned by `ops/test/continuum-adopt.test.sh` (**44** hermetic assertions). New "Adopting a standalone install with Ansible" runbook in `ops/README.md`. **Operator handles NO secrets**; deploy is the normal `ansible-playbook … --tags continuum`. Code + PR only — not merged/tagged/deployed.
- **ONBOARDING-GATING + APP-MOUNT (Continuum PR slice) — real claimed-state Step-3 gating, dup-pay race closed, completion lands on the actual app dashboard. DONE v0.2.39-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.20-preview** (`preview-assets/onboarding-v0.1.20/`). **User-funded Routstr key + all encrypted agent memory + prior payment state preserved — no invoice created/paid/re-submitted, no `memory/**` touched.** **(A) Claimed-state gating (root cause):** v0.1.19 still rendered both setup cards, key-entry/reveal, *Verify & connect* and *Request an invoice* on a verified funded key — a CSS cascade defect, not logic: author `display` rules (`.btn-primary{inline-flex}`, `.wallet-choices/.routstr-paths{flex}`, `.choice-card{grid}`) outrank the UA `[hidden]{display:none}`, so `el.hidden=true` visually no-op'd. Fix: canonical `[hidden]{display:none !important}` in `shared.css`; the status-only resume path funnels through `renderSuccessAdvance` via a single `showClaimed` helper (replaces `showConnected`) so every claimed entry point collapses identically; the panel carries `data-claimed="1"` as single source of truth, leaving one green summary + exactly one standalone Continue. **Dup-pay race closed:** `data-claimed` checked as the first line of quote/key-connect/pay-confirm handlers (pay button also permanently disabled once claimed) — a second invoice/payment is structurally impossible in claimed state. **(B) App mount:** `CONTINUUM_HOME` `/continuum/` → `/continuum/#/dashboard` (+ curtain anchor) so completion opens the multi-view Amber SPA, not the marketing root; open-redirect guard preserved. Session handoff added — `adoptOnboardingSession()` (in `src/data/agent.js`, called once in `main.js` before `initStore()`) fails closed if the SPA already has a live session, else adopts the `localStorage['torii.session']` envelope token into `continuum.session.v1` only if `tokenLooksLive` (shape + unexpired), so a freshly onboarded user is not bounced to the sales/login screen; `deriveSameOriginBase` makes `agentUrl()` fall back to same-origin `/continuum` under the mount (no CDN/third-party origin). **No separate torii-suite PR required** — `/continuum/` alias + subpath refresh + `/continuum/api/*` proxy + SPA build + agent install + nginx fragment + launcher registration are all in this repo's `ops/ansible`; the root launcher is never overwritten. Safe idempotent deploy (operator step, NOT run here, from `ops/ansible/`): `ansible-playbook -i inventory.yml site.yml --ask-vault-pass --tags continuum`. **(C) Wording:** residual VPS/daemon/box copy in `routstr.js`/`landing.js`/`auth.js` scrubbed to "your Torii, your gateway". Tests: root vitest **762/762** (new `src/data/agent.test.js` 11 cases; preview v0.1.20 **153**), agent `node --test` **104/104**, `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Honest limitation: no jsdom/live agent here, so no live funded-agent click-through — gating proven by CSS `!important` cascade guarantee + source/behaviour guards. Code + PR only — not merged/tagged/deployed.
- **ONBOARDING-FINISH (Continuum PR slice) — verified-state cleanup, correct balance units, inline key reveal, deterministic final curtain. DONE v0.2.38-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.19-preview** (`preview-assets/onboarding-v0.1.19/`). Focused live UX/completion pass from three screenshots; no character/animation/CDN/provider-pinning behaviour changed and no payment/NWC semantics changed beyond balance *display* + safe key *retrieval*; funded key + prior state preserved. **(A) Balance units (root cause):** Routstr denominates balances in MILLISATS but `extractBalanceSats` surfaced raw `balance` verbatim (10,000-sat key showed "10000000 sats"); provider adapter now divides msat fields to whole sats (explicit `*_sats` trusted), marker-guarded `displayBalanceSats` migration divides legacy stored envelopes (no `balance_units`) by 1000 on read, new envelopes carry `balance_units:'sat'`, all balances render via `formatSats` (`10,000 sats`). **(B) Step 3 verified state:** `collapseStep3Setup` hides both path cards, both forms, invoice card, progress bar and all three setup CTAs once verified, leaving one green summary + a SINGLE Continue button outside it, no countdown/auto-advance (supersedes v0.1.18 countdown), advance idempotent. **(C) Step 5 recovery kit:** removed the unbranded "Reveal full Routstr key (one-time)" button + `window.confirm` flow (never revealed the key); ROUTSTR KEY row now has branded inline eye + copy controls fetching the key over the existing admin-authenticated no-store `export-key` endpoint, held in memory only (never localStorage/sessionStorage/URL/logs), re-masked on toggle/step-leave/tab-hide/session-loss/timeout; Download click is the explicit confirmation and embeds the full key in the saved file while excluding the NWC secret. **(D) Final curtain:** old curtain only `console.log`'d a commented redirect and spun forever; `initCurtain` navigates deterministically to same-origin `/continuum/` (override only same-origin — open-redirect guard), reveals a real "Open Continuum now" fallback on a bounded timer, honours reduced motion, drops the spinner shortly after the nav attempt so it can never hang. Tests: agent `node --test` **104/104**, root vitest **269/269** (preview v0.1.19 **145**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed.
- **ONBOARDING-PAY-RECOVERY (Continuum PR slice) — paid-but-unclaimed Routstr recovery + payment progress state machine + working recovery kit. DONE v0.2.37-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.18-preview** (`preview-assets/onboarding-v0.1.18/`). Live incident: an operator connected NWC and paid 10,000 sats to fund a Routstr session; the UI said "payment confirmed" but never surfaced the `sk-` key, never auto-advanced, and the Recovery Kit "download" was a no-op that just stepped the deck. **No new invoice created or paid during diagnosis/fix.** Delivered a safe SSH recovery runbook first (inspect `memory/secrets/*.enc` by metadata only — never decrypt; reuse the operator's existing browser session token to curl the loopback admin API; **idempotent `POST /api/onboarding/routstr/recover` with `{}` claims the already-paid key without re-paying**; redacted status/verify; one-time no-store `export-key` only if manual copy is required). Enumerated recovery states: (A) paid+claimed, (B) paid+unclaimed [the live case], (C) unpaid/expired pending. Established **payment-confirmed does NOT imply key stored** (claim is a separate post-settlement step). **Agent (v0.2.37-alpha):** new admin-gated endpoints `GET /recovery/state` (redacted resume snapshot; `claimable` = pending stored AND no key), `GET /recovery-kit` (`Cache-Control: no-store`, secret-free by default), `POST /routstr/export-key` (one-time full-key reveal; `confirm===true` required else 400, 409 when no key, `no-store`, rate-limited, persists `export_count`/`last_exported_at` audit, logs warn never the key). **Preview (v0.1.18):** pure `ONBOARD_PHASES`/`PHASE_META` state machine with an accessible (role=status, aria-live) progress + scanning bar + elapsed timer across connect→quote→pay→claim→verify; **SUCCESS only when agent reports `key_stored===true`** (`classifyPayResult`/`classifyRecoverResult` map bare paid/recoverable → retryable `PAID_UNCLAIMED`, never success); duplicate-payment prevention (Confirm button permanently disabled after first click); success renders redacted key then auto-advances after a short countdown with immediate Continue-now; refresh-resume reads `recovery/state` on load and finishes a `claimable` claim via empty-body `recover` (no re-payment); real Recovery Kit download via Blob (`buildRecoveryKit`) — default excludes NWC secret + full key, carries redacted previews + restoration instructions; full key included only via explicit confirmed one-time `export-key` reveal; Step 5 markup no longer prints static secret-adjacent values. Preserved: character/animation behaviour, no CDN, provider pinning, redaction, rate limits, no autonomous spend, no nsec on server. Tests: agent `node --test` **102/102**, root vitest **453/453** (preview v0.1.18 **124**), `npm run build` clean, `npm audit --omit=dev` **0 vulns** (root + agent). Code + PR only — not deployed.
- **ONBOARDING-ASSET (Continuum PR slice) — new Chiefmonkey GLB + forbidden-safe animation state machine. DONE v0.2.36-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.17-preview** (`preview-assets/onboarding-v0.1.17/`). Replaced the old `chiefmonkey6.glb` with a fresh source GLB, crushed for production by a new reproducible local optimizer `tools/optimize-glb.mjs` (gltf-transform 4.4.1 + draco3dgltf 1.5.7 + sharp 0.35.3, build-time only, no third-party runtime CDN): `dedup → weld → resample → textureCompress(webp,q82,≤1024²) → prune → draco(edgebreaker)`, dropping every forbidden locomotion/knock-down clip via the runtime's own `isForbiddenClip` predicate (single source of truth). **Source 9,298,852 B (SHA `87b0048c…c37dd`) kept OUT of git; optimized `assets/chiefmonkey-onboarding.glb` is 2,347,780 B — 74.75% smaller (target ≥60%), SHA `0253d5e1…e2fcb`**, with a `.manifest.json` (sizes, %reduction, deterministic SHA, retained/dropped inventory). **13 clips retained, 5 forbidden dropped** (`Clapping_Run`/`Knock_Down`/`Running`/`Stylish_Walk_inplace`/`Walking`); mesh/skin(24 joints)/material/texture counts preserved nonzero, clip names survive Draco. `isForbiddenClip`/`filterForbidden` do case-insensitive semantic matching robust to spaces/underscores/camelCase and gate BOTH build-time drop and runtime selection, so a forbidden clip can never be a step clip, a click reaction, or a status phase. `character.js` gained a dedicated forbidden-safe state machine: each of the 5 deck steps plays one dedicated clip from `STEP_CLIPS`/`selectStepClip` (1 Talk_with_Hands_Open, 2 Agree_Gesture, 3 mage_soell_cast_3, 4 Gentlemans_Bow, 5 Idle_10; dormant step-6 Victory_Cheer curtain), applied deterministically on entering/restoring via the v0.1.15 `resolveReadyStep` path (order-independent). Clicking on the model raycasts (`Raycaster.intersectObject`, NDC from canvas rect) and plays one random one-shot from a curated `CLICK_POOL` (`pickClickReaction`, no immediate repeat) then crossfades back to the current step clip on the mixer `finished` event — ignores UI-control clicks (never steals a panel/button click), respects `prefers-reduced-motion`, and ignores click-spam while a reaction is active. Step-1/2/3 prompt/success/failure `onboarding:anim` overrides are transient and always return to the *current* step clip, so async auth/wallet/Routstr outcomes and load order can't race or permanently overwrite step state. Deterministic offline tests parse the optimized GLB JSON chunk (counts nonzero, every referenced clip name survives), assert no forbidden pattern is selectable, verify the committed asset matches the manifest SHA + size budget (≤3 MB, ≥60%), and cover every step mapping, restore ordering, status→current-step return, click hit/no-hit + UI guard + no-immediate-repeat + spam guard + crossfade return, reduced motion, and final load-error. Verified target-file vitest + full root vitest green, `npm run build` clean. **Preview snapshot only — NOT wired into the production Continuum app, code + draft PR only, not deployed.**
- **ONBOARDING-STEP2/3 (Continuum PR slice) — existing-wallet NWC connect + two-path Routstr setup. DONE v0.2.35-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.16-preview** (`preview-assets/onboarding-v0.1.16/`). Reworked Step 2 (Wallet) + Step 3 (Routstr) around real, secret-safe agent flows, replacing the earlier local-wallet/LNbits mockup. **Step 2 — wallet-agnostic NWC connect:** paste an NWC URI (password field, reveal opt-in, cleared on every outcome); agent validates + connects via NIP-47, calls `get_info`, reports a capability matrix; missing optional caps are not rejected but Routstr funding-by-payment stays gated on `pay_invoice` (`can_fund_routstr`). **Step 3 — two paths:** existing `sk-…` key (agent verifies balance/models/info) or fund a new session (quote invoice → explicit confirm → pay via connected NWC → claim minted key). Routstr Lightning contract source-grounded against `Routstr/routstr-core` (`CREATE /lightning/invoice`, `STATUS /lightning/invoice/{id}/status`, `RECOVER /lightning/recover`); a poll timeout returns a precise RECOVERABLE state carrying the bolt11 (sats never lost; UI offers Claim-key retry). **Security:** provider adapter pinned to one https origin (`redirect:'error'`, no SSRF pivot, bounded timeout + body cap + bounded polling, fail closed, constant-safe redaction); secrets never touch storage/URL/logs/errors, submitted only over the authenticated same-origin API, stored encrypted at rest (AES-256-GCM from `session_secret`), only redacted shapes returned; mutation/test/pay/recover routes admin-gated + rate-limited (`onboarding_per_min`, default 12); pay refuses unless `confirm===true` AND wallet advertises `pay_invoice`; no nsec on the server, no autonomous spend. Tests: agent `node --test` **96/96**, root vitest **223/223**, `npm run build` clean, `npm audit --omit=dev` 0 vulns (root + agent). Code + PR #32 only — not deployed.
- **ONBOARDING-STEPSYNC (Continuum PR slice) — deterministic character↔deck step sync (Chiefmonkey after soft reload). DONE v0.2.34-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.15-preview** (`preview-assets/onboarding-v0.1.15/`). Fixed the intermittent "Chiefmonkey stays absent after an ordinary `Cmd+R` soft refresh" bug while Step 2 restore still succeeds. A live browser diagnostic ruled out assets (every file 200, GLB parsed with all clips incl. `Idle_03`, canvas present at opacity 0), leaving startup ordering as the only variable. **Root cause:** `character.js` (ES module) + `deck.js` (classic) execute in non-deterministic order relative to each other and the async GLB load; when `deck.js`'s `onboarding:step` (incl. restored Step 2) fired before the GLB finished, the old `character.js` dropped it (empty `actions` map) then hard-applied `applyStep(1)` in `onLoaded`, reverting the restored step and leaving the stage dark. **Fix (client-only, no server/schema/validation-endpoint/asset change):** new pure injectable step-sync state machine in `onboarding-client.js` (`createCharacterSync`/`recordStep`/`resolveReadyStep`/`markCharacterFailed`); `character.js` records every `onboarding:step` (remembered, not dropped, before readiness) and on load applies `resolveReadyStep(sync, window.__toriiRestoredStep)` → deck's resolved step, else restored session step read at ready time, else Step 1; hard-coded `applyStep(1)` removed; a step arriving after readiness applies immediately (both paths order-independent). Explicit readiness/terminal state + events (`__toriiCharacterReady`/`onboarding:model-loaded`; `__toriiCharacterFailed`/`onboarding:model-error`). Preserved byte-for-byte: retry policy, no-`crossorigin` preload hints, fail-closed restore, NIP-07/NIP-46 flow, signer wording, session shape, the three auth-phase clips; `deck.js`/`index.html`/`shared.css`/assets/`three-libs/` unchanged from v0.1.14. Tests: root vitest preview **51/51** for v0.1.15 (both orderings, reload/cache-hit → Step 1 no revert, out-of-range restore fail-safe, terminal-failure branch), v0.1.14 still 41/41; agent `node --test` 39/39; `npm run build` clean. Root `npm audit` findings are dev-tooling only (vite/vitest/esbuild dev-server; fix needs breaking vite@8, out of scope). Code + PR #31 only — not deployed.
- **ONBOARDING-REFRESH (Continuum PR slice) — restore Step 2 on reload + Chiefmonkey reappears. DONE v0.2.33-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.14-preview** (`preview-assets/onboarding-v0.1.14/`). Live acceptance hotfix: after a successful NIP-07 sign the deck advanced to Step 2, but a plain page refresh dropped the operator back to Step 1 and left Chiefmonkey invisible. Two independent refresh-path root causes, client-only, no server/schema/validation-endpoint change. **RC1 — Step 2→1 on refresh:** `onboarding-client.js` wrote `localStorage['torii.session']` on success but nothing read it back on load, and `deck.js` always hard-started at Step 1. Fix: `restoreSession()` reads + validates the stored session at module load and, when valid, sets `window.__toriiRestoredStep`; `deck.js` opens directly on that step (with an `onboarding:advance` dispatch covering the reverse load order). Validation is fail-closed and adds no server surface — `isSessionValid()` enforces every non-secret invariant the agent's `verifySessionToken` enforces (exact `iat.exp.pubkey.sig` shape, numeric timestamps, unexpired, pubkey match); HMAC secret never needed in the browser; invalid/expired/tampered sessions removed → clean restart at Step 1. **RC2 — Chiefmonkey invisible after refresh:** v0.1.11's watchdog retried the GLB once on an 8s stall, but a reload could make the preloaded same-origin fetch error outright and the old `onErr` merely hid the canvas + cancelled the watchdog so the retry never ran. Fix: both stall and error route through the pure `nextLoadAttempt()` policy (first failure either kind → one cache-busting retry, second → give up, no loop); preload hints untouched (v0.1.11 same-origin no-`crossorigin` fix preserved). Preserved: v0.1.13 bodyless-challenge fix, NIP-07 + browser-client NIP-46 (no server bunker-connect), signer wording, session shape, the three auth-phase clips, no new CDN. Tests: focused preview/auth vitest **41 passed** (restore + loader-retry + no-crossorigin guardrails), agent `node --test` **39 passed**, `npm run build` OK, `npm audit --omit=dev` 0 vulns (root + agent). Code + PR #30 only — not deployed.
- **ONBOARDING-STEP1-FIX (Continuum PR slice) — "agent challenge failed (400)" on Sign with Plebeian Signer. DONE v0.2.32-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.13-preview** (`preview-assets/onboarding-v0.1.13/`). On the live preview the operator clicked "Sign with Plebeian Signer"; Chiefmonkey animated but no signer prompt opened and the panel reported exactly `agent challenge failed (400)`. Root cause was client-only: `onboarding-client.js`'s `postJson` always sent `Content-Type: application/json`, including on the **bodyless** `POST /api/auth/challenge`. The agent's Fastify v5 JSON parser rejects an empty body with that header as `400 FST_ERR_CTP_EMPTY_JSON_BODY`, so step 1 failed before the NIP-07 `window.nostr` prompt (which only fires after a challenge is fetched) — hence animation-but-no-signer. Fix: `postJson` sets the JSON content-type + serialises a body **only when a body is provided**; the challenge call now goes out bodyless (→ 200), verify unchanged. No server endpoint added, no validation weakened, no bunker-connect endpoint. NIP-07 primary + browser-client NIP-46 secondary, `localStorage['torii.session']`, exact "Sign with Plebeian Signer" wording, and the three clips (`HandGesture_00`/`Idle_03`/`Confused_02`) all preserved. New offline vitest case (challenge POST has no body/no JSON content-type; verify still does) + new agent `fastify-v5-api` case pinning the empty-body-400 vs bodyless-200 contract. Verified: root vitest, root build, agent `node --test`, agent `npm audit --omit=dev`. Code + PR only — not deployed.
- **ONBOARDING-STEP1 (Continuum PR slice) — live NIP-07/NIP-46 auth for onboarding step 1. DONE v0.2.31-alpha (NOT yet deployed).** Onboarding preview bumped to **v0.1.12-preview** (`preview-assets/onboarding-v0.1.12/`). New self-contained `onboarding-client.js` (ES module, no build step, no third-party CDN) wires step 1 to the same-origin agent API. **NIP-07** primary via `window.nostr` (button "Sign with Plebeian Signer"): `POST /api/auth/challenge` → sign the exact kind-22242 event `agent/core/auth.mjs` expects (`content == challenge`, `['challenge', …]` + `['relay', origin]` tags) → `POST /api/auth/verify` → store the session at exactly `localStorage['torii.session']` as `{ token, expires_at, pubkey, method, created_at }` (token + public identity only, no secrets). Fails closed on malformed challenge/verify responses, expired challenges, pubkey/challenge mismatch, or an absent token. **NIP-46** secondary with the browser as the client (bunker46 architecture), revealed by "Use a different signer": parses a pasted `bunker://` string and asks the remote signer to sign the same event over its relay — **no server bunker-connect endpoint**, no key/secret sent to the agent, no silent fallback to NIP-07. Chiefmonkey animation phases via the existing event channel: `HandGesture_00` prompting/signing, `Idle_03` success, `Confused_02` failure, each with an ordered fallback to an existing GLB clip (GLB ships neither `HandGesture_00` nor `Confused_02` → graceful keep-current). Desktop-only gate + all prior perf fixes preserved. 28 offline vitest cases (NIP-07 success/failure, storage shape/key, response validation, animation selection, NIP-46 client + no-server-endpoint, no forbidden UI terminology/CDN). Verified: root vitest 28/28, root build, agent `node --test`, ops regressions, agent `npm audit --omit=dev`. Code + draft PR only — not deployed.
- **AGENT-SEC-OPT-TORII-PERMS (Continuum PR slice) — least-privilege fix for the shared `/opt/torii` parent. DONE v0.2.30-alpha (NOT yet deployed).** Production regression found after the v0.2.29 deploy to the SHC VPS: `ops/install-agent.sh` created the **shared** parent `/opt/torii` with an unconditional `install -d -m 0750 -o continuum -g continuum /opt/torii`. Because `install -d` re-applies mode+owner on every run, it clamped the directory torii's *other* apps live under (torii-base launcher, quest tooling) to `0750 continuum:continuum`, stripping its world-execute bit; nginx (`www-data`) could no longer traverse `/opt/torii` to reach `/opt/torii/launcher/index.html`, so `/` fell through to a default nginx 404 (Quest under `/var/www` unaffected). Fix: create `/opt/torii` **only if absent**, `root:root 0755`, and **never re-own/re-mode an existing** shared parent; the agent's own subdir `/opt/torii/continuum-agent` stays locked `0750 continuum:continuum` (nginx never serves from it — loopback-proxied on 8787). New hermetic + anti-drift `ops/test/installer-shared-parent.test.sh` (10 assertions): installer no longer chowns the shared parent to the service user, guards creation with an existence check, creates it `root:root 0755`, keeps `$INSTALL_DIR` locked, and functionally that an existing parent's mode (incl. operator-chosen `0751`) survives a re-run while a fresh parent is world-traversable and the agent subdir stays `0750`. Verified: ops `installer-shared-parent` 10/10 + `installer-preflight` 18/18 + `installer-signal` 9/9 + `nginx-install` 13/13, all `ops/*.sh` `bash -n` clean, agent `node --test`, root build. Operator out-of-band unblock before redeploy if needed: `sudo chown root:root /opt/torii && sudo chmod 0755 /opt/torii`. **Onboarding work moves to v0.2.31-alpha.** Code + draft PR only — not yet deployed.
- **AGENT-SEC-CASHU-LTS (Continuum PR slice) — maintained money-path dependency. DONE v0.2.28-alpha (NOT yet deployed).** Executed the v0.2.27 follow-up: migrated `@cashu/cashu-ts` off the deprecated `2.5.3` onto the maintained **v3-lts "security-fixes-only" line**. Dist-tag evidence at implementation time: `v3-lts → 3.7.1` (latest `4.7.0`; v4 deliberately not taken). Pinned `^3.7.1` per repo convention; lockfile regenerated with sha512 integrity, deprecation notice cleared. Code adapted in `agent/core/wallet.mjs` only: class rename `CashuMint→Mint` / `CashuWallet→Wallet`; boot warm-up `getMintInfo()` (async network in v2 → **sync cached getter that throws pre-load** in v3) replaced by `await wallet.loadMint()`; idempotent `ensureLoaded()` guard added before `receive()`/`send()` (v3 requires `loadMint()` first; verified it skips both network fetches once cached, so v2 lazy-load-on-demand is preserved at zero extra traffic). Token/proof codec, `Token`/`Proof`/`SendResponse` shapes, `receive()→Proof[]` all unchanged (both versions default to token-v4 `cashuB`). **Serialized-state safety proof:** a token encoded by the real 2.5.3 library decodes under 3.7.1 preserving mint/unit/memo + every proof id/amount/secret/C AND re-encodes byte-identically; on-disk `memory/wallet/*.json` survives a JSON round-trip unchanged — no re-mint/conversion/deletion/network mutation. New offline `agent/test/cashu-migration.test.js` (8 cases) pins every used boundary incl. malformed/truncated tokens failing closed without leaking secrets. Verified: `npm audit --omit=dev` + full audit **0 vulnerabilities**; agent `node --test` **38/38**; rate-limit smoke; real `node index.mjs` boot on Node 20.20.1 clean, no new warnings, Fastify 5.10.0 intact; root build; ops 13/13 + 9/9; shell `bash -n`. Prod tree 71→68 packages. **Runtime gate (resolved in v0.2.29):** cashu-ts 3.7.1 declares `engines.node >=22.4.0` across the whole v3-lts line — this is treated as a **HARD deployment prerequisite** for the money path, not an install-time advisory. Node 22 enforcement landed as **AGENT-SEC-CASHU-LTS-RUNTIME (v0.2.29-alpha)** below. **v0.2.26 remains the live server; v0.2.27 the newest shipped code — this slice is code + PR only.**
- **AGENT-SEC-CASHU-LTS-RUNTIME (Continuum PR slice) — enforce the Node 22 money-path floor. DONE v0.2.29-alpha (NOT yet deployed).** Follow-up to the two independent reviews of PR #26, which both requested exactly one blocking change: align the runtime contract with cashu-ts 3.7.1's `engines.node >=22.4.0` so the wallet is not deployed onto an unsupported runtime by default. Changes: (1) `agent/package.json` + agent lockfile `engines.node` `>=20.0.0 → >=22.4.0` (root frontend `package.json` deliberately left engine-free — it is static vite/vitest tooling with no agent runtime, so falsely requiring Node 22 there was avoided). (2) `ops/install-agent.sh` preflight rewritten to gate on a new sourceable helper `ops/lib/node-version.sh::node_version_ok`, doing a **robust major.minor.patch** comparison (NOT major-only — 22.0.x–22.3.x are correctly rejected), erroring with the explicit supported floor and **stopping before any user/service/file is touched**. (3) New host-Node-independent regression `ops/test/installer-preflight.test.sh` (18 assertions) pins the required boundaries — 20.x reject, 22.3.x reject, 22.4.0 accept, later-major accept — plus edges (v-prefix, prerelease suffix, bare major, unparseable→rc2) and anti-drift that the installer still sources the lib and gates before state changes. (4) `agent` `npm test` script `node --test test/ → node --test` (the directory-arg form regresses under Node 22's changed discovery; no-arg auto-discovery is green on both 20 and 22). (5) `ops/README` Prerequisites now state Node 22 LTS is a **hard prerequisite** and that an `EBADENGINE` warning is not an acceptable production state for a wallet; removed "at convenience" language. (6) `wallet.mjs` stale comment `// mintUrl → CashuWallet` → `Wallet`; `ensureLoaded()` in-flight dedup evaluated and left as-is (documented non-blocking: loadMint is idempotent, boot warm-up primes each mint, worst case is duplicate idempotent fetches, never a double-spend). cashu-ts held at **3.7.1** (version choice unchanged); all v0.2.28 compatibility fixtures preserved. **Verified under a real Node 22.11.0 runtime** (fetched as a non-global tarball; sandbox default is Node 20.20.1): `npm ci --omit=dev` clean with **no EBADENGINE**, `npm audit` + `--omit=dev` **0 vulnerabilities**, agent `node --test` **38/38**, ops `installer-preflight` 18/18 + `installer-signal` 9/9 + `nginx-install` 13/13, root `npm ci` + build green, vitest (no frontend specs) exit 0, and a real `node index.mjs` boot → `/api/health` **200**, version `0.2.29-alpha`, **no deprecation/EBADENGINE/experimental warnings**. **Deploy prerequisite:** the VPS must be upgraded to the Node 22 LTS line before `ops/install-agent.sh` will run; until then **v0.2.27 remains the newest shipped code and v0.2.26 the live server**.
- **AGENT-SEC (Continuum PR slice) — production dependency remediation. DONE v0.2.27-alpha (NOT yet deployed).** Cleared the 5 HIGH production advisories `npm ci --omit=dev` reported in `agent/` during the live v0.2.26 deploy, all rooted in `fast-uri` (path-traversal GHSA-q3j6-qgpj-74h6 + host-confusion GHSA-v39h-62p7-jpjc) cascading through `@fastify/ajv-compiler` / `fast-json-stringify` / `@fastify/fast-json-stringify-compiler`, plus a Fastify Content-Type body-validation bypass (GHSA-jx2c-rxcm-jvmq) with no v4 backport. Fix: `fastify ^4.28.1 → ^5.10.0`, `@fastify/cors ^9 → ^11`, `@fastify/rate-limit ^9 → ^11`, lockfile regenerated (`fast-uri` → patched 3.1.3/4.1.0); the only app change was dropping the explicit `disableRequestLogging: false` (passing it at all — even the default — trips Fastify v5's `FSTDEP023`, removed in v6; the drop is a no-op that silences the warning, request logging still emits; v4→v5 was otherwise API-transparent for this daemon). `@cashu/cashu-ts` held at 2.5.3 — **no published npm/GHSA advisory**, so it does not block this HIGH-clearing hotfix; but the deprecation points to `@cashu/cashu-ts@v3-lts` (upstream "security-fixes-only LTS"), so a v3-lts/v4 migration is a **security-relevant money-path follow-up to prioritise** (touches proof/token handling), deferred to its own slice — not merely spec/maintenance cleanup. Post-fix `npm audit --omit=dev` = **0 vulnerabilities**. New offline tests: `agent/test/wallet.test.js` (wallet guard/failure paths + cashu-ts codec regression) and `agent/test/fastify-v5-api.test.js` (cors + rate-limit contract under fastify 5). Verified: agent `node --test` 30/30, rate-limit smoke, root build, ops regression 13+9, shell `bash -n`. **v0.2.26 remains the currently deployed server version until an operator re-runs `ops/install-agent.sh`.**
  - **Follow-up (security-relevant, prioritise — not part of the hotfix): DONE v0.2.28-alpha** as **AGENT-SEC-CASHU-LTS** (see the entry above). Migrated `@cashu/cashu-ts` 2.5.3 → v3-lts (3.7.1); adapted `agent/core/wallet.mjs` (`Mint`/`Wallet` rename, `loadMint()` boot warm-up, `ensureLoaded()` guard) and added offline `agent/test/cashu-migration.test.js`. v4 (4.7.0) deliberately not taken — the maintained v3-lts line is the minimal safe security move for the money path.
- **SUITE-VPS-READY-2 (Continuum PR slice) — standalone agent deploy tooling + first-touch admin claim. DONE v0.2.26-alpha** — shipped `ops/systemd/torii-continuum-agent.service` (locked non-login `continuum` user, `/opt/torii/continuum-agent`, `NODE_ENV=production`, safe restart with `StartLimitBurst`; `ProtectSystem=strict` with only `memory/` + the single `config.yaml` file writable, `NoNewPrivileges`, `PrivateTmp`/`PrivateDevices`, kernel/proc/clock protections, `MemoryDenyWriteExecute`, `@system-service` syscall filter, empty caps, `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX`), `ops/nginx/torii-api.conf` (server-scoped same-origin `location /api/` → `127.0.0.1:8787` + edge `limit_req` 30r/s burst 60 as a second layer above the in-process limiter; `limit_req_zone` documented + shipped to the http-context `conf.d/` fragment; `X-Real-IP` bucket-key passthrough), `ops/install-agent.sh` (strict-mode idempotent installer: root + dependency preflight, locked user, `rsync --delete` that never clobbers `memory/`/`pending/`/`ciphertexts/`/`config.yaml`, `npm ci --omit=dev`, one-time `config.yaml` gen with fresh `openssl rand -hex 32` `session_secret` written 0600 and never echoed, config validation before restart, systemd + nginx wiring with `nginx -t` before any reload and a refuse-to-redeclare-zone guard, bounded `/api/health` liveness proof; `SKIP_NGINX`/`SKIP_HEALTHCHECK`/`TORII_NGINX_SITE` overrides), and a refreshed `ops/README.md` runbook. First-touch admin bootstrap in `agent/core/auth.mjs`: empty `admin_npub` → agent boots unclaimed; first valid NIP-07 verifier atomically claims admin (npub persisted 0600 to `config.yaml` via injected `persistAdminNpub` in `config.mjs`, in-place rewrite that preserves comments + re-parses as a YAML sanity check + refuses malformed npubs), race-safe via an in-flight promise guard and fail-closed (persist error → no token, box stays claimable); configured-admin path unchanged; `/api/health` reports `admin_claimed`; logging stays prefix-only. Tests: 13 agent unit tests (`node --test`) covering claim success/single-persist, restart-honours-persisted, second-caller-rejected, configured-admin, bad/wrong-kind sig no-claim, concurrent one-winner, persist-fail + no-persister fail-closed, and 4 `persistAdminNpub` temp-tree tests. **The agent is NOT deployed by this slice** — running `install-agent.sh` on a server is a separate operator step.
- **SUITE-VPS-READY-1 (Continuum PR slice) — rate-limit auth surface + bounded challenges Map + structured logs. DONE v0.2.14-alpha** — registered `@fastify/rate-limit@^9.1.0` with `global: false`; route-scoped configs on `POST /api/auth/challenge` (10/min/IP) and `POST /api/auth/verify` (20/min/IP) return `429 { ok:false, reason:"rate_limited", retry_after_sec }` with `Retry-After`; `agent/core/auth.mjs` rewritten around a `MAX_CHALLENGES`-bounded Map (default 1000, LRU-by-expiry eviction with a warning log) and takes `deps.log` for structured JSON events (`auth.challenge.issued|evicted`, `auth.verify.success|fail` with a `reason` enum, `auth.ratelimited`); all prefix-only, never full pubkeys/challenges/IPs. Config knobs live under `rate_limit:` in `agent/config.example.yaml`; `cfg.rate_limit.enabled: false` disables the plugin entirely. Local smoke `agent/scripts/smoke-rate-limit.mjs` proves 429 at N+1, cap enforcement, evict logs, and disable-path passthrough. Suite install slice (systemd unit, nginx fragment, arena-ws) tracked in `torii-suite-v0.6-plan.md` (items G–P) and lands in a separate suite PR.
- **CONT-HEALTH-1 — dashboard provider reachability card. DONE v0.2.13-alpha** — `#/dashboard` now shows a live Provider card polling `/api/health/models` every 20s. Routstr = `Enabled` (no server-side reachability probe yet); Ollama = `Reachable`/`Unreachable`/`Disabled` with reason line. Self-cleans on route change; graceful when logged-out or when `VITE_AGENT_URL` is empty. Bonus: `/api/health*` responses now read version from `agent/package.json` at boot instead of the hardcoded `0.2.6-alpha` string.
- **CONT-HEALTH-2 — real Cashu wallet health probe (next).** `torii doctor` today only pings the mint URL. Add a signed wallet-info call so it verifies the mint's public key and reports actual balance. Ship as a new row in the same Provider card + a new admin-gated `/api/health/wallet` endpoint mirroring `/api/health/models`.
- Keep Continuum as the separate oversight app and do not merge its task queue back into Quest.
- Prepare Continuum to use the same safe assistant-editable .md pipeline as `torii-quest-todo.md` so Continuum todo updates can be made without manual copy-editing. **BUILT v0.2.259** — `torii-continuum-todo.md` is now in the `mdPatch` whitelist (full append/replace/note/list); `npm run md:patch -- note torii-continuum-todo.md "..."` appends a timestamped note under "Active tasks". (Note: the `md:patch` tool itself lives in the `torii-quest` repo; Continuum consumes it. When invoking from within Continuum, adjust the target-file argument for the current working directory.)
- Keep Continuum work read-only / mockup-first unless a live admin action is explicitly required and approved.
- **CONT-AGENT-1 — Self-Learning Continuum v1 (VPS agent skeleton, drafts-only, no key material).** **PARTIAL v0.2.0-alpha** — invariants scaffold shipped: `agent/` Fastify daemon with NIP-07 challenge/verify, HMAC-signed session tokens, Cashu wallet on VPS (`@cashu/cashu-ts` v2, per-mint proofs at `memory/wallet/`, mode 0600), Routstr chat client (OpenAI-compat, DeepSeek-Chat default, DeepSeek-Coder-V2 for coding, fallback ladder, per-request Cashu payment, `memory/costs.jsonl`), first `chat` skill. Frontend integrations: landing page at `#/`, sidebar Login button (Plebeian Signer), chat dock routes through `/api/chat` when signed in, Routstr Connect opens real Cashu-token top-up modal. Docs: `agent/README.md`, `agent/PRIVACY.md`, slice report `torii-continuum-v0.2.0-cont-agent-1-report.md`. **Deferred**: `brain.read`/`brain.write`/`todo.patch`/`nostr.draft` skills (CONT-AGENT-2/3), local Ollama fallback (CONT-AGENT-1b), Cashu refund tracking, `pending/` drafts panel in Console. Turn Continuum from a read-only mockup dashboard into a sovereign personal AI + project engine. **VPS-only in v1** — Linux/Mac clients come later as thin frontends. **No nsec on the VPS**: signing lives in the browser via **Plebeian Signer** (NIP-07). **No plaintext Nostr write path**: `nostr.draft` skill only produces NIP-17 gift-wrapped envelopes; the plaintext code path is absent, not disabled. **Model layer = Routstr**: pay-per-request in Cashu against pinned providers, OpenAI-compatible client, per-skill model pinning + fallback ladder present-but-off. **Human-in-the-loop by construction**: agent drafts to `agent/pending/*.json`; Continuum Console renders; user approves; Plebeian Signer signs; browser publishes.

  **Scope (v1 surface intentionally small)**:
  1. **Repo layout** — new `agent/` folder under `torii-continuum` with `core/` (model client + skill runtime + memory), `cli/` (`torii ask "..."`), `console/` (HTTPS endpoint the Continuum Console page calls), `nostr/` (NIP-17 listener, read-only in v1), `skills/`, `memory/`, `pending/`, `config.yaml`. Dedicated `continuum` user on the VPS, `/home/continuum/agent/` chmod 700.
  2. **Four skills, only these**:
     - `brain.read` — read-only under `projects/torii-*/knowledge/`, `memory/knowledge/`, `memory/notes/`.
     - `brain.write` — write only under `learnings/YYYY-MM-DD.md` (new) and `changelog.md` (append). Refuses elsewhere.
     - `todo.patch` — wraps existing `tools/mdPatch.mjs` (Quest repo). Permitted files + capability map inherited from mdPatch-2.
     - `nostr.draft` — builds NIP-17 gift-wrapped envelopes only (inner kinds 14 / 30078 / 30081); writes to `agent/pending/`; never signs, never publishes.
  3. **Model routing** — Routstr as default, cheap fast model for skill work, small conversational model for chat; per-skill pins encoded in `config.yaml`. Cost log at `agent/memory/costs.jsonl` — `{skill, model, tokens_in, tokens_out, sats_spent, at}`. No prompt bodies logged.
  4. **Cashu float** — single Cashu wallet on VPS, ~5k sats initial float, refill from Plebeian Signer's built-in Cashu wallet.
  5. **Continuum Console wiring** — wire the previously-inert Console page to the VPS agent HTTPS endpoint. Show live chat + a "pending drafts" panel that renders `agent/pending/*.json`; each draft has an Approve button that hands the payload to `window.nostr.signEvent` (Plebeian Signer) and publishes via the existing relay pool. Preserves the current read-only default — the Approve button is the only live action.
  6. **Memory** — grep-able markdown/JSONL under `agent/memory/`. Naive keyword retrieval in v1 (no embeddings). `sessions/YYYY-MM-DD.md` for transcripts, `notes/*.md` for durable facts, `index.md` auto-maintained catalog.
  7. **Success criteria** (blocking merge): agent runs on VPS + restarts cleanly + chmod 700; `torii ask "summarise today's Brain changelog"` returns a grounded cited answer; `torii ask "add a slice for X to Quest todo"` produces a valid `md:patch note` command, executes it, appends matching learning to today's `learnings/YYYY-MM-DD.md`; Console page shows agent state + drafts + Plebeian Signer approves and publishes; cost log accumulates realistic sats spend with no runaway calls; **zero nsec anywhere on the VPS**; **zero plaintext Nostr writes** — both are structural (code paths absent), not policy.

  **Explicitly NOT in v1**: autonomous signing / publishing; NIP-46 bunker (deferred to v2 pending Plebeian Signer NIP-46 support); local Ollama fallback; Quest NPC bridge (`quest.npc.talk`, v2); dev-help watcher (v2); marketplace worker skill (v2); embedding-based retrieval (v2); own relay / own Cashu mint (v2).

  **Docs**: create `agent/README.md` (install / VPS bring-up / Plebeian Signer pairing / Cashu topup / Routstr provider pinning) and `agent/PRIVACY.md` (encryption invariants, points at `concepts/privacy-first-nostr.md` in the Brain). Slice report `torii-continuum-vX.Y.Z-cont-agent-1-report.md` in the Continuum repo; do NOT bump Quest version. Continuum picks up its own versioning cadence with this slice.

  **Risk**: medium — introduces a new runtime surface. Mitigations: v1 has no autonomous publish path, no signing, no key material on the VPS, `chmod 700` filesystem, no plaintext prompts in logs. Every publish requires a browser click. Kill-switch: `systemctl stop continuum-agent` on the VPS.

  **Reference**: Space Brain — `projects/self-learning-continuum.md` (architecture + skill definitions + success criteria), `concepts/privacy-first-nostr.md` (encryption invariants), `entities/routstr.md` (model layer), `entities/plebeian-market.md` (signer).